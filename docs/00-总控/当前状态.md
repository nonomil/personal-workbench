# 当前状态

> 核对日期：2026-08-21。工作台发布记录仍是 **v0.7.4**，详见 `docs/04-发布/v0.7.4.md`。方块传奇独立仓本版是 **0.9.3**。下文旧段落未整篇重写。
>
> 本文记录事实，不把计划当作完成。

## 2026-08-21 方块传奇独立仓 0.9.3

- 产品名仍是「我的方块学园」；文件夹 / `GAME_ID` / Android `appId` 未改。
- 仓库：https://github.com/nonomil/blocklegend ；网页：https://nonomil.github.io/blocklegend/
- 本版：5 个营地广场、13 座可进房子、河湖小山、屋内床和家具。工作台 APK 仍不含本游戏。
- 说明见独立仓 `CHANGELOG.md` 与 `docs/发布/v0.9.3.md`。

## 2026-08-20 方块传奇独立仓 0.9.2

- 产品名仍是「我的方块学园」；文件夹 / `GAME_ID` / Android `appId` 未改。
- 仓库：https://github.com/nonomil/blocklegend ；网页：https://nonomil.github.io/blocklegend/
- 本版：语音 HUD、动作词（jump/cut/swim/eat/ride）、每关一句中英剧情。工作台 APK 仍不含本游戏。
- 说明见 `docs/01-方案/工作台小游戏设计/05-方块传奇/00-README.md` 与独立仓 `CHANGELOG.md`。

## 2026-08-20 方块传奇独立仓 0.9.1

- 产品名仍是「我的方块学园」；文件夹 / `GAME_ID` / Android `appId` 未改。
- 仓库：https://github.com/nonomil/blocklegend ；网页：https://nonomil.github.io/blocklegend/
- 本版：512 世界、地形高 48、骑龙第三人称、说中特效、气候地标。工作台 APK 仍不含本游戏。
- 说明见 `docs/01-方案/工作台小游戏设计/05-方块传奇/00-README.md` 与独立仓 `CHANGELOG.md`。

## 2026-08-19 发布快照 v0.7.4

- 页脚 / `package.json` / Android stamp 已改为 `0.7.4` / `versionCode=704`。
- 平板结论：工作台三界 + 课程适合安卓平板；方块传奇要指针锁和键盘，不进 APK，启动页不再放入口。
- 本版纳入：英语 mastery 方案 A +「今天练这个」、共享 `speech-match.js`、`core-*-2026.08.19` 多科词包。
- APK/MuMu 未按 0.7.4 重验，口径 PARTIAL。方块传奇仍只在本地独立仓。

## 2026-08-19 地下城式关卡 + 词级记忆（dungeon-anki-uplift）

- 性质：五文件已绿，浏览器含隔离新档 A1，**不标 accepted**。
- 包：`docs/plans/T20260819-dungeon-anki-uplift/`。D1 词级 Leitner 5 盒 + 词力；D2 默认/冒险/天启三档；D3 卷轴→词灵回廊 + 今日层营地（假人/商人摊/直接进关）；D4 难词第 3 次见面降级支架 + 结算学习密度。
- 证据：隔离空档 kite 首对盒 2 / 3 天、tree 首错盒 1 / 1 天，词力 0→2→3。`memoryFromProgress` 不再把 `learnedIds` 水成幽灵盒。E2 17 项未改断言。`game.js` 戳与 E2 断言同为 `camp3`。
- 存储：`growth.worldGames.blocklegend` 有 `wordMemory` / `clearedTiers` / `scrolls` / `stats.sessionDensity` / `secretLooted`。无新顶层 key。旧档已还原。
- 门控债：不与 W1 mastery 跨轨对齐；独立仓未推（本地无 `.git`）。


## 2026-08-19 Echo Loop 借鉴方案分析（echoloop-borrow）

- 性质：分析记录，不是已完成的改造。方案包 `docs/plans/T20260819-echoloop-borrow/`（README + 01–04 共 5 份）。
- 内容：分析 AGPL 项目 Echo Loop（`prj/games/Echo-Loop-main`，本地参考、已 gitignore 不入库）的听说学习闭环，产出方块传奇 E1–E4 借鉴分期（跟读评测引擎 / 复习之门+今日任务 / 语境词卡+难词本+听说统计 / 句子跟读关）与工作台英语体系升级建议（今日任务队列、mastery 间隔升级+版本快照、复习事件日志）。
- 许可口径（用户 2026-08-19 确认）：可直接参考源码提炼思路参数，唯一纪律是不复制代码/资源；AGPL 义务只在对外分发或网络服务时触发。关键算法参数已提炼进包内 `04-机制参数速查.md`，实施可不翻原仓。
- 顺手发现（记录在包内 02 §5）：bridge `total` 已改为 12；游戏内 plans Phase 9–12 已回真；`.gitignore` 注释改为独立仓说明。本地 `prj/games/blocklegend/` 仍无 `.git`，独立仓未推本轮改动。
- 拍板（2026-08-19 上午）：mastery 间隔走**方案 A**（新词 v2 课表、存量 v1 双轨，零迁移）。执行包已建三个：`T20260819-E1-speech-match`、`T20260819-W1-mastery-uplift`、`T20260819-E2-review-gate`。
- E1 已落地（2026-08-19 午）：共享模块 `prj/games/shared/speech-match.js`（lemma + LCS 覆盖率 + 分场景阈值），blocklegend 跟读/Boss/练一句走同一判定。证据：`tests/speech-match.test.mjs` 10/10，`tests/blocklegend.test.mjs` 187/187。未手玩、未同步独立仓。
- W1 S1 已落地（2026-08-19 午）：英语/MC mastery 每词追加 `events`（最多 20 条），`normalize`/`cloneProgress`/`saveMinecraft` 会保留；方块传奇和 wordboss 回流带 `source`。
- W1 S2 已落地（2026-08-19 午）：新词 `planVersion=2` 走 `[0.25,1,2,4,7,14,28]` 天，存量无版本字段走 v1 `[1,3,7,14]`；`nextReview` 新写 ISO；到期 48h 后才 `isOverdue`，不降级。
- W1 S3 已落地（2026-08-19 午）：`selectTodayTasks` 排序 overdue > due > 24h 内到期 > 新词配额（默认 3）；英语专区置顶「今天练这个」卡，完成翻「今日已完成 · 连续 X 天」。证据：调度测试 12/12。未手玩。不标 accepted。
- E2 S1 已落地（2026-08-19 午）：`prj/games/blocklegend/data/review-schedule.js` 七轮链式课表 6/18/24/48/72/168/336 小时，第 1 轮宽限 6h、其后 24h，七轮后毕业。
- E2 S2 已落地（2026-08-19 午）：顶栏「门」列出到期发光 / 倒计时 / 待复习；进门打 2–3 波+降配 Boss；结算金币减半、阳光 `bl-review-<关>-<轮>` 幂等；`__blDebug.skipHours(6)` 可快进。
- E2 S3+S4 已落地（2026-08-19 午）：今日冒险首屏（到期门优先，否则「推进新关」）；图鉴翻卡播例句；`hardWords` 错 2 次入本、复习连对 2 次出本；家长报告「听说比」。证据：`tests/blocklegend-review-schedule.test.mjs` 17/17，`tests/blocklegend.test.mjs` 188/188。未手玩。独立仓未推。完整测试步骤见 `docs/plans/T20260819-echoloop-borrow/05-完整测试方案.md`。不标 accepted。
- E4 已落地（2026-08-19 午）：练一句改为播原句→跟读三遍→遍间停顿；句记录在 `sceneSentences`。证据：`tests/blocklegend-scene-loop.test.mjs` 8/8。未手玩。不标 accepted。

## 2026-08-16 发布快照 v0.7.0

- 页脚 / `package.json` / 配置已改为 `0.7.0`。
- 本版纳入：识字升级、认单词五件套、MC 宠物蛋与方块基地、设置页单列、工作台对照 W1–W3、方块传奇抛光。
- 全仓最近记录 468/475，7 项既有失败未清。APK/MuMu 未按 0.7.0 重验，口径 PARTIAL。
- 仓库整理：忽略 `.playwright-mcp/`、`prj/games/_backup/`、`--/`、生图 recon。

## 2026-08-16 宠物蛋合入（PVZ / 我的世界素材）

- 参考 `G:\StudyCode\宠物积分系统` 的蛋→养成，**没有**搬 261 种、战斗、HP、技能或第二套积分。
- 幼儿花园基地现有 9 颗蛋：向日葵 / 豌豆射手 / 坚果墙 / 樱桃炸弹 + 史莱姆 / 蜜蜂 / 雪人 / 铁傀儡 + 探险星芒。
- 我的世界工作台蛋巢只显示 MC 角色：史莱姆 / 蜜蜂 / 蜘蛛 / 蝙蝠 / 雪人 / 铁傀儡 / 烈焰人。立绘复用方块世界素材（宠物积分系统的 wolf/cat webp 本机不在）。合同新增 voxel 主题过滤。
- 立绘复用已发布 PVZ PNG 与方块世界敌人/物品图。未解锁蛋变灰；破壳后不能换蛋。
- 状态仍写 `growth.pet`（新增 `speciesId`），喂食仍扣 5 阳光。合同 `tests/preschool-pet.test.mjs` **15/15**。
- 我的世界工作台成长页已改成「方块基地」：主按钮去方块世界，不再浇水/保卫战，折叠区去掉植物图鉴和星芒造型；今日做完后下一步是「去方块世界探险」。未解锁蛋标明「方块 N 关」。
- 共享 `growth.pet` 若还是花园蛋，方块基地只显示史莱姆蛋巢；喂食会写成 `mc-slime`，不另开 key。顶栏同步「方块基地」。未标 accepted。

## 2026-08-16 认单词优化（english-word-uplift）

- 任务包 `docs/plans/T20260816-english-word-uplift/`：用户已确认裁决表。S1–S5 均已落地，定向 **30/30**。未标 accepted。
- 今日 3 词：speak → quiz（听音选图 / 看图选词）→ match → spell。无配图词跳过 quiz。
- 口径偏差（兼容）：`markKnown`（会了 / 游戏 `recordWordAnswer`）仍一次正确即 ready。客观题 `recordQuizAnswer` 才走「≥3 次且 ≥2 类」。
- S2 英语错词本：mistakes.errorType = listen/read/spell；专项复习 3 次出本。浏览器：cake 听力误判。
- S3 词汇档案：`masteredAt` + SVG 曲线（80/300）+ 三维正确率。
- S4：`scripts/build-english-vocab-svgs.mjs` 补齐 **80/80**（未走 grok）；wordboss 16 张 SVG。
- S5：`prj/games/wordboss/` 拼词放技能；金币仅局内；不进 `GAME_IDS`。浏览器拼 water → 金币 10、哥布林王掉血。
- 浏览器未走完：match/spell、错词本整轮复习、三关通关。

## 2026-08-16 识字体验升级（literacy-uplift）

- 任务包 `docs/plans/T20260816-literacy-uplift/`：S1 25 题识字量测评，S2 同 key 扩 `assessments`（≤24）+ 家长摘要，S3 错字本字卡/专项复习/打印，S4 SVG 档案+证书，S5 首页 KPI+三大卡。
- 定向测试：识字/错字/摘要 **18/18**，无新 localStorage key，测评不发阳光；低置信度记录不刷新最高纪录。
- Codex 浏览器已走通：25/25 高置信度测评（1500 字、100%）、结果→错字本→专项复习→打印 8 卡→档案→证书；768px 无横向溢出，390px/桌面证据见任务包截图。
- V1–V3 已完成：9 个 RGBA 素材已接入 `prj/assets/generated/preschool-literacy-uplift/published/`，本地资源/回退/A4 2×4 打印 CSS 已验收。
- 任务包仍标 `review`：仓库全量 `npm test` 当前 468/475，7 项为花园/资源包/图标/发布素材等范围外既有失败，详见 `docs/plans/T20260816-literacy-uplift/test-report.md`。

## 2026-08-16 方块传奇画面抛光（mc-polish）

- 任务包 `docs/plans/T20260816-blocklegend-mc-polish/`：S1 图集+裂纹瓦片，S2 树缓存+顶点 AO，S3 挖掘裂纹遮罩，S4 世界后扩到 **192×192** + 区块流式，S5 文档收口。
- 续做地图：6 气候关卡 + 虫洞洞穴 + 煤/铁矿 + 水塘/小溪 + 平原/森林/暮谷出生点东侧橡木村。仍非无限世界。水上可走、不游泳。
- 定向 `node --test tests/blocklegend.test.mjs tests/world-games.test.mjs`：**61/61** 绿。预览 `prj/games/blocklegend/index.html?v=20260816-bl-fight2`。未标 accepted，等手玩。
- 词卡弹出时解开指针锁，鼠标可点选项；答完自动锁回视角。每只怪只问一次，Boss 仍每刀问。选项也可按 1–4。
- 2026-08-16 续：左键按住对着怪会连续挥剑（不再只挖地）；近战距离 4.5。出生点附近金色单词方块，挖掉 +3 金币 +4 HP，并写入已有 `learnedIds` / `recordWordAnswer`。不另开积分或经验条。

## 2026-08-15 方块传奇设计分册与 sim 包

- 方案落在 `docs/01-方案/工作台小游戏设计/05-方块传奇/`（身份、循环、HUD、薄商店、放置手、边界）。
- 增量任务包 `docs/plans/T20260815-blocklegend-sim/`：S1 买装备/药水，S2 热键 5 放置，S3 收口。
- 首版 S1–S6 仍待手玩验收，见上一节。未标 accepted。

## 2026-08-15 方块传奇 blocklegend S2–S6

- 新建可玩页 `prj/games/blocklegend/`：近战扇形 + 追踪弹 + 词卡四选一暴击 + Boss 盾状态机 + 金币解锁 + F 商人 + 全英文帮助。
- 进度写在既有 `growth.worldGames.blocklegend`（unlockedLevel/coined/learnedIds/rightCount/wrongCount/clearedLevels），无新 localStorage key。
- bridge 增量：`GAME_IDS` 含 `blocklegend`；`hasTripleDay` 改为当日 ≥3 世界（花园+方块+闯关仍真）。
- 方块主题首页有「方块传奇 · 学英语」入口卡；必做未完时与冒险出口一并置灰。
- 合同：`tests/blocklegend.test.mjs` 14/14；`tests/world-games.test.mjs` 22/22。全量 `npm test` 377/384，7 个既有失败未修。浏览器：60fps、词卡暴击 24、刷新后计数/金币保留、320/390/1280 无横溢、无 CDN。MuMu 未验。未标 accepted。

## 2026-08-15 B2 练习入口与错题回流

- 今日六项核心任务都有 `practiceLessonId`，首页未完成项显示同级「去练习」。
- 错题复习队列由 `mistakes[].date` 派生：恰好第 1/3/7 天且未 mastered 才入队；练对标 mastered。无新 storage 字段，主 key 未换。
- 首页队列非空时出现「今天复习 N 题」。
- 合同：`preschool-daily-plan` + `preschool-lesson-mistakes` 绿。全量 `npm test` 347/352，5 个既有失败未修。浏览器走查未做，包未标 accepted。
- B3 S1 已落地：阳光商城兑换先进入 `pendingRewardIds`，家长长按 2 秒才扣阳光核销。未标 accepted。
- B3 S2 已落地：今日必做未完时冒险入口置灰并提示「先完成今日必做」；三项必做完成后点亮三世界。选做未完不挡游戏。
- B3 S3 已落地：完成课弹层出现「去喂星芒」；今日卡显示「连续学习 n 天」（复用打卡天数，含补签）；重开已完成课提示「已领过阳光，再练不加分也不扣分」。无新 storage 字段。未标 accepted。
- B3 S4 已落地：英语专区「今日 3 词」来自 80 词日定量表；会了的进「我的词库」；到期复习标黄。597 词运行库未替换。未标 accepted。

## 1. 总体判断

项目已经具备三套可运行工作台、根入口五种选择、幼儿版花园玩法、本地快照、课程练习、JSON 导入导出和 Android 包装基础，但还不是“稳定发布候选版本”。当前资料库已经较重，工作台运行时反而应该收敛为打卡、简单测试、奖励和游戏出口；独立的“花园保卫战”小游戏 MVP 已完成，首页进一步收敛为草坪战场打卡，不承载战斗棋盘。

## 2. 已经存在

| 领域 | 当前事实 | 证据 |
| --- | --- | --- |
| 入口结构 | 三套运行工作台；根入口上方为三个幼儿游戏世界、下方为成人与少儿两个通用工作台 | `index.html`、`成人成长工作台/`、`儿童学习工作台/`、`preschool-workbench/` |
| 幼儿花园 | 植物、入侵者、豌豆能量、收集和奖励逻辑已存在 | `preschool-garden.js`、`app.js` |
| 花园保卫战 MVP | 5 路 × 6 列独立棋盘；植物选择、放置、自动行动；普通/路障/铁桶三类僵尸；进入 battle 才启动 tick | `preschool-garden.js`、`app.js`、`css/preschool/17-defense-game.css` |
| 本地状态 | 幼儿版有独立快照、刷新恢复和 JSON 导入导出 | `storage.js`、`docs/data-model.md` |
| 研究资料 | 已有 Tavily 结果、Firecrawl 正文、GitHub README/许可证存档 | `docs/03-研究与参考/幼儿学习工作台研究/` |
| 课程数据 | 自然拼读已补齐 60 节课程、94 个词和 36 个短句；其他科目有资料包，但不代表已经接入工作台运行时 | `data/preschool/english/phonics/`、`data/preschool/` |
| 发布基础 | GitHub Pages 和 Capacitor Android 流程已配置 | `README.md`、`.github/workflows/`、`android/` |

## 3. 当前未完成

| 问题 | 事实 | 风险 |
| --- | --- | --- |
| 测试基线 | 幼儿工作台专项 48/48 通过；最新全量测试 112 项中 112 项通过；`node --check app.js`、`node --check storage.js`、`node --check config.js`、`git diff --check` 通过 | 网页功能基线已转绿，发布仍需独立验收 |
| 运行数据与课程包 | 七条课程路线已经有完整 docs/data/assets/tmp 包；页面运行时接入仍需逐科验收 | 数据齐全不等于网页功能已完成 |
| 文档来源分散 | 日期计划、幼儿课程总控、单科资料包并存 | 容易出现两个主线 |
| 工作树有分叉 | 主项目已回写本轮分析；`幼儿-游戏场景版`仍有未合并的计划/研究文档；另有未登记的资料片段目录 | 正式来源仍是主项目 `docs/00-总控`，不得把 worktree 当长期资料库 |
| 统一入口已经历本轮回归 | 当前状态、进度看板和优化分析已记录最新 108/108 基线、五入口顺序、首页两层结构和轻量练习浏览器证据 | 后续继续维护唯一总控，不能回到日期计划堆叠 |
| 工作台与游戏边界 | 工作台应负责打卡、简单测试、奖励和去游戏；趣味动画集中在独立游戏 | 避免首页继续膨胀成课程和游戏混合平台 |

## 3.1 本轮花园保卫战 MVP

### 已实现

- 页面名称统一为“阳光成长工作台”，游戏入口统一为“花园保卫战”。
- battle 页面拥有稳定的 5 路 × 6 列棋盘；首页只保留任务、阳光、豌豆能量、去游戏和领奖励出口。
- 选择卡会写入当前植物并改变放置/攻击规则：豌豆射手产生投射物，坚果墙只阻挡，向日葵按 tick 产阳光。
- 一波最多同时出现三只僵尸，普通、路障、铁桶具有不同生命值和移动节奏；状态刷新后保留棋盘实体和波次。
- 离开 battle 页面会清理自动 tick；新页面不复用旧页面的定时器或动画回调。

### 状态与接入边界

- 沿用 `petbank_huchuliang_preschool_workbench_state_v1`，没有新增 localStorage key；新增业务状态位于 `growth.garden.defense`。
- 战斗消耗现有 `growth.sunlight`；任务和活动继续通过现有奖励/成长逻辑增加阳光与 `garden.defenseEnergy`，战斗层不直接写 localStorage。
- 游戏仍沿用现有奖励页面和页面路由，只提供“去游戏/回到工作台/去领奖励”出口；未改成人、儿童入口。

### 验收证据

- `node --test tests/preschool-garden.test.mjs tests/preschool-defense-game.test.mjs tests/preschool-workbench-refresh.test.mjs tests/workbench-contract.test.mjs`：51/51 通过。
- `node --test tests/preschool-workbench-refresh.test.mjs`：48/48 通过；`npm test`：105/105 通过。
- 本机 Chrome CDP：标题为“阳光成长工作台”；battle 页面渲染 30 格；点击选择豌豆射手和坚果墙后保存两株植物；一波同时显示普通 `3/3`、路障 `5/5`、铁桶 `7/7`；刷新后保留 `selectedPlantId`、植物和僵尸；离开 battle 前后 tick 保持 4；首页棋盘 DOM 为 0；320/390px 无横向溢出。
- 320px、390px 浏览器验收均无横向溢出，棋盘保持 30 格；页面离开清理和工作台首页不渲染棋盘也已验收。

## 3.2 首页草坪战场减法更新

### 已实现

- 首页结构收敛为顶部状态栏、伙伴成长卡、一个草坪战场主面板、六条可编辑任务战线、学习证据区和两个出口按钮。
- 每条战线直接使用 `toggle-plan` 完成打卡，保留阳光、成长、豌豆能量和 `recordAction()` 奖励幂等链路。
- 使用 GPT 草坪背景与透明植物/僵尸素材；首页不再渲染行动路径、重复任务面板或复杂战斗棋盘。
- 浏览器验收：明确加载 `index.html?cb=20260806-v050-landscape-fix-3#overview`；首页显示六条可编辑战线、学习证据区和两个出口；首页战斗棋盘节点为 0，运行日志为空。

### 证据

- `app.js`、`css/preschool/24-game-study-loop.css`、`tests/preschool-workbench-refresh.test.mjs`、`docs/99-归档/优化/2026-08-06-游戏化学习工作台参考分析与落地优化.md`。
- 定向测试 `48/48`；`npm test` 全量 `105/105`；`node --check app.js`、`node --check storage.js`、`node --check tests/helpers/css-graph.mjs`、`git diff --check` 通过。
- 本地页面：`http://127.0.0.1:4178/preschool-workbench/index.html?cb=20260806-task-wall-v1#overview`；入口 CSS 已升级为 `20260806-task-wall-v1`，避免任务墙规则复用旧缓存。

### 3.2.1 轻量练习证据闭环

- 内置英语和专注计划显示与主打卡同级的“去练习”；运动、用户新建计划没有虚假的练习入口。
- 浏览器实测英语练习答对并领取后：今日任务 `3/6 → 4/6`，阳光 `100 → 120`，豌豆能量 `4 → 5`，学习证据 `0 → 1`；来源计划标记为练习完成。
- 刷新后保持 `4/6`、`120` 阳光、`5` 豌豆能量和 `1` 张练习证据；已完成计划不再显示“去练习”。
- 本轮不把 60 日自然拼读 JSON 全量读取到首页；它继续作为资料库和后续活动来源。

### 3.2.2 参考案例任务墙优化（2026-08-06）

- 对照 `G:\UserCode\跨平台采集器\notes\游戏设计\游戏化学习工作台` 的暑期作业台、幼小衔接和 WorkBuddy 参考帧，首页任务区从单列长战线调整为响应式任务墙：宽屏三列、761–1180px 两列、竖屏一列。
- 每项任务仍保留植物/僵尸反馈、直接打卡、同级轻量练习和现有幂等奖励；没有增加课程全量加载、第二套积分或战斗结算。
- 入口样式缓存版本升级为 `20260806-task-wall-v1`。
- 新增合同测试后，幼儿专项 `preschool-workbench-refresh` 为 48/48，全量 `npm test` 为 105/105；本轮入口顺序仍需浏览器和远端发布验收，APK、MuMu 仍未完成。

### 明确未做

- 不实现完整原版塔防的关卡、路线、复杂 AI、卡牌经济、多波次调度、多人、联网排行榜或第三方 iframe。
- 不把战斗 HUD、实体动画和复杂敌人逻辑塞回工作台首页。
- 原有 `garden.invader` 保留为旧页面兼容；新独立战斗使用 `garden.defense`，两者不宣称为官方游戏复刻。

## 3.3 侧边栏方向适配更新

- 变更：幼儿版在横屏窄视口保持侧边栏常驻，隐藏重复的顶部菜单和底部快捷导航；竖屏窄视口保留手动菜单、关闭按钮和遮罩，页面切换只在竖屏自动收起。
- 证据：`app.js`、`css/preschool/20-sidebar-orientation.css`、`css/preschool-workbench.css`、`preschool-workbench/index.html`、`tests/helpers/css-graph.mjs`、`tests/preschool-workbench-refresh.test.mjs`；`npm test` 全量 105/105；`node --check app.js`、`git diff --check` 通过；390×844 竖屏侧栏默认移出且菜单可见，844×390 横屏侧栏 `transform=0` 且菜单隐藏。
- 边界：仅调整幼儿版导航壳层，不改变任务、阳光、奖励、课程资料或独立花园游戏数据；本轮未执行 APK、MuMu 或远端发布验收。
- 下一步：冻结本轮源码；正式签名包、Android 内部版本号同步、MuMu 和 GitHub 发布验收另开发布任务。

## 4. 当前工作树规则

- 正式项目总控以 `G:\StudyCode\个人工作台\docs\00-总控\` 为准；学科资料目录只作为内容方案和资料库，不创建第二套项目级总控。
- `.worktrees` 只服务分支开发，不作为长期资料库。
- 未提交的用户改动必须保留，不能用整理文档的名义清理。
- 工作树中的计划要合并为正式方案，不能长期形成第二套目录。

## 5. 当前风险排序

### P0

- 课程完成、奖励发放和活动实例仍需继续补充浏览器级证据；当前 Node 合同测试已覆盖旧快照迁移、轻量练习同步和奖励幂等。
- 游戏 MVP 仍需独立观察真实用户操作后的可玩性和存档规模；当前规则层和页面边界已验证。

### P1

- 课程资料已经补齐到可供研究和试用的程度，但网页运行时只按需接入；自然拼读以外的内容、授权和浏览器流程仍需逐科验收。
- 幼儿版大文件仍承担过多页面、状态和渲染职责。

### P2

- 自托管同步、正式签名 APK、Android 内部版本号同步和完整家长系统仍需后续单独验收。

## 3.4 发布门禁优化（2026-08-06）

- 新增 `scripts/release-verify.mjs` 和 `tests/release-contract.test.mjs`，验证根入口“幼儿三主题在前、成人/儿童在后”、主题参数、目标页面和五张卡片主图均存在。
- `package.json` 新增 `npm run release:verify`；`.github/workflows/android-apk.yml` 现在会在 Android 工程生成前运行 `npm test`、发布合同和关键 JavaScript 语法检查，APK 构建后检查文件非空。
- 本地证据：`npm test` 108/108；`npm run release:verify` 通过；`node --check launcher.js`、`node --check app.js`、`node --check storage.js`、`node --check config.js`、`git diff --check` 通过；`npm run android:prepare` 成功复制 13 个文件和 5 个目录。
- 制品证据：`dist/` 中五个 HTML 入口和三张幼儿主题主图存在；临时 HTTP 服务请求 9 个根/入口/素材路径全部 HTTP 200。
- 远端证据：`a8b371f` 的 main Actions run `31080823999` 和 `v0.5.1` 标签 run `31080849490` 均 success；Release 已附带 `personal-workbench-v0.5.1.apk`（60,140,096 bytes），Pages 根入口、5 个 HTML 入口和 3 张主题图已取得 HTTP 200。
- 当前边界：Release 资产 HEAD 可读但本机大文件下载在约 5.4 MB 后超时，未形成完整 APK；本机 Android 构建因无 SDK 路径失败；MuMu 设备已连接但未安装。因此不能把 Release 资产存在写成 APK/MuMu 验收完成。

## 3.5 根入口首页两层选择器（2026-08-06）

- 方案：`docs/plans/2026-08-06-workbench-launcher-home-ux.md`；根入口只承担工作台选择，不加载课程或战斗运行时。
- 代码：`index.html` 增加幼儿世界/通用工作台卡语义、跳过链接、主题视觉层级、44px 操作区和手机断行；`tests/release-contract.test.mjs` 增加首页结构合同。
- 浏览器证据：`http://127.0.0.1:4179/?choose=1&cb=20260806-launcher-ux`；375/768/1024/1440px 分组行数为上三下二，中屏为 2+1，竖屏单列；320px `htmlScrollWidth=305`、`clientWidth=305`，无横向溢出；点击花园世界进入正确主题参数。
- 发布状态（本节写就时）：源码版本 `v0.5.2` 已推送；main run `31082766447`、标签 run `31082784622` 均 success；Pages 根入口和入口素材已取得 HTTP 200；Release APK 已上传。APK 下载、安装和 MuMu 仍未验收，不把资产存在写成已安装。后续本机 PARTIAL 证据见 **3.6**。

## 3.6 2026-08-06 APK 代理下载与 MuMu PARTIAL 验收

- 权威口径：以交接包 `docs/handoff/001_个人工作台发布验收与CC续接_20260806/` 与本轮命令/设备输出为准；3.4/3.5 中“未下载/未安装”为历史状态，**更新而不删除**。
- 源码门禁：`npm test` 108/108；`npm run release:verify` 通过（根入口五卡：三幼儿主题在前、成人/儿童在后，5 素材）。
- 完整下载：经代理 `127.0.0.1:10808` 取得有效包 `C:/Users/No'mi'l/AppData/Local/Temp/personal-workbench-release-v0.5.2/personal-workbench-v0.5.2-proxy.apk`；大小 `60,140,796` bytes；SHA-256 `40cbf426afe619354a04307dee0532aaa7cb0cdb0935c7df2c9ae594c1f66fa8`。
- 禁用：残缺包 `personal-workbench-v0.5.2.apk`（`6,602,752` bytes）不得安装或验收。
- MuMu：`127.0.0.1:16384` 为 device；`adb install -r` → `Success`；前台 `com.nonomil.personalworkbench/.MainActivity`；安装冲突根因为旧包签名不同（非文件损坏），卸载后成功。
- 已证（交接首轮）：根入口 → 第一个幼儿花园（阳光花园工作台）；截图 `mumu-launch.png`、`mumu-garden.png`（temp 目录，不提交仓库）。
- 已证（2026-08-06 CC 续接 V10，MuMu `127.0.0.1:16384` + WebView CDP）：
  - 根入口 `?choose=1` 三幼儿卡可见；三主题首页：阳光花园 / 方块探险 / 彩虹闯关均进入成功（主题参数与标题匹配）。
  - 轻量练习闭环：古诗题答「明月」并 `lesson-finish` 后，阳光 40→60、豌豆能量 0→1、XP 40→60、UI 1/6→2/6，`preschool-poetry-1` 写入 `courseProgress`。
  - 返回 `#overview` 花园首页成功；`am force-stop` 后冷启动状态保持（阳光 60 / 能量 1 / XP 60 / 2/6）；方块主题读取同一成长快照。
  - 无 FATAL；本轮无业务缺陷，未改业务代码。续接后回归：`npm test` 108/108，`release:verify` 通过。
  - 证据文件（temp，不入库）：`pw-v10-launcher.png`、`pw-v10-theme-*.png`、`pw-v10-practice-finished.png`、`pw-v10-restart-final.png`、`pw-v10-practice-seq.json`、`pw-v10-after-restart-final.json`。
- 已证（同日成人/儿童入口冒烟）：根入口点进 `成人成长工作台/index.html`、`儿童学习工作台/index.html`，各自首页渲染正常（截图 `pw-v10-adult-click.png`、`pw-v10-child-click.png`）；错误英文路径会 404，以中文目录为准。未做成人/儿童深度任务与持久化闭环。
- 仍未证 / 非本批：成人/儿童深度任务闭环；正式签名 APK、商店包、云同步、Supabase、60 日课程全量运行时、完整家长系统。
- 包内版本：`versionName=1.0`、`versionCode=1` 为 Android 包内事实；不声称与 Git tag `v0.5.2` 已同步。APK 为 Debug 包。
- 结论：五入口 APK 可进入；幼儿最小核心路线已闭环。整体仍为 **PARTIAL**（非稳定 RC / 非正式商店包）。不得写成全产品全功能通过。

## 3.7 根入口首屏始终显示多工作台（2026-08-06）

- 用户事实：打开网页/App 时先应看到多个工作台入口，而不是被记忆选择直接带进单一工作台。
- 根因：`index.html` 在 `shouldAutoRedirect(search)` 为真时 `location.replace` 到上次变体；有记忆选择时冷启动会跳过多入口。
- 修复：`launcher.js` 的 `shouldAutoRedirect` 恒返回 `false`；记忆选择仍用于「上次使用」徽章/最近面板，不用于冷启动跳过。
- 证据：`tests/launcher.test.mjs` 合同「never auto-redirects…」；`npm test` 108/108；`npm run release:verify` 通过。
- 边界：未改五入口卡片、主题参数、localStorage key、课程/游戏；未重打 tag / 未改 Release；发布口径仍为 **PARTIAL**。已发布的 v0.5.2 APK 仍含旧自动跳转，需后续新包才带此修复。

## 3.8 五入口切换与主题游戏化呈现（2026-08-06）

- 切换器：设置页与顶栏「切换工作台」均露出 **5 个入口**（阳光花园 / 方块探险 / 彩虹闯关 / 成人 / 儿童），不再只有 3 个 variant。
- 数据合同未改：仍 3 套 localStorage；三幼儿主题共享 `petbank_huchuliang_preschool_workbench_state_v1`。
- 主题体验：`getPreschoolThemePlaybook()` 让 voxel 呈现「方块基地/晶体/迷雾怪物」，platform 呈现「横版闯关/金币/障碍」；首页任务墙与 battle 文案/美术随主题变化。
- 证据（文案层）：`npm test` 曾到 110/110；`npm run release:verify` 通过。

## 3.9 独立小游戏世界 games/（2026-08-06）

- 用户事实：马里奥与我的世界要**单独世界**，不与花园战场共用；小游戏可放独立文件夹。
- 目录：
  - `games/voxel-adventure/`：方块放置/拆除、晶体收集、独立 localStorage `personal_workbench_voxel_world_v1`
  - `games/platform-quest/`：横版跑跳、金币、终点旗
  - 两页均不加载 `preschool-garden.js` / `renderPreschoolBattle`
- 路由：
  - garden：`worldGameHref: ''`，仍进 `#battle` 花园保卫战
  - voxel / platform：`worldGameHref` 指向 `../games/.../index.html`；首页出口 `data-action="open-world-game"`；侧栏/底栏 `syncPreschoolGameNavLabels` 改标签并改动作
  - `setPage('battle')` 在非花园主题时先 `openPreschoolWorldGame()` 外跳
- 移动包：`scripts/prepare-mobile.mjs` 的 `directories` 增加 `'games'`
- 证据：`tests/world-games.test.mjs`；`npm test` **112/112**；`npm run release:verify` 通过
- 边界：不是完整 Minecraft/Mario 引擎；工作台任务数据仍在幼儿快照；发布仍 **PARTIAL**；未提交、未重打 tag、未改 Release

## 3.10 运行时封装到 prj/（2026-08-06）

- 仓库根只保留：`prj/`、`docs/`、`tests/`、`scripts/`、`.github/`、工具链配置（`package.json` 等）、薄跳转 `index.html` → `prj/`。
- 产品代码与素材：`prj/` 下含入口、三工作台、`css/`、`assets/`、`games/`、`data/`、运行时 JS。
- 打包：`prepare-mobile` 从 `prj/` 复制到 `dist/`（Capacitor `webDir` 仍为 `dist`，APK 内路径不变）。
- 证据：`tests/project-layout.test.mjs`；`npm test` **115/115**；`release:verify` 通过。
- 边界：GitHub Pages 根路径仍兼容（跳转）；已发布 v0.5.2 APK 不含本批；未 commit。

## 3.11 幼儿今日真实工作流卡（2026-08-13）

- 对照《什么是真正的工作台》和 `docs/01-方案/优化方案2`：幼儿今日页主 CTA 改为真实下一步，不再把一键打卡当主路径。
- 代码：`getPreschoolHomeWorkflow` / `renderPreschoolHomeWorkflowCard`；未完成且有练习映射 → `open-plan-practice`；全部完成后 → 当前主题游戏；无练习候选 → 诚实空态或去任务页。伙伴卡主按钮走同一派生。`toggle-plan` 仍留在任务墙。文案去掉“首页只负责打卡”。
- 证据：`npm test` **120/120**；`npm run release:verify` 通过；`node --check prj/app.js` 通过。缓存戳 `app.js?v=20260813-real-workflow-card-v1`。
- 边界：未做浏览器点卡验收；未改存储 key/schema；未拆首页世界进度/周节奏；未 commit / 未发版。

## 3.12 首页信息减法（2026-08-13）

- 世界进度墙和 7 天节奏从幼儿今日页移到花园基地（成长页），首页只留工作流卡、伙伴、任务墙、证据和游戏/奖励出口。
- 证据：`npm test` **121/121**；浏览器工作流卡闭环见 `scripts/verify-preschool-workflow-card.mjs`。
- 边界：任务墙兼容勾选仍在；未 commit / 未发版。

## 3.13 学习模块扩展与小游戏重构 + 测试治理（2026-08-14，v0.6.0）

- 新增 9 个学习模块：识字（240 字闪卡/组词/找字）、拼音（63 项声韵母/整体认读）、古诗（听句选下句题库）、自然拼读、英语词汇（340 词口语）、数学题库（数一数/比大小/口算）、成就徽章、宠物养成、成长世界（地图/小镇/花园地标）。全部经 `preschool-workbench/index.html` 在 app.js 前加载，数据题库入 `prj/data/preschool/`。
- 三游戏重构：花园保卫（12 关多波次、失败态、连续 x 坐标种植、难度参数放缓）、方块世界（40×18 岛屿世界、挖放/合成、晶体收集）、横版闯关（独立 `data/physics.js`、缩短关卡）。游戏资产本地化到各自 `games/*/assets/`。
- 修复审查发现的致命 bug：花园保卫多波次卡死（清场后 `status='won'` 被 `spawnWave` 守卫误拦，第 4-12 关第二波永不刷新；守卫改为只拦 `'lost'`，底层 `spawnDefenseWave` 会重置为 `'playing'`）。
- 测试治理：test 脚本加 `--test-concurrency=1`，根除并发跑测试时跨文件 `globalThis` 污染导致的间歇失败；同步过时断言（古诗专区文案、缓存戳、test 脚本格式）。
- 证据：`npm test` **202/202**；`npm run release:verify` 通过；5 个数据题库 JSON 校验通过。
- 边界：发布口径仍 **PARTIAL**（APK 未重打）；徽章总数硬编码 11。platform-quest 水平击退死代码与全宽地面保护已在 2026-08-15 横版 S1 销账，见 §3.19。

## 3.14 文档回真 B1（2026-08-15）

- 任务包 `T20260815-B1-docs-truth/` 已执行、待用户验收：5 处文档偏离改回与代码一致——卡片墙方案标"已实施"（缓存戳 `20260815-course-wall-v1`）、refresh 测试断言迁到卡片墙结构（53/53）、"每天三项核心"改为 6 项今日任务（`prj/storage.js` `PRESCHOOL_DAILY_ITEMS`）、数据与奖励合同按代码实读改齐（课完成 +20、游戏日 cap 80、11 个游戏里程碑精确值等，对数表见包内 test-report）、识字目标改为实数 1500 字（L1–L5 各 300；盘点口径 240 已过时）。
- B1 当前复核证据：`npm test` **269/269**、`node --test tests/preschool-workbench-refresh.test.mjs` **54/54**、数学合同定向测试 **8/8**、`git diff --check` 均退出码 0；并行会话的课程页/数学卡片/点亮文案改动已作为当前工作树事实保留，B1 状态为 `review` 待用户验收、未执行 commit；对数以 `prj/` 代码实读为准。
- 边界：在途进展已按用户裁决先提交为基线 `ebc5592`；本包未改产品代码；"家长确认兑换"“错题回流”等未实现机制只在合同标注"裁决：未实现"，实现属 B2/B3。

## 3.15 三游戏优化一期·花园 S1（2026-08-15）

- 当前事实：花园保卫战 G1–G3 已在当前 HEAD 中具备学习难度联动、三行结算、星芒 HUD 与里程碑庆祝卡；S2 的 roster、土豆地雷和星级重定义未开始。
- 自动证据：`node --test tests/world-games.test.mjs` 仍为 9/9、退出码 0；`npm test` 曾复核为 260/260、退出码 0，但最终复跑受并发 B1 的 `prj/app.js` / 工作台测试改动影响，为 264 项中 263 通过、退出码 1。当前数量修正了旧的 238/259 记录，但全量回归需待 B1 收口后重跑。
- 浏览器证据：`http://127.0.0.1:4180/prj/games/garden-defense/index.html` easy 档显示「难度 · 简单」和星芒 HUD；第 1/2/3 关结算均显示所得、冒险等级、下一目标；第 3 关弹出「三关守卫」庆祝卡；花园页日志无 error/warning。
- 边界：本轮未重新改写 S1 代码文件；`workbench-bridge.js`、`preschool-garden.js`、`data/stages.js` 和 `prj/app.js` 不纳入本包 S1；普通/困难档、失败路径、390px 视口及第 12 关困难档时长留人工确认。
- 状态：S1 实现与定向证据齐全，但全量回归被外部并发改动阻塞，正式验收仍待确认；未越过边界进入 S2 或其他游戏包。

## 3.16 三游戏优化一期·方块 S1（2026-08-15）

- 当前事实：方块世界 V1–V3 已落地——`getWeeklyReport` 方块行 total 动态读 `VoxelQuests.list.length`（无全局兜底 12）；任务完成有三行结算与星芒 HUD；rank 升段有仪式卡且同段位不重复（`lastCelebratedRank`）。S2 的工具门禁、蓝图、家园快照、q13–q18 未开始。
- 自动证据：合同先红后绿；`node --test tests/world-games.test.mjs` **13/13、退出码 0**。`npm test` 基线曾 269/269；收口复跑为 275 项中 273 通过、退出码 1，失败 2 项在 `preschool-workbench-refresh` CSS 戳，不属于本包允许文件。
- 浏览器证据：`http://127.0.0.1:4192/prj/games/voxel-adventure/index.html` 具备 companion/settle/celebrate 节点，周报 total=12；花园页与横版页无 `VoxelQuests` 时同样 total=12 且页面可打开。
- 边界：只改了 `workbench-bridge.js` 的 getWeeklyReport labels 一处、`voxel-adventure` 的 game.js/css/html、以及 `tests/world-games.test.mjs`。未改 `quests.js`、`workshop.js`、`prj/app.js`。`app.js` 世界卡仍硬编码 voxel total=8，与 bridge 周报 12 不一致，属后续工作台/S2。
- 状态：S1 实现与定向证据齐全，全量回归被外部并发改动阻塞；未进入 S2。

## 3.17 积分打卡 S1 点亮口径（2026-08-15）

- 任务包 `T20260815-points-lighting` S1 已执行、待用户验收：孩子可见「打卡」改「点亮/完美日」；向日葵旁路方向 A（不进 `totalSunlightEarned`）已锁定测试；`claimReward` 独立合同测试 4/4；`docs/data-model.md` 增「点亮日 vs 完美日」。本包定向 `96/96`。未启动 S2。未 commit。

## 3.18 学习专区中间层改版（2026-08-15）

- 任务包 `T20260815-course-middle-layer/` 已执行、待用户验收：墙卡加今日预览行；`flashcard-classic` 落点改为「更多练习」菜单；原大卡整卡进「家长看详情」折叠区；按钮文案改为「更多练习 / 更多资料」。
- 证据：`npm test` **275/275**；`node --check prj/app.js` 通过；390/320/桌面走查无横向溢出；打开卡片墙后再进古诗，今日一首仍为《悯农》。缓存戳 `20260815-course-middle-v1`。
- 边界：未 commit；最终验收留给用户；未改 `dailyPlans`/`courseProgress`、未新 localStorage key。

## 3.19 三游戏优化一期·横版 S1（2026-08-15）

- 当前事实：横版闯关 P0–P3 已落地——击退死代码确认已删、全宽地面 `penX>=penY` + `resolveGroundContact`；手感纯函数按现状锁定（coyote/buffer 120ms、GRAVITY 1500、JUMP_VY -620），简单档 coyote 140ms 只走 `canJump` 选项；`playMods` 接敌速/阳光/困难巡逻×1.2；通关三行结算 + 星芒 HUD（5 秒节流）。S2 的时间挑战外显、检查点反馈、关卡减肥未开始。
- 自动证据：合同先红后绿（8/29 红、退出码 1 → 29/29、退出码 0）；`npm test` **286/286、退出码 0**。
- 浏览器证据：`http://127.0.0.1:4192/prj/games/platform-quest/index.html?cb=pq-s1` 选关页徽标「难度 · 简单」；第 1 关「青青草地」开场白「先跑稳再跳，旗就在前面。」；跳跃键 68×68px；无新账本 key。
- 边界：只改 `platform-quest` 的 `game.js/css/html` 与 `tests/platform-physics.test.mjs`、`tests/world-games.test.mjs` 追加。未改 `workbench-bridge.js`、`data/physics.js` 数值、`data/levels.js`、`prj/app.js`。1/3/6/10 关手玩、通关三行与失败打气待人工确认。
- 状态：S1 实现与定向证据齐全，待手玩验收；未进入 S2。

## 3.20 三游戏优化二期·S2 玩法加深（2026-08-15）

- 当前事实：S2 玩法代码已落地。花园 G4 roster（7 关首现铁桶、9 关橄榄）+ G5 土豆地雷（造价 20、3 tick 武装、同格秒杀，正式贴图已接线）+ G6 星级。方块 V4–V7。横版 P4「可挑战」+ 最佳时间 + P5 检查点。
- 自动证据：`node --test tests/world-games.test.mjs tests/preschool-defense-game.test.mjs tests/voxel-world.test.mjs tests/platform-physics.test.mjs` **69/69、退出码 0**。
- 未做：横版 P6 关卡减肥（理论最短跑低于门槛，先不砍关）。S3 扩量未开。
- V6：成长页「我的家园」已落地，快照从 bridge 读取；方块进度口径改为 12 任务。
- 状态：S2 含家园卡；合同定向绿；未 commit。

## 3.21 对照《什么是真正的工作台》全项目再评估（2026-08-15）

- 性质：分析记录，不是已完成的改造。源头文章 `docs/01-方案/什么是真正的工作台/什么是真正的工作台.md`（三观点：不反人性/不舍本逐末/不局限作用）。
- 结论（以代码实读为证）：
  - **幼儿版基本符合观点一**：`recordAction`（`prj/child-growth.js`）由真实行动自动写 `checkinDates`，无独立打卡按钮；补签只补连续性不发奖励；§3.11 的工作流卡改造已把主 CTA 换成真实下一步。
  - **成人版是文章批评的形态**：habits `checkedDates` 手动点选、lifeEntries/milestones 全表单手填（`prj/app.js` add-life-entry/add-habit），无任何自动数据来源，仓库自身的真实工作痕迹（commit/docs/测试）不进工作台。
  - **观点二内容层是实的**：识字 1500 / 英语 597 / 拼音 63 / 古诗约 34 / 数学题库均有真实练习入口（`prj/data/preschool/banks-index.json` 等），错题 1/3/7 回流、点亮日与完美日口径严格区分。风险：激励账本（阳光/能量/XP/徽章/星级/金币/里程碑）复杂度已超学习内容，近期开发重心偏"仪表与烟花"。
  - **观点三是最大缺口**：运行时无 AI 交互、无素材采集（`api-adapter.js` 仅可选账号同步）；数据孤岛已出现——blocklegend 词卡 `learnedIds` 只在 `growth.worldGames`，不回流 `courseProgress.minecraft.mastery` 与复习队列（字段实名以 `prj/child-courses.js` `normalize` 为准；`docs/data-model.md` 中 `wordStates/charStates` 口径已过时，订正列入整改包）；无 GitHub 式年热力图（成人 `yearActivityDates` 只算活跃天数）。仓库真正符合"综合数据库"定义的是 docs/ 本身，但与产品运行时割裂。
- 纠正（同日）：对照文章的整改对象是三套工作台，不是方块传奇。主包 `docs/plans/T20260815-工作台对照整改/` 的 W1–W3 已落地待手点：成人首页去习惯勾、儿童今日学习/阅读进课程、设置/复盘导出周 Markdown。合同 16/16。未改 `prj/games/`。未 commit。

## 6. 下一次只做这件事

1. 方块世界点「拍照」，回成长页看「我的家园」马赛克。
2. 手玩横版 1/2/9/10 关记时，只有超时才砍 P6。
3. 未要求则不要 commit。
