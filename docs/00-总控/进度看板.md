# 进度看板

> 这是当前唯一的进行中清单。日期型计划中的任务只有同步到这里，才算当前任务。

状态值：`未开始`、`进行中`、`阻塞`、`待验收`、`完成`、`冻结`。

| 编号 | 任务 | 状态 | 完成证据 | 下一步 |
| --- | --- | --- | --- | --- |
| CTRL-001 | 建立项目级总控目录和入口 | 完成 | `docs/00-总控/`、`docs/README.md`、`docs/文件夹形式.md` | 后续按变更规则维护 |
| CTRL-002 | 统一主项目和工作树的文档来源 | 进行中 | `决策记录.md` 已确定主项目 docs 为正式来源 | 审计 worktree 计划并回写最终结论 |
| BASE-001 | 修复当前失败测试 | 完成 | `npm test` 全量 95/95 通过；幼儿专项 40/40；配色页、首页草坪战场和方向导航契约均通过 | 保持全绿基线，新增功能按影响范围回归 |
| MVP-001 | 冻结幼儿工作台轻量打卡字段 | 完成 | `storage.js` schema v5 的幼儿日计划迁移到 3 项 `required=true` 核心 + 3 项 `required=false` 可选；专项测试覆盖旧快照完成状态 | 保持资料库与工作台运行时解耦 |
| MVP-002 | 统一今日打卡、简单测试、奖励事件 | 完成 | 首页英语轻量选择题复用现有 `courseProgress`、`awardSunlight()` 和 `lesson:<id>` 幂等事件；浏览器验证答题后阳光 40→60 | 后续只按真实需求选择性接入单个资料活动 |
| MVP-003 | 完成刷新、导入导出、重复操作回归 | 完成 | 专项测试与浏览器刷新验证通过；`npm test` 全量 95/95；重复奖励由 `recordAction()` 按事件 ID 去重 | 新增数据字段必须继续补迁移和幂等测试 |
| MVP-004 | 收敛幼儿版首页为草坪战场打卡 | 完成 | 首页六条可编辑战线直接使用 `toggle-plan`；伙伴成长、学习证据和花园/奖励出口已接入；专项 40/40、全量 95/95；首页无战斗棋盘 | 保持首页轻量，复杂战斗只从独立入口进入 |
| UX-001 | 修复幼儿版横竖屏侧边栏行为 | 完成 | 横屏侧栏浏览器检查为 `flex`；竖屏手动折叠与方向合同通过；专项与全量测试通过 | 发布前保持方向规则不回退 |
| GAME-001 | 独立植物大战小游戏 MVP | 完成 | `preschool-garden.js`、`app.js`、`css/preschool/17-defense-game.css`；五路六列棋盘、植物差异、僵尸入侵、奖励日志和离页边界由专项/全量测试覆盖；浏览器进入 `#battle` 可见 30 格 | 只观察真实操作反馈，暂不扩展完整塔防 |
| MVP-005 | 游戏化学习工作台闭环优化 | 完成 | `docs/优化/2026-08-06-游戏化学习工作台参考分析与落地优化.md`；伙伴→今日行动→学习证据→阳光/成长→独立游戏出口；首页不全量读取 60 日课程 | 冻结首页结构，后续只按真实需求接入单个轻量活动 |
| PUB-001 | 网页全绿后构建 APK | 完成 | `main` Actions `31060158558`、标签 Actions `31060165057` 和 Pages `31060158051` 均成功；Release 已附 `personal-workbench-v0.4.4.apk` | 正式签名 APK 另行处理 |
| PUB-002 | MuMu 安装并验证核心流程 | 完成 | MuMu 12 安装成功；`MainActivity` 启动、三模式入口进入幼儿版、花园保卫战、豌豆射手放置、强制停止后状态保留均已实测 | 后续若要扩展，单独补更多设备场景 |
| FUT-001 | 扩展 21-60 日自然拼读资料 | 完成 | `data/preschool/english/phonics/lessons.json` 60 条；自然拼读专项测试通过 | 作为资料库保留，按需接入简单测试，不建设完整课程引擎 |
| FUT-002 | 自托管同步和家长奖励确认 | 冻结 | API/离线/冲突测试 | 阶段 2 内容稳定后再开启 |

## 更新格式

每次更新必须写明：

```text
日期：YYYY-MM-DD
变更：完成了什么
证据：文件、测试命令、截图或发布地址
问题：仍有什么失败
下一步：只写一个最小动作
```

没有证据的任务只能写“进行中”，不能写“完成”。

## 2026-08-03 本轮更新

- 变更：完成 3 项核心 + 3 项可选的数据合同、旧快照迁移兼容、首页英语轻量测试入口和可选活动折叠；补齐独立配色实验页的游戏场景契约。
- 历史证据（2026-08-03 资料与轻量工作台阶段）：`tests/preschool-daily-plan.test.mjs`、`tests/phonics-course-data.test.mjs`、`tests/palette-comparison.test.mjs` 共 6/6；当时 `npm test` 为 72/72。当前全量基线以 BASE-001 的 95/95 为准；本地验收入口已迁移到 `http://127.0.0.1:4178/preschool-workbench/index.html`。
- 边界：60 日自然拼读资料保留在 `data/preschool/english/phonics/` 资料库；首页和今日页不读取 60 日 JSON。移除的是复杂课程引擎和复杂游戏对工作台主流程的强耦合，不是删除资料或独立游戏。
- 问题：网页测试基线已全绿；APK 构建、MuMu 安装和 GitHub 发布仍未在本轮完成。
- 下一步：记录一次工作台到独立游戏再返回的浏览器证据，之后再进入 APK 发布验收。

## 2026-08-03 花园保卫战 MVP 更新

- 变更：完成“阳光成长工作台 / 花园保卫战”命名和独立小游戏 MVP；新增版本化 `garden.defense` 状态、5 路 × 6 列棋盘、植物选择/放置/投射物、三类僵尸和 battle 离页清理。
- 历史证据（2026-08-03 花园保卫战阶段）：`tests/preschool-defense-game.test.mjs` 等专项测试 51/51；当时 `npm test` 为 73/73。当前全量基线以 BASE-001 的 95/95 为准；浏览器真实页面验证标题、30 格、植物选择改变种植、豌豆射手与坚果墙差异、普通/路障/铁桶生命值、刷新保留、离页 tick 停止；当前仍以专项合同覆盖参考宽度。
- 边界：战斗只复用现有 `growth.sunlight`、`garden.defenseEnergy` 和奖励/路由出口，不新增 localStorage key；首页不渲染战场 HUD。未实现完整塔防、多关卡、复杂 AI、多人和联网排行榜。
- 问题：APK 构建、MuMu 安装和 GitHub 发布仍未在本轮完成；需要后续观察真实操作反馈。
- 下一步：只做一次独立游戏真实操作回访，不扩展为完整塔防。

## 2026-08-03 首页草坪战场减法更新

- 变更：移除首页行动路径、重复任务面板和未使用的首页渲染函数；新增单一草坪战场面板，三条核心任务战线直接打卡，保留花园游戏与奖励中心两个出口。
- 证据：`app.js`、`css/preschool/24-game-study-loop.css`、`tests/preschool-workbench-refresh.test.mjs`；定向测试 40/40；`npm test` 全量 95/95；`node --check app.js`、`git diff --check` 通过；浏览器桌面验收通过。
- 边界：60 日自然拼读仍是资料库；首页不读取 60 日 JSON，也不承载独立战斗棋盘。APK 构建、MuMu 安装和 Pages 制品验收仍冻结。
- 下一步：推送当前已验证的源码提交，之后再单独进行 APK/Pages 发布验收。

## 2026-08-03 侧边栏方向适配更新

- 变更：横屏窄视口不再自动移出侧边栏；竖屏仍默认收起并支持顶部菜单、关闭按钮和遮罩手动控制；入口 CSS 查询版本更新为 `20260803-sidebar-orientation-ratio-important`。
- 证据：`tests/preschool-workbench-refresh.test.mjs` 新增方向契约；`npm test` 95/95；`node --check app.js`、`git diff --check`；横屏侧栏浏览器检查为 `flex`，竖屏规则由方向专项合同覆盖。
- 问题：APK 构建、MuMu 安装和 GitHub 发布仍未在本轮完成。
- 下一步：推送本轮已验证的源码提交。

## 2026-08-06 游戏化学习工作台闭环优化

- 变更：参考 `G:\UserCode\跨平台采集器\notes\游戏设计\游戏化学习工作台` 的本地案例，将幼儿首页收敛为“专属伙伴 → 今日行动 → 完成证据 → 阳光/成长/豌豆能量 → 花园游戏/奖励出口”；首页不再承担完整课程或战斗棋盘。
- 证据：`app.js`、`css/preschool/24-game-study-loop.css`、`preschool-workbench/index.html`、专项测试 `40/40`、`npm test` `95/95`、四个 `node --check` 入口和 `git diff --check` 均通过。
- 浏览器：首页显示 `2/6` 今日战线、阳光 `90`、豌豆能量 `3` 和学习证据区；学习专区展开 8 个入口，自然拼读进入 `#courses?course=preschool-english` 的 3 张轻量练习卡；花园进入 `#battle` 显示五路六列棋盘、植物卡、僵尸和奖励日志；浏览器错误日志为空。
- 边界：60 日自然拼读和各科资料继续保留为资料库；只移除首页对复杂课程/复杂游戏的强耦合，不删除资料，也不把独立游戏宣称为完整原版复刻。APK、MuMu 和远端 Pages/Actions 结果仍待发布验收。
- 工作树：正式记录已回写主项目 `docs\00-总控` 和 `docs\优化`；`幼儿-游戏场景版` 仍有未合并计划/研究文件，`.worktrees` 下另有未登记的资料片段目录，未擅自删除或合并。
- 发布状态：`main`、`v0.4.4` 已推送；GitHub Pages、两个 Android Actions 和 GitHub Release 均成功，标签 APK 已下载并完成 MuMu 核心 smoke。当前 APK 仍是 Debug 包，Android 内部 `versionName` 仍为 Capacitor 默认 `1.0`，没有冒充正式商店版本。
- 下一步：冻结本轮首页/课程/游戏改动；若后续需要同步 Android 内部版本号或正式签名，另开独立发布修复，不回改本次已发布标签。
