# 进度看板

> 这是当前唯一的进行中清单。日期型计划中的任务只有同步到这里，才算当前任务。

状态值：`未开始`、`进行中`、`阻塞`、`待验收`、`完成`、`冻结`。

> 2026-08-19 源码快照 **v0.7.4**。说明见 `docs/04-发布/v0.7.4.md`。APK/MuMu 未按本版重验，仍 PARTIAL。

| 编号 | 任务 | 状态 | 完成证据 | 下一步 |
| --- | --- | --- | --- | --- |
| CTRL-001 | 建立项目级总控目录和入口 | 完成 | `docs/00-总控/`、`docs/README.md`、`docs/文件夹形式.md` | 后续按变更规则维护 |
| CTRL-002 | 统一主项目和工作树的文档来源 | 进行中 | `决策记录.md` 已确定主项目 docs 为正式来源 | 审计 worktree 计划并回写最终结论 |
| BASE-001 | 修复当前失败测试 | 完成 | `npm test` 全量 **120/120**（含幼儿工作流卡契约）；入口五卡、独立小游戏、prj 布局合同通过 | 保持全绿基线，新增功能按影响范围回归 |
| CTRL-003 | 运行时封装到 prj/ | 完成 | 产品进 `prj/`；根留 docs/tests/scripts/配置；薄 `index.html` 跳转；prepare/release/workflow 已改路径；115/115 + release:verify | 可选 commit；Pages/设备冒烟 |
| MVP-001 | 冻结幼儿工作台轻量打卡字段 | 完成 | `storage.js` schema v5 的幼儿日计划迁移到 3 项 `required=true` 核心 + 3 项 `required=false` 可选；专项测试覆盖旧快照完成状态 | 保持资料库与工作台运行时解耦 |
| MVP-002 | 统一今日打卡、简单测试、奖励事件 | 完成 | 首页英语/专注轻量选择题复用现有 `courseProgress`、`awardSunlight()` 和 `lesson:<id>` 幂等事件；浏览器实测英语练习后阳光 100→120、豌豆能量 4→5，来源计划由 3/6→4/6 | 后续只按真实需求选择性接入单个资料活动 |
| MVP-003 | 完成刷新、导入导出、重复操作回归 | 完成 | 旧快照迁移、练习与计划同步、重复奖励和刷新保留由专项/全量测试覆盖；`npm test` 105/105；浏览器刷新后仍为 4/6、120 阳光、5 豌豆能量、1 张练习证据 | 新增数据字段必须继续补迁移和幂等测试 |
| MVP-004 | 收敛幼儿版首页为草坪战场打卡 | 完成 | 首页六条可编辑战线直接使用 `toggle-plan`；有映射的任务显示同级“去练习”，伙伴成长、学习证据和花园/奖励出口已接入；专项 48/48、全量 105/105；首页无战斗棋盘 | 保持首页轻量，复杂战斗只从独立入口进入 |
| UX-001 | 修复幼儿版横竖屏侧边栏行为 | 完成 | 横屏侧栏浏览器检查为 `flex`；竖屏手动折叠与方向合同通过；专项与全量测试通过 | 发布前保持方向规则不回退 |
| GAME-001 | 独立植物大战小游戏 MVP | 完成 | `preschool-garden.js`、`app.js`、`css/preschool/17-defense-game.css`；五路六列棋盘、植物差异、僵尸入侵、奖励日志和离页边界由专项/全量测试覆盖；浏览器进入 `#battle` 可见 30 格 | 只观察真实操作反馈，暂不扩展完整塔防 |
| MVP-005 | 游戏化学习工作台闭环优化 | 完成 | `docs/99-归档/优化/2026-08-06-游戏化学习工作台参考分析与落地优化.md`；伙伴→今日行动→轻量练习/打卡→学习证据→阳光/成长→独立游戏出口；首页不全量读取 60 日课程；任务墙为宽屏三列、窄桌面两列、竖屏一列 | 冻结首页结构，后续只按真实需求接入单个轻量活动 |
| UX-002 | 根入口两层五入口选择器 | 完成 | `docs/plans/2026-08-06-workbench-launcher-home-ux.md`、`index.html`、`tests/release-contract.test.mjs`；本地 `npm test` 108/108、`release:verify` 通过；浏览器已验证 320/375/768/1024/1440px、花园主题跳转和线上五入口顺序 | 保持根入口结构冻结 |
| UX-003 | 打开网页首先显示多个工作台 | 完成 | `launcher.js` `shouldAutoRedirect` 恒 false；`tests/launcher.test.mjs`；`npm test` 108/108、`release:verify` 通过；记忆选择仅驱动最近徽章不跳过入口 | 可选纳入下次包与设备冒烟；不重打 v0.5.2 tag |
| UX-004 | 应用内切换器露出五个入口 | 完成 | `app.js` `getWorkbenchSwitchEntries` + `renderWorkbenchSwitcher`；`config.js` 顶栏五入口；三幼儿主题链 `?theme=`；`npm test` 110/110 | 保持三主题共享幼儿数据仓 | |
| GAME-002 | voxel/platform 独立小游戏世界 | 完成 | `games/voxel-adventure` + `games/platform-quest`；playbook `worldGameHref`/`navGameLabel`；`openPreschoolWorldGame`；花园仍 `#battle`；`prepare-mobile` 含 `games`；`tests/world-games.test.mjs`；`npm test` 112/112 | 可选源码提交 + 设备冒烟；真完整引擎另开 |
| PUB-001 | 网页/APK 发布验收 | 待验收 | 源码快照 **v0.7.4**（`docs/04-发布/v0.7.4.md`）。平板工作台可发；方块传奇不进 APK。状态 **PARTIAL** | 推送后以 Actions/Pages 实际结果为准；本版 APK 另打，不重打旧 tag |
| PUB-002 | MuMu 安装并验证核心流程 | 完成 | 幼儿 V10 全核心 + 成人/儿童入口冒烟；五入口均可进入；无 FATAL；temp 证据 `pw-v10-*` | 冻结本切片；成人/儿童深度验收或签名包另开任务 |
| FUT-001 | 扩展 21-60 日自然拼读资料 | 完成 | `data/preschool/english/phonics/lessons.json` 60 条；自然拼读专项测试通过 | 作为资料库保留，按需接入简单测试，不建设完整课程引擎 |
| FUT-002 | 自托管同步和家长奖励确认 | 冻结 | API/离线/冲突测试 | 阶段 2 内容稳定后再开启 |
| M2-UX-001a | 幼儿今日页真实工作流卡 | 完成 | 首页顶部一张由真实下一步派生的大卡：未完成练习走 `open-plan-practice`，全完成去主题游戏，无候选诚实空态；`toggle-plan` 仍留在任务墙；`npm test` 120/120，`release:verify` 通过 | 浏览器点卡练习闭环仍待验收；首页世界进度/周节奏减法另开 |
| LEARN-B2 | 六项去练习 + 错题 1/3/7 回流 | 进行中 | `storage.buildMistakeReviewQueue` / 首页「去练习」「今天复习」；daily-plan + mistakes 假时钟绿 | 浏览器六项走查与复习入口截图 |
| LEARN-B3 | 奖励闭环 S1–S4 | 进行中 | S1 家长确认；S2 必做门槛；S3 喂星芒/连击/再练不加分；S4 今日 3 词+80 词溯源；相关合同绿 | 浏览器走查；未标 accepted |
| LEARN-EW | 认单词优化五件套 | 进行中 | S1–S5 代码绿；定向 30/30；80 词配图 80/80；wordboss 可玩 | 手玩 match/spell、错词本整轮、三关通关；未标 accepted |
| GAME-003 | 方块传奇 3D 单词战斗 | 待验收 | `prj/games/blocklegend/` S1–S6；定向 42 项绿；三界 ≥3 仍绿 | 手玩 1→2；MuMu 未验 |
| GAME-004 | 方块传奇薄商店+放置 | 待验收 | 方案 `05-方块传奇/`；sim 包 S1–S3 代码绿；定向 50/50 | 手玩：F 买装备、挖土后按 5 放下 |
| GAME-005 | 方块传奇画面抛光 | 待验收 | 世界 192 + 洞穴/矿/水/村；近战可打中；单词方块收金币/HP；定向 **60/60** | 手玩：对着怪按住左键、挖金色单词方块 |
| ALIGN-001 | 《什么是真正的工作台》工作台整改 | 待验收 | W1–W3 已落地：成人首页去习惯勾、儿童今日主按钮去学习、设置/复盘导出周 Markdown。合同 16/16 先红后绿 | 手点成人首页、儿童今日、幼儿设置导出 |
| GROW-001 | 幼儿宠物蛋合入 PVZ/MC 素材 | 待验收 | 花园 4 PVZ + 闯关星芒；我的世界工作台 7 只 MC。成长页「方块基地」去浇水/保卫战/星芒造型；花园蛋不串到方块巢。`tests/preschool-pet.test.mjs` **15/15** | 手点 `?theme=voxel-adventure#growth` |
| LEARN-E1 | 方块传奇跟读评测（speech-match） | 待验收 | 共享 `speech-match.js`；Boss/练一句/跟读走 lemma+LCS。`tests/speech-match.test.mjs` **10/10** | 按 `05-完整测试方案.md` §4 手玩；不标 accepted |
| LEARN-W1 | 英语 mastery 方案 A 双轨 + 今日卡 | 待验收 | 新词 v2 6h→28d，存量 v1；`selectTodayTasks` +「今天练这个」。调度 **12/12** | 按 `05-完整测试方案.md` §3 手玩 |
| GAME-E2 | 方块传奇复习之门 + 今日冒险 | 待验收 | 七轮课表、门、缩短副本、今日冒险、图鉴/难词/听说比。定向 **17/17**，回归 **188/188** | 按 `05-完整测试方案.md` §5 手玩；独立仓未推 |
| GAME-E4 | 方块传奇句子跟读关 | 待验收 | `scene-loop.js` 三遍循环 + 练一句装配。定向 **8/8** | 按 `05-完整测试方案.md` §10 手玩 |
| GAME-D1 | 方块传奇词力 + 三档重玩 + 词灵回廊 | 待验收 | Leitner 5 盒、三档、卷轴回廊、营地、难词支架、结算密度。隔离新档 A1 已核。 | 独立仓未推。不标 accepted |

## 2026-08-19 Echo Loop 借鉴方案分析

- 日期：2026-08-19
- 变更：完成 Echo Loop（AGPL）借鉴分析，产出方案包 `docs/plans/T20260819-echoloop-borrow/`（方块传奇 E1–E4 分期 + 工作台英语升级建议 + 源码级参数速查）；`prj/games/Echo-Loop-main/` 已 gitignore 留本地参考。未改产品代码。
- 证据：包内 README/01–04 五份文档；`git check-ignore` 验证两路径已忽略；`docs/plans/README.md` 已登记。
- 问题：blocklegend 本地副本无 `.git` 靠手动同步独立仓，改动窗口期无保护（见包内 02 §5）。
- 下一步：按 `docs/plans/T20260819-echoloop-borrow/05-完整测试方案.md` 手玩 W1/E1/E2/E4。独立仓未推。不标 accepted。
- 补记（2026-08-19 午）：E4 练一句跟读循环已绿（8/8）。顺手修正：周报 `total` 12、gitignore 注释、气候表与 levels.js 对齐、Phase 9–12 文档回真。
- 补记（2026-08-19 上午）：用户拍板 mastery 间隔走**方案 A**；执行包已建三个。
- 补记（2026-08-19 午）：E1 S1–S3 已落地。`node --test tests/speech-match.test.mjs` 10/10；`tests/blocklegend.test.mjs` 187/187。未标 accepted。
- 补记（2026-08-19 午）：W1 S1 事件日志已绿。`tests/english-mastery-schedule.test.mjs` 5/5。S2/S3 未启动。
- 补记（2026-08-19 午）：W1 S2 方案 A 双轨课表已绿。新词 6h→28d，存量仍 1/3/7/14；48h 宽限不降级。调度测试 9/9，英语回归合计 30/30。S3 未启动。
- 补记（2026-08-19 午）：W1 S3「今天练这个」已绿。`selectTodayTasks` 12/12，英语回归 33/33。未手玩。不标 accepted。
- 补记（2026-08-19 午）：E2 S1 关卡复习课表已绿。`tests/blocklegend-review-schedule.test.mjs` 4/4。S2 未启动。
- 补记（2026-08-19 午）：E2 S2 纯函数 + 首通盖章已绿（10/10）。门 UI / 缩短副本未接。
- 补记（2026-08-19 午）：E2 S2 门 UI + 缩短副本 + 结算 + 快进已绿（13/13）。S3 未启动。未手玩。
- 补记（2026-08-19 午）：E2 S3+S4 已绿。今日冒险 / 图鉴翻卡 / 难词本 / 听说比已接线。定向 17/17，方块回归 188/188。独立仓未推。手玩步骤见 `T20260819-echoloop-borrow/05-完整测试方案.md`。不标 accepted。

## 2026-08-15 工作台对照整改 W1–W3 落地

- 日期：2026-08-15
- 变更：成人首页改「下一步」真入口，习惯改为备忘且不进年度活跃；儿童去掉「先打卡」，学习/阅读主按钮进课程；幼儿设置页与成人复盘页可导出本周 Markdown。
- 证据：`prj/app.js` `getAdultNextStep` / `renderChildPlanRows` / `buildWorkbenchWeekMemo`；`tests/workbench-contract.test.mjs` + `tests/child-workbench-ui.test.mjs` **16/16**。
- 问题：浏览器手点未做；未 commit。
- 下一步：打开成人首页确认没有习惯勾；儿童今日点「数学口算」应进课程；幼儿设置导出一份 md。

## 2026-08-15 纠正：对照文章应改工作台不是 3D 游戏

- 日期：2026-08-15
- 变更：用户指出分析优化对象是工作台项目。另开主包 `T20260815-工作台对照整改/`（成人去习惯打勾、儿童今日去学习、设置/复盘导出周总结）。前一包 A1 方块传奇回流标为偏题，代码暂留不回滚。
- 证据：`docs/plans/T20260815-工作台对照整改/` 全套控制面。
- 问题：工作台三处改动尚未执行。
- 下一步：用户确认 W1 默认「首页去掉习惯勾、checkedDates 不进年度活跃」后执行 S0 红合同。

## 2026-08-15 A1 词学回流落地（偏题，保留记录）

- 日期：2026-08-15
- 变更：blocklegend 答词经 bridge `recordWordAnswer` 回流 `courseProgress.minecraft.mastery`；答题分支静默调用，不发阳光；`data-model.md` 去掉过时的 `wordStates/charStates`。
- 证据：`prj/games/shared/workbench-bridge.js`、`prj/games/blocklegend/game.js`、`prj/games/blocklegend/index.html`、`tests/blocklegend.test.mjs` **26/26**（先红后绿）；`docs/plans/T20260815-真正的工作台整改/test-report.md`。
- 问题：浏览器 3D 手玩未做；全量 `npm test` 395/404，9 红为既有/并行问题，本包未修。A2/A3 未启动。
- 下一步：打开 `prj/games/blocklegend/index.html` 答对两张词卡，回工作台 MC英语专区看「会了」是否 +2。

## 2026-08-15 对照《什么是真正的工作台》全项目再评估

- 日期：2026-08-15
- 变更：完成一次对照分析并登记整改包；未改产品代码。结论：幼儿版数据自动沉淀基本达标；成人版全手动录入是文章反对的"人为打卡"形态；运行时无 AI 交互/素材采集；blocklegend 词卡 `learnedIds` 不回流 `courseProgress` 属数据孤岛。
- 证据：`docs/00-总控/当前状态.md` §3.21（含代码实读证据文件清单）；`docs/plans/T20260815-真正的工作台整改/`（完整控制面：README / task / steps / requirements-checklist / acceptance / test-plan / test-report）。补充侦查：`courseProgress.minecraft.mastery` 已存在（`child-courses.js`），blocklegend 已加载同一 MC 324 词库，`markKnown` 纯函数现成，A1 预计 app.js 零改动。
- 问题：A2 成人版方向（接真实痕迹 vs 收敛为记录本）未拍板；激励账本复杂度已超学习内容，本包只设红线未做削减；`docs/data-model.md` 的 `wordStates/charStates` 字段名与代码实况不符（订正列入 A1）。
- 下一步：用户拍板 A2 方向甲/乙；A1 可按 steps.md 直接启动（先红合同）。初版英文命名包已删除，由中文包替代。

## 2026-08-13 幼儿今日真实工作流卡

- 日期：2026-08-13
- 变更：对照《什么是真正的工作台》和 `docs/01-方案/优化方案2`，把幼儿今日页主 CTA 从人为打卡改为真实下一步。新增 `getPreschoolHomeWorkflow` / `renderPreschoolHomeWorkflowCard`；伙伴卡主按钮同步走同一派生；去掉“首页只负责打卡”文案。
- 证据：`prj/app.js`、`prj/css/preschool/24-game-study-loop.css`、`prj/preschool-workbench/index.html`（`app.js?v=20260813-real-workflow-card-v1`）、`tests/preschool-workbench-refresh.test.mjs`；`npm test` **120/120**；`npm run release:verify` 通过；`node --check prj/app.js` 通过。
- 问题：未做浏览器手工点卡验收；任务墙仍保留兼容勾选；世界进度和周节奏仍在首页。
- 下一步：打开 `prj/preschool-workbench/index.html#overview`，点工作流卡进入练习并核对 `completionSource=practice`。

## 2026-08-06 独立小游戏 worlds（games/）

- 日期：2026-08-06
- 变更：方块世界与横版闯关从花园 battle 拆出，落在 `games/`；工作台非花园主题「去游戏」外跳独立页；花园保留 defense。
- 证据：`games/voxel-adventure/*`、`games/platform-quest/*`、`app.js`（playbook + openPreschoolWorldGame + setPage 拦截）、`scripts/prepare-mobile.mjs`、`tests/world-games.test.mjs`；`npm test` 112/112；`npm run release:verify` 通过。
- 问题：未提交；已发布 v0.5.2 APK 不含本批；完整 Minecraft/Mario 物理未做。
- 下一步：用户确认后可选 commit；设备冒烟独立世界进出。

## 更新格式

每次更新必须写明：

```text
日期：YYYY-MM-DD
变更：完成了什么
证据：文件、测试命令、截图或发布地址
问题：仍有什么失败
下一步：只写一个最小动作
```

没有证据的任务只能写“进行中”，不能写“完成”。

## 2026-08-03 本轮更新

- 变更：完成 3 项核心 + 3 项可选的数据合同、旧快照迁移兼容、首页英语轻量测试入口和可选活动折叠；补齐独立配色实验页的游戏场景契约。
- 历史证据（2026-08-03 资料与轻量工作台阶段）：`tests/preschool-daily-plan.test.mjs`、`tests/phonics-course-data.test.mjs`、`tests/palette-comparison.test.mjs` 共 6/6；当时 `npm test` 为 72/72。当前全量基线以 BASE-001 的 105/105 为准；本地验收入口已迁移到 `http://127.0.0.1:4178/preschool-workbench/index.html`。
- 边界：60 日自然拼读资料保留在 `data/preschool/english/phonics/` 资料库；首页和今日页不读取 60 日 JSON。移除的是复杂课程引擎和复杂游戏对工作台主流程的强耦合，不是删除资料或独立游戏。
- 问题：网页测试基线已全绿；APK 构建、MuMu 安装和 GitHub 发布仍未在本轮完成。
- 下一步：记录一次工作台到独立游戏再返回的浏览器证据，之后再进入 APK 发布验收。

## 2026-08-03 花园保卫战 MVP 更新

- 变更：完成“阳光成长工作台 / 花园保卫战”命名和独立小游戏 MVP；新增版本化 `garden.defense` 状态、5 路 × 6 列棋盘、植物选择/放置/投射物、三类僵尸和 battle 离页清理。
- 历史证据（2026-08-03 花园保卫战阶段）：`tests/preschool-defense-game.test.mjs` 等专项测试 51/51；当时 `npm test` 为 73/73。当前全量基线以 BASE-001 的 105/105 为准；浏览器真实页面验证标题、30 格、植物选择改变种植、豌豆射手与坚果墙差异、普通/路障/铁桶生命值、刷新保留、离页 tick 停止；当前仍以专项合同覆盖参考宽度。
- 边界：战斗只复用现有 `growth.sunlight`、`garden.defenseEnergy` 和奖励/路由出口，不新增 localStorage key；首页不渲染战场 HUD。未实现完整塔防、多关卡、复杂 AI、多人和联网排行榜。
- 问题：APK 构建、MuMu 安装和 GitHub 发布仍未在本轮完成；需要后续观察真实操作反馈。
- 下一步：只做一次独立游戏真实操作回访，不扩展为完整塔防。

## 2026-08-03 首页草坪战场减法更新

- 变更：移除首页行动路径、重复任务面板和未使用的首页渲染函数；新增单一草坪战场面板，三条核心任务战线直接打卡，保留花园游戏与奖励中心两个出口。
- 历史证据：`app.js`、`css/preschool/24-game-study-loop.css`、`tests/preschool-workbench-refresh.test.mjs`；当时定向测试 40/40、全量 95/95；当前基线以 BASE-001 的 105/105 为准，浏览器桌面验收通过。
- 边界：60 日自然拼读仍是资料库；首页不读取 60 日 JSON，也不承载独立战斗棋盘。APK 构建、MuMu 安装和 Pages 制品验收仍冻结。
- 下一步：推送当前已验证的源码提交，之后再单独进行 APK/Pages 发布验收。

## 2026-08-03 侧边栏方向适配更新

- 变更：横屏窄视口不再自动移出侧边栏；竖屏仍默认收起并支持顶部菜单、关闭按钮和遮罩手动控制；入口 CSS 查询版本更新为 `20260803-sidebar-orientation-ratio-important`。
- 历史证据：`tests/preschool-workbench-refresh.test.mjs` 新增方向契约；当时 `npm test` 95/95；当前基线以 BASE-001 的 105/105 为准；`node --check app.js`、`git diff --check`；横屏侧栏规则由方向专项合同覆盖。
- 问题：APK 构建、MuMu 安装和 GitHub 发布仍未在本轮完成。
- 下一步：推送本轮已验证的源码提交。

## 2026-08-06 游戏化学习工作台轻量证据闭环

- 变更：参考 `G:\UserCode\跨平台采集器\notes\游戏设计\游戏化学习工作台` 的本地案例，将幼儿首页收敛为“专属伙伴 → 今日行动 → 轻量练习/打卡 → 完成证据 → 阳光/成长/豌豆能量 → 花园游戏/奖励出口”；首页不再承担完整课程或战斗棋盘。
- 运行改动：内置计划增加可选 `practiceLessonId`；旧快照只补缺失映射，不覆盖改名、完成状态或自定义计划；练习完成在同一次提交中同步来源计划，奖励仍走 `lesson:<id>` 幂等事件。
- 证据：`tests/preschool-workbench-refresh.test.mjs` + `tests/preschool-daily-plan.test.mjs` 定向 52/52；`npm test` 全量 105/105；`node --check app.js`、`node --check storage.js`、`node --check tests/helpers/css-graph.mjs`、`git diff --check` 通过。
- 浏览器：明确加载 `http://127.0.0.1:4178/preschool-workbench/index.html?cb=20260806-v050-landscape-fix-3#overview`；390×844 竖屏无横向溢出、侧栏默认收起；844×390 横屏侧栏常驻、菜单和底部快捷导航隐藏；英语练习使任务 3/6→4/6、阳光 100→120、豌豆能量 4→5、练习证据 0→1，刷新后保持，运行日志为空。
- 边界：60 日自然拼读和各科资料继续保留为资料库；移除的是复杂课程/复杂游戏与首页主流程的强耦合，不删除资料，也不把独立游戏宣称为完整原版复刻。
- 工作树：正式记录写在主项目 `docs\00-总控` 与 `docs\优化`；本轮未创建 worktree 总控。现有 `.worktrees` 分支和资料目录仍有未合并内容，未擅自清理或合并。
- 任务墙补充：参考 WorkBuddy/暑期作业台的任务密度，将首页六条任务改为响应式任务墙；入口 CSS 查询升级为 `20260806-task-wall-v1`。
- 发布状态：源码提交 `1b35b0c` 已推送 `main`；本地 Android 因 SDK 缺失阻塞，GitHub Pages/Actions/Release、APK 下载和 MuMu 仍未形成新证据。
- 下一步：读取远端 Actions/Pages 结果并核对 APK artifact；在获得证据前保持发布任务进行中。

## 2026-08-06 发布门禁优化

- 变更：新增 `docs/plans/2026-08-06-release-gate-and-publish.md`，把五入口顺序、真实素材路径、网页回归、Android 包装和远端证据拆成独立验收项。
- 代码：新增 `scripts/release-verify.mjs` 与 `tests/release-contract.test.mjs`；`package.json` 增加 `npm run release:verify`；`.github/workflows/android-apk.yml` 在 Android 构建前运行全量测试、发布合同和语法检查，构建后拒绝缺失/空 APK。
- 本地证据：`npm test` 为 106/106；`npm run release:verify` 通过；`node --check launcher.js`、`node --check app.js`、`node --check storage.js`、`git diff --check` 通过；`npm run android:prepare` 复制 13 个文件和 5 个目录到 `dist/`。
- 制品证据：根入口、三个幼儿主题入口、成人入口、儿童入口和三张主题主图均存在；临时 HTTP 服务请求根入口、5 个入口和 3 个图片资源共 9 条路径，全部返回 HTTP 200。
- 问题：本机 Android 构建因缺少 `ANDROID_HOME`/`android/local.properties` 中的 SDK 路径失败；Release APK 的 HEAD 元数据可读，但 `gh release download`/`curl` 大文件传输在约 5.4 MB 后超时，尚未形成完整 APK 文件；MuMu 设备虽连接，尚未安装。
- 下一步：从可完成大文件传输的网络或浏览器下载 `v0.5.1` APK，再执行 MuMu 安装验收；不使用部分文件安装。

## 2026-08-06 v0.5.1 远端发布

- 提交与标签：`a8b371f` 已推送 `main`；`v0.5.1` 标签已推送。
- Actions：main run `31080823999`、标签 run `31080849490` 均为 `completed / success`；标签 run 生成并上传 APK，Release 已发布：<https://github.com/nonomil/personal-workbench/releases/tag/v0.5.1>。
- APK 资产：Release 元数据确认 `personal-workbench-v0.5.1.apk`，类型 `application/vnd.android.package-archive`，大小 `60,140,096` bytes；HEAD 请求 HTTP 200。完整下载在本机 CDN 传输超时，未宣称已下载/安装。
- Pages：根入口、三个幼儿主题 HTML、成人入口、儿童入口均 HTTP 200；三张在线主题主图均 HTTP 200；根页面包含 `幼儿游戏世界`、`data-workbench-theme` 和五入口标记。
- 设备：MuMu `127.0.0.1:16384` 为 `device`；本机 Gradle 已同步 WebView 资源但因没有可识别 Android SDK 失败，因此当前没有可安装 APK 文件。

## 2026-08-06 v0.5.2 本机代理 APK 下载与 MuMu PARTIAL 验收

- 日期：2026-08-06
- 变更：对齐交接包设备证据；将旧“APK 未下载/MuMu 未装”标为历史，写入本机 PARTIAL 验收事实；不删除 v0.5.1 与门禁历史段。
- 证据：
  - 源码：`npm test` 108/108；`npm run release:verify` 通过（五入口 + 5 素材）。
  - 有效 APK：`C:/Users/No'mi'l/AppData/Local/Temp/personal-workbench-release-v0.5.2/personal-workbench-v0.5.2-proxy.apk`；`60,140,796` bytes；SHA-256 `40cbf426afe619354a04307dee0532aaa7cb0cdb0935c7df2c9ae594c1f66fa8`。
  - 禁用残缺包：`6,602,752` bytes 的非 proxy 文件。
  - 下载：代理 `127.0.0.1:10808` 完整下载成功。
  - MuMu：`127.0.0.1:16384` install Success；`com.nonomil.personalworkbench/.MainActivity` 前台；首卡进入阳光花园工作台。
  - 截图：`mumu-launch.png`、`mumu-garden.png`（temp，不提交仓库）。
  - 包内版本：`versionName=1.0` / `versionCode=1`；Debug 包；不与 Git tag `v0.5.2` 声称内部版本已同步。
- 问题：全核心路线未做；不得把 PARTIAL 写成全功能通过。正式签名/商店/云同步/Supabase/课程全量运行时不在本批。
- 下一步：V10 — 三幼儿主题 → 轻量练习 → 返回花园 → 刷新持久化；仅修真实复现问题；回归后写回本看板与 `当前状态.md`。

## 2026-08-06 v0.5.2 MuMu V10 幼儿核心路线验收（CC 续接）

- 日期：2026-08-06
- 变更：在已校验 proxy APK 上完成幼儿最小核心路线设备验收；无业务代码修改；文档写回 PARTIAL + V10 证据。
- 证据：
  - 设备：MuMu `127.0.0.1:16384`；adb `D:/Program Files/Netease/MuMu/nx_main/adb.exe`；WebView CDP `webview_devtools_remote_<pid>` → `127.0.0.1:9333`。
  - 根入口：`https://localhost/index.html?choose=1` 显示 GARDEN/VOXEL/QUEST；`pw-v10-launcher.png`。
  - 三主题：`garden-defense` / `voxel-adventure` / `platform-quest` 首页标题与侧栏主题正确；`pw-v10-theme-garden|voxel|platform.png`。
  - 练习：`open-plan-practice` → `lesson-answer`「明月」→ `lesson-finish`；store 阳光 40→60、defenseEnergy 0→1、unicorn.xp 40→60；UI 1/6→2/6；`completedLessonIds` 含 `preschool-poetry-1`；`pw-v10-practice-seq.json`、`pw-v10-practice-finished.png`。
  - 返回花园：`#overview` 成功。
  - 持久化：`am force-stop` + 冷启动后阳光/能量/XP/课程进度/`updatedAt` 一致；`pw-v10-after-restart-final.json`、`pw-v10-restart-final.png`。
  - 共享数据：方块主题读取同一 `petbank_huchuliang_preschool_workbench_state_v1`。
  - 回归：`npm test` 108/108；`npm run release:verify` ok；logcat FATAL=0。
- 问题：无本轮业务复现失败。成人/儿童 APK 路线、正式签名、商店发布仍未做。
- 下一步：可选成人/儿童冒烟；否则冻结扩展并保持 PARTIAL。

## 2026-08-06 成人/儿童 APK 入口冒烟

- 日期：2026-08-06
- 变更：在同一 MuMu 会话补五入口中成人/儿童首页冒烟；不改业务代码。
- 证据：
  - 根入口卡 href：`./成人成长工作台/index.html`、`./儿童学习工作台/index.html`。
  - 成人：标题「成人成长工作台」，`#overview` 可见；`pw-v10-adult-click.png`。
  - 儿童：标题「儿童学习工作台」，今天/成长地图可见；`pw-v10-child-click.png`。
  - 错误路径 `adult-workbench`/`child-workbench` → HTTP 失败页（记录以免误用）。
  - FATAL=0；汇总 `pw-v10-adult-child-summary.json`（temp）。
- 问题：仅入口+首页，未验证成人/儿童任务打卡与持久化。
- 下一步：无新需求则冻结；需要时另开成人/儿童深度设备切片。

## 2026-08-06 根入口首屏多工作台（取消冷启动自动跳转）

- 日期：2026-08-06
- 变更：响应「打开网页首先看多个工作台」；关闭根入口冷启动自动跳转，五卡始终先出现。
- 证据：
  - 代码：`launcher.js` `shouldAutoRedirect` → 恒 `false`；`index.html` 仍可展示最近徽章与「重新选择」。
  - 测试：`tests/launcher.test.mjs` 红→绿；`npm test` 108/108；`npm run release:verify` ok。
- 问题：当前 Release/已装 v0.5.2 APK 尚未包含此修复；未重打 tag、未改 Release 资产。
- 下一步：用户确认后再提交源码；需要设备验证时另开冒烟，不扩课程/游戏。

## 2026-08-14 学习模块扩展与小游戏重构（v0.6.0）

- 日期：2026-08-14
- 变更：新增 9 个学习模块（识字/拼音/古诗/自然拼读/英语词汇/数学/成就/宠物/成长世界）；重构三游戏（花园保卫多波次、方块世界岛屿、横版闯关物理）；数据题库入 `prj/data/preschool/`。
- 证据：
  - 审查：两轮子代理深审 app.js 与三游戏；发现并修复花园保卫多波次卡死（`spawnWave` 守卫误拦 `won` 状态）。
  - 测试治理：`--test-concurrency=1` 根除并发 `globalThis` 污染；同步过时断言；`npm test` **202/202**；`release:verify` 通过。
  - 版本：`package.json` → 0.6.0；`docs/04-发布/v0.6.0.md`、`docs/CHANGELOG.md`、`当前状态.md` 3.13 已更新。
- 问题：APK/MuMu 未按 v0.6.0 重新验收（仍 PARTIAL）；platform-quest 击退/碰撞两个中等问题已记录未修。
- 下一步：浏览器过一遍新学习模块闭环；如需发版打 `v0.6.0` 标签触发 APK 构建。
