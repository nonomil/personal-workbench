# 个人工作台 v0.5.2

> 状态：根入口首页优化版；网页门禁与静态制品验收以本文件的实际记录为准。

## 本版变化

- 根入口采用明确的“两层五入口”结构：上方三个幼儿游戏世界，下方成人与少儿两个通用工作台。
- 三个幼儿入口分别传递 `garden-defense`、`voxel-adventure`、`platform-quest` 主题参数，仍进入同一个 `preschool-workbench/index.html` 和同一份本地成长快照。
- 幼儿世界卡使用更强的主题视觉，成人/少儿卡使用更紧凑的通用工作台展示节奏。
- 增加跳到入口的可访问链接、世界/通用卡语义标记、键盘焦点和 44px 触控区域。
- 修复 320px 手机宽度在存在竖向滚动条时由 `body min-width: 320px` 引发的横向滚动；手机标题改为可控断行。
- 不新增 localStorage key，不加载 60 日课程资料到根入口，不改变独立花园游戏规则。

## 本地证据

- `npm test`：108/108 通过。
- `npm run release:verify`：通过，3 个幼儿主题 + 2 个通用工作台，5 张卡片素材存在。
- `node --check launcher.js`、`node --check app.js`、`node --check storage.js`、`node --check config.js`、`git diff --check`：通过。
- `npm run android:prepare`：成功复制 13 个文件和 5 个目录到 `dist/`。
- 浏览器静态服务 `http://127.0.0.1:4179/?choose=1&cb=20260806-launcher-ux`：根入口的五张卡、三张幼儿主图和五个目标页面可读取；375/768/1024/1440px 无横向溢出，320px 修复后 `htmlScrollWidth=305`、`clientWidth=305`。
- 浏览器跳转：点击植物世界进入 `preschool-workbench/index.html?theme=garden-defense#overview`，页面变体为 `preschool`。

## 远端证据

- 提交、标签、GitHub Actions、Pages 和 Release：待推送后填写。
- APK 下载、安装和 MuMu 验收：本版不把网页门禁通过误写成 APK 已安装；沿用 `v0.5.1` 中的 Android SDK/大文件下载阻塞记录，除非本轮取得新证据。

## 回滚边界

- 若入口视觉不合适，只回滚 `index.html` 的根入口样式和卡片标记；不回滚幼儿工作台、课程资料或成长快照。
- 若发布门禁失败，保留源码和测试修复，不把未验证的 tag 或 Release 宣称为已发布。
