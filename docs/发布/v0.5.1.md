# 个人工作台 v0.5.1

> 状态：发布候选，已完成源码门禁；GitHub Release、APK 下载和 MuMu 安装只在对应事实发生后填写。

## 本版变化

- 新增 `npm run release:verify` 与发布合同测试，锁定根入口上方三个幼儿游戏世界、下方成人与少儿两个通用工作台。
- GitHub Actions 在构建 Android 前运行 `npm test`、发布合同和关键语法检查，构建后检查 Debug APK 非空。
- 同步 `package.json`、`package-lock.json`、三个工作台页脚和配置版本为 `v0.5.1`。
- 不扩展课程引擎、不改变独立花园游戏、不新增积分账本或 localStorage key。

## 本地证据

- `npm test`：106/106 通过。
- `npm run release:verify`：通过，3 个幼儿主题 + 2 个通用工作台，5 张卡片素材存在。
- `node --check launcher.js`、`node --check app.js`、`node --check storage.js`：通过。
- `npm run android:prepare`：成功复制 13 个文件和 5 个目录到 `dist/`。
- 本地 HTTP 服务请求根入口、5 个入口和 3 张主题图共 9 条路径：全部 HTTP 200。

## 远端证据

- `a8b371f` 已推送 `main`；main Actions run `31080823999`：`completed / success`。
- `v0.5.1` 标签已推送；标签 Actions run `31080849490`：`completed / success`。
- GitHub Release：<https://github.com/nonomil/personal-workbench/releases/tag/v0.5.1>；资产 `personal-workbench-v0.5.1.apk`，类型 `application/vnd.android.package-archive`，大小 `60,140,096` bytes，HEAD HTTP 200。
- GitHub Pages：根入口、三个幼儿主题入口、成人入口和儿童入口均取得 HTTP 200；三张在线主题图均取得 HTTP 200，在线页面版本标识为 `v0.5.1`。
- 本机完整 APK 下载仍未完成：`gh release download` 与 `curl` 均在大文件传输阶段超时，约 5.4 MB 的部分文件已判定不可用，不进入安装流程。
- 本机 Android 构建已同步 WebView 资源，但 Gradle 报错 `SDK location not found`；环境缺少 `ANDROID_HOME`/`android/local.properties` SDK 路径。
- MuMu 已连接 `127.0.0.1:16384 device`，但没有完整 APK 文件，尚未执行安装和功能验收。
