# 个人工作台 v0.5.0

> 状态：源码发布候选。网页代码可推送到 `main`，APK、GitHub Actions、Pages 和 MuMu 的状态只填写实际验收结果。

## 本版变化

- 参考本地“游戏化学习工作台”案例，保留专属伙伴、今日任务、完成证据、阳光/成长和奖励出口的短闭环。
- 首页任务墙在宽屏/中屏使用两列，竖屏使用一列；任务标题、完成状态和轻量练习保持可读。
- “今日草坪战场”标题旁新增“安排任务”快捷入口，直接打开现有计划弹窗；任务仍可改名、删除、打卡，奖励仍走现有幂等记录。
- 复杂植物大战僵尸玩法继续位于独立“花园保卫战”页面；60 日自然拼读和各科学习资料继续作为资料库，不作为首页课程引擎。
- 根入口调整为上方三个幼儿游戏世界（植物大战僵尸式花园、我的世界式方块探险、马里奥式横版闯关），下方两个通用工作台（成人、少儿）；三套运行工作台和 npm 包版本统一为 `v0.5.0`。

## 本地证据

- `node --test tests/preschool-workbench-refresh.test.mjs`：48/48 通过。
- `npm test`：105/105 通过。
- `node --check app.js`、`node --check storage.js`、`node --check tests/helpers/css-graph.mjs`：通过。
- `git diff --check`：通过。
- 本机 Android 构建仍受 Android SDK 配置影响；没有 APK 文件就不宣称 APK 已产出。

## 远端验收记录

源码提交 `1b35b0c` 已推送到 `main`。本轮尚未读取到新的 GitHub Actions run、Pages 部署和 APK artifact 证据，因此不宣称网页已完成远端部署、APK 已生成或 MuMu 已安装；只有 Actions 真实生成并下载 Debug APK 后，才能继续填写 Release 和 MuMu 安装结果。正式签名包不属于本版交付。
