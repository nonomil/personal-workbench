# 下一步行动

> 优先级依据：管线已在两个形态差异大的角色（人形村民、大体型铁傀儡）上验证，
> P0 是直接复用到剩余角色——证据直接支持，无需再探索。

## P0 · 动物五只迁移到 atlas 管线（预计 0.5~1h）

1. 跑编译器（specs.json 已含 pig/cow/sheep/chicken/wolf 规格）：

```powershell
cd g:\StudyCode\个人工作台
foreach ($m in 'pig','cow','sheep','chicken','wolf') {
  python prj/games/blocklegend/tools/fourview-to-atlas.py --model $m
}
```

2. `createProps3d.js` 各动物工厂头部加委托（照 createVillager 的三行模式）。
3. `review-roster.html` / `index.html` 加 `<script src="./assets/atlas4v/<id>-atlas.js?...">`。
4. `compare-four-view.html` 的 MODELS 数组加 5 只动物，逐格对比；四足动物注意
   头在 +z，若图集侧视头尾反了，检查 specs.json 里盒子 z 值符号。
5. 每只通过后截图存 `prj/assets/generated/blocklegend-roster/review/<id>-4view-compare.png`，
   并在 `tests/blocklegend.test.mjs` 的 atlas 测试里把 id 加进数组。

## P1 · 新增敌对生物（恶魂/烈焰人/循声守卫，用户点名）

先走生图：用 `gpt-game-asset-pipeline`（Cliprox gpt-image-2）按现有 prompt 模板
（`prj/assets/generated/blocklegend-roster/source/prompts/`）出 2×2 四视图 →
用户过目 → specs.json 加 cuboid 规格（查公开 ModelRenderer 尺寸，不要目测）→
走 P0 同样的编译+验收流程。

## P2 · 验收自动化（可选增强）

对比页目前人眼验收。可加像素级指标：四向渲染 canvas.toDataURL 与参考图
象限做轮廓 IoU / 主色直方图距离，低于阈值直接标红。做之前先确认人眼验收
确实成为瓶颈，避免过度建设。

## 风险与阻塞

- 无硬阻塞。已知坑全部记录在 技术分析/解决方案.md 的 8 条调试经验里。
- 提醒：git 工作区有大量未提交文件（含本轮全部产物），建议接手后先让用户
  决定是否提交一版快照再继续。
