---
title: 个人工作台 v0.5.2 发布与 MuMu 验收测试分析报告
seq: 001
created_at: 2026-08-06
status: partial
confidence: high
---

# 个人工作台 v0.5.2 发布与 MuMu 验收测试分析报告

> 一句话结论：源码门禁、APK 完整下载、安装、启动和首个幼儿花园跳转通过；APK 全核心路线仍是部分验收。

## 执行摘要

**结论**：⚠️ PARTIAL

| 指标 | 目标 | 实测 | 结果 |
|------|------|------|------|
| 源码回归测试 | 108 项通过 | 108 通过、0 失败 | ✅ |
| 发布合同 | 五入口、5 个素材 | 通过 | ✅ |
| APK 完整性 | 60,140,796 bytes + 指定 SHA-256 | 两项均匹配 | ✅ |
| MuMu 安装 | 可安装 | `Success` | ✅ |
| APK 启动 | `MainActivity` 前台 | 已确认 | ✅ |
| 首个幼儿入口 | 进入阳光花园 | 已确认 | ✅ |
| 三主题完整路线 | 3 个主题 + 练习 + 返回 + 持久化 | 未执行 | 🔲 |

## 数字自洽

- `npm test`：总计 108，成功 108，失败 0，跳过 0，待办 0。
- APK：发布响应 `Content-Length=60,140,796`，本地文件 `Length=60,140,796`；大小差为 0。
- SHA-256：本地值与发布记录完全一致。

## 验证项明细

| # | 验证项 | 方式 | 实测 | 结果 |
|---|--------|------|------|------|
| V1 | 源码回归 | `npm test` | 108/108 | ✅ |
| V2 | 发布合同 | `npm run release:verify` | 3 幼儿主题 + 2 通用工作台，5 素材 | ✅ |
| V3 | 代理可用 | `curl.exe --proxy http://127.0.0.1:10808 --head --location` | HTTP 200，Content-Length 60,140,796 | ✅ |
| V4 | APK 哈希 | `Get-FileHash -Algorithm SHA256` | `40cbf426afe619354a04307dee0532aaa7cb0cdb0935c7df2c9ae594c1f66fa8` | ✅ |
| V5 | MuMu 设备 | `adb devices -l` | `127.0.0.1:16384 device` | ✅ |
| V6 | APK 安装 | `adb install -r` | `Success` | ✅ |
| V7 | APK 启动 | `monkey -p ... -c LAUNCHER 1` + `dumpsys activity` | `MainActivity` 前台 | ✅ |
| V8 | 根入口 → 花园 | MuMu 触控 + 截图 | 阳光花园工作台首页可见 | ✅ |
| V9 | 启动崩溃 | `adb logcat -d -t 300` | 0 条 `FATAL EXCEPTION`/`AndroidRuntime` | ✅ |
| V10 | 三主题全流程 | MuMu 操作 | 尚未执行 | 🔲 |

## 图像证据

- 启动页：`C:/Users/No'mi'l/AppData/Local/Temp/personal-workbench-release-v0.5.2/mumu-launch.png`。
- 花园页：`C:/Users/No'mi'l/AppData/Local/Temp/personal-workbench-release-v0.5.2/mumu-garden.png`。

## 失败项与历史问题

- 旧安装第一次返回 `INSTALL_FAILED_UPDATE_INCOMPATIBLE`，原因是同包名旧包签名不同；用户确认后卸载旧包，重新安装返回 `Success`。
- 直接 CDN 下载曾在大文件传输阶段超时；改走 10808 代理后完整下载成功。
- 旧的 6,602,752 字节文件是残缺下载，不能用于安装或验收。

## 尚未执行

- 三个幼儿主题逐项点击和主题状态验证。
- 简单练习完成、奖励幂等、回到花园和刷新持久化。
- 成人/儿童工作台 APK 设备级路线。

## 风险与建议

| 风险 | 等级 | 缓解 |
|------|------|------|
| 旧总控状态落后 | 高 | 先更新三份总控/发布文档，保留历史记录 |
| 部分设备证据被误报为全量通过 | 高 | 保持 PARTIAL，完成 V10 后再改状态 |
| APK 仍为 Debug 包 | 中 | 正式签名另开发布任务 |

