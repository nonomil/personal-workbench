---
title: 个人工作台发布验收与 CC 续接
seq: 001
created_at: 2026-08-06
status: active
confidence: high
handoff_from: Codex
handoff_to: CC
---

## 现状一句话

个人工作台 v0.5.2 的网页、五入口启动器、GitHub Actions APK、代理下载、MuMu 安装和“根入口 → 植物大战僵尸式花园”启动路径已经有直接证据；接手者第一步不是继续扩展功能，而是把旧总控/发布文档中的过期 APK 状态与最新证据对齐，再补齐 APK 全核心路线验收。

## 完整度模式

完整模式。关键数字、路径、安装错误根因、当前边界和 CC 启动提示词已内嵌；长代码、长日志和历史方案只引用原路径。

## 建议阅读顺序

1. 本文件：确认边界、证据和接手协议。
2. `测试分析报告.md`：查看本轮新鲜测试与 MuMu 证据。
3. `背景上下文/关键发现.md`：查看实现事实和文档口径冲突。
4. `行动项/下一步行动.md`：按 P0 顺序继续。
5. `技术分析/解决方案.md`：确认允许做什么、禁止做什么。
6. `G:/StudyCode/个人工作台/docs/00-总控/`：完成 reconciliation 后再作为正式总控。

## 关键指标摘要

| 指标 | 目标 | 当前实测 | 结果 | 来源 |
|------|------|----------|------|------|
| Node 回归测试 | 108 项全通过 | 108 通过、0 失败 | ✅ | `npm test`，本轮新鲜执行 |
| 发布合同 | 五入口、5 个素材 | 通过 | ✅ | `npm run release:verify`，本轮新鲜执行 |
| APK 文件大小 | 60,140,796 bytes | 60,140,796 bytes | ✅ | `C:/Users/No'mi'l/AppData/Local/Temp/personal-workbench-release-v0.5.2/personal-workbench-v0.5.2-proxy.apk` |
| APK SHA-256 | 发布记录值 | `40cbf426afe619354a04307dee0532aaa7cb0cdb0935c7df2c9ae594c1f66fa8` | ✅ | `Get-FileHash` |
| MuMu 安装 | 安装成功 | `adb install -r` 返回 `Success` | ✅ | MuMu `127.0.0.1:16384` |
| APK 核心路线 | 根入口、三主题、练习、花园、刷新持久化 | 只完成根入口 → 第一个幼儿花园启动 | 🔲 | `测试分析报告.md` |

## 已完成

- 根入口已改为两层五入口：上方三个幼儿游戏世界，下方成人工作台和儿童工作台。
- 三个幼儿主题共用同一工作台、localStorage 和成长数据，不另造三套业务内核。
- 60 日自然拼读和各学科资料包保留为资料库；首页不全量读取 60 日课程 JSON，也不把复杂课程引擎塞进工作台首页。
- 工作台最小闭环保留今日任务、打卡、简单练习入口、阳光/成长、奖励和独立游戏出口。
- 植物能力边界已由测试覆盖：豌豆射手发射，坚果墙阻挡，其他植物走各自效果；僵尸死亡状态不会被归一化逻辑复活。
- `v0.5.2` 已推送到 `main`，GitHub Actions 和 Release 已生成 Debug APK。
- 本轮已用 10808 代理解决大文件下载超时，并完成 MuMu 安装、启动和第一个幼儿花园页面跳转。

## 未完成或暂不验收

- APK 尚未逐项验证：三个幼儿主题、轻量练习/测试、返回花园、任务/阳光持久化刷新。
- 成人和儿童两个通用工作台的 APK 入口尚未做设备级核心路线验收。
- APK 是 Debug 包；Android `versionName=1.0`、`versionCode=1` 仍是 Capacitor 默认值，`v0.5.2` 由仓库版本、Git tag 和 Release 承载。
- 正式签名 APK、应用商店发布、多设备云同步和 Supabase 方案不属于本轮完成项。
- 60 日课程资料未全量接入工作台运行时；不要把资料 JSON 存在写成完整课程产品。

## 口径说明

- 采用本交接包和 `测试分析报告.md` 中 2026-08-06 本轮直接命令/设备证据，原因是它们晚于旧发布文档并由本机实际复核。
- `docs/发布/v0.5.2.md`、`docs/00-总控/当前状态.md`、`docs/00-总控/进度看板.md` 仍保留“APK 下载/安装/MuMu 未完成”的旧记录；这些文件不是本轮设备事实，CC 必须先 reconciliation，不能删除历史，只更新当前状态和证据链接。
- 完整核心路线尚未实测，因此不能把“安装成功 + 进入第一个花园”升级成“APK 全功能验收完成”。

## 禁区

- 不要在 `worktree` 或本交接目录创建第二套长期总控；正式总控只在 `docs/00-总控/`。
- 不要回退 `7b1943a`、`168ad67` 或旧 UI，只因为历史文档仍写旧状态。
- 不要恢复 Supabase 为默认后端；项目保持本地优先和自托管边界。
- 不要把 60 日课程全量接入首页，也不要把复杂战斗重写进工作台主线。
- 不要使用 `personal-workbench-v0.5.2.apk` 的 6,602,752 字节残缺文件；有效文件是带 `-proxy` 后缀的 60,140,796 字节文件。
- 不要提交 APK、MuMu 截图、临时下载文件、密钥或浏览器配置到 Git。

## 接手确认协议

CC 必须先读本 README、`测试分析报告.md` 和 `行动项/下一步行动.md`，然后向用户复述：需求、当前已验证事实、下一步方案和禁区；用户确认方向后才能修改代码或总控文档。交接包、凝蜕状态和 transcript 若有冲突，先停下问用户，不自行选择口径。

## 当前会话 transcript 路径

- 项目内规范路径 `.claude/backups/{session-id}.jsonl`：本项目未发现 `.claude/` 目录。
- 平台原始记录候选：`C:/Users/No'mi'l/.codex/sessions/2026/08/06/rollout-2026-08-06T17-31-19-019fd669-b345-7e61-a236-8c1f16d72c83.jsonl`。这是平台候选路径，CC 需按实际会话核对；禁止整读，只能用关键词定位并按 offset/limit 读取。

## 凝蜕状态指向

- `.claude/state/session-progress.md`：项目不存在，不能把缺失文件当成最新状态。
- `.claude/memory/recent-decisions.md`：项目不存在。
- 优先级链：本交接包 → 凝蜕（若后续建立）→ transcript；任两层冲突都停下问用户。

## CC 下一会话启动提示词

```text
接手 G:/StudyCode/个人工作台 的“发布验收与文档 reconciliation”任务。先读：
1. G:/StudyCode/个人工作台/docs/handoff/001_个人工作台发布验收与CC续接_20260806/README.md
2. 同目录/测试分析报告.md
3. 同目录/行动项/下一步行动.md
4. G:/StudyCode/个人工作台/docs/00-总控/当前状态.md
5. G:/StudyCode/个人工作台/docs/00-总控/进度看板.md

读完后先向我复述：
- 已完成的网页/发布/APK/MuMu 事实；
- 仍未完成的 APK 全核心路线；
- 旧总控文档与本轮设备证据的冲突；
- 你准备执行的最小步骤和禁区。

在我确认方向前不要改代码或文档。确认后按 P0 顺序执行：
1. 先更新 v0.5.2 发布说明、总控当前状态和进度看板，保留历史并写入本轮真实 APK/MuMu 证据；不要把部分设备验证写成全功能通过。
2. 使用已经校验的 APK：
   C:/Users/No'mi'l/AppData/Local/Temp/personal-workbench-release-v0.5.2/personal-workbench-v0.5.2-proxy.apk
   SHA-256：40cbf426afe619354a04307dee0532aaa7cb0cdb0935c7df2c9ae594c1f66fa8
3. 在 MuMu `127.0.0.1:16384` 验证根入口 → 三个幼儿主题 → 轻量练习/测试入口 → 返回花园 → 刷新后的任务/阳光状态；每一步记录命令和截图路径。
4. 若出现真实业务失败，再做最小修复；否则不要扩展课程、游戏、Supabase 或新入口。
5. 运行 `npm test`、`npm run release:verify`、必要的语法检查，并将结果写回交接/总控；不要提交 APK 和临时截图。
```

## 下一跳建议

- **建议接手者**：CC。
- **建议先调用的 skill**：`verification-before-completion`；如果发现功能失败，再用 `systematic-debugging`；若要修改多步实现，先用计划技能。
- **接手后第一步**：只读 reconciliation，随后执行“APK 全核心路线”设备验收。

