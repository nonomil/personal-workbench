---
title: 问题背景
seq: 001
created_at: 2026-08-06
status: verified
confidence: high
---

# 问题背景

## 问题起因

个人工作台经历了成人、少儿、幼儿三个入口和三个幼儿视觉主题的多轮调整。当前目标已收敛为：根入口提供三个幼儿游戏世界和两个通用工作台，网页与 APK 共用同一份资源，工作台保持轻量打卡与简单练习，复杂趣味集中到独立游戏环节。

此前 GitHub Release 的 APK 大文件下载在 CDN 传输阶段超时，MuMu 中还存在不同签名的旧包。最新一轮使用本机 10808 代理下载，解决了文件获取问题，并完成旧包清理、重新安装和第一个幼儿花园入口验证。

## 项目位置

- 根启动器：`index.html`、`launcher.js`、`config.js`。
- 幼儿运行入口：`preschool-workbench/index.html`及其 CSS/JS/素材。
- 测试入口：`tests/*.test.mjs`，发布合同：`scripts/release-verify.mjs`。
- 正式总控：`docs/00-总控/`；本交接包只承担跨会话接力，不替代总控。

## 时间线

1. `7b1943a`：完成五入口启动器刷新并打 `v0.5.2` 标签。
2. `168ad67`：记录 v0.5.2 发布证据并推送 `main`。
3. 本轮：`npm test` 108/108、`release:verify` 通过；经 10808 下载 APK 并校验。
4. 本轮：MuMu 卸载签名冲突旧包，安装成功，启动 `MainActivity`，点击第一个入口进入阳光花园。

## 接手边界

- ✅ 已验证：源码门禁、Release APK 完整下载、APK 安装、启动和第一个幼儿主题跳转。
- 🔲 待验证：三主题全路径、轻量练习、返回花园和刷新持久化。
- ❌ 禁止：以资料库存在代替课程运行时完成，以一次页面截图代替完整 APK 验收。

