# 01 P0：仓库基线与产品减法分计划

## 范围

覆盖 M0、M1、M2。目标是先让产品简单、数据稳定，不做大规模技术重构。

## 顺序

1. `M0-GOV-001` 只读基线。
2. `M0-GOV-002` 建立恢复点。
3. `M0-GOV-003` 写目标合同测试。
4. `M1-IA-001/002` 三卡和主题选择。
5. `M1-IA-003/004` 五导航和旧路由。
6. `M2-UX-001～003` 首页与二级归并。
7. `M2-UX-004` 浏览器/设备核心旅程。

## 文件边界

主要允许：`prj/index.html`、`prj/launcher.js`、`prj/preschool-workbench/index.html`、`prj/config.js`、`prj/app.js`、现有幼儿 CSS 和相关 tests。

禁止：存储 key、奖励算法、独立游戏规则、课程数据批量改写、素材物理删除。

## P0 完成

- GATE-0、GATE-1、GATE-2 通过。
- 三卡、五导航、首页四块真实可用。
- 旧快照和奖励幂等通过。
- 尚未模块化可接受，但不得新增更多 `app.js` 业务。

