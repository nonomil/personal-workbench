你总结得很到位——“资料侧更厚，但大部分还没接到页面”。核心矛盾是：**数据已经有了，但 playable 的内容还没铺开**。

下面按你文档里的缺口，逐一给出可直接参考/复用的开源资料库。

---

## 一、古诗专区：从 3 首 → 小学必背 75+ 首

**你现在**：8 首诗库，只有 3 首接入了 playable 课时。

**可用的开源资料库**：

| 项目 | 数据量 | 格式 | 直接可用性 |
|------|--------|------|-----------|
| **chinese-poetry** | 5.5 万首唐诗 + 26 万首宋诗 | JSON | ⭐⭐⭐⭐⭐ 直接取“唐诗三百首”子集 |
| **小学必背古诗 72 首** | 72 首，4-12 岁适用 | 内置数据 | ⭐⭐⭐⭐⭐ 可直接抄诗单 |
| **chinese-gushiwen** | 1 万首古文，含作者/名句 | JSON | ⭐⭐⭐⭐ 可作为补充来源 |

**chinese-poetry** 的 JSON 格式可以直接读取，你只需要从全唐诗中筛选出“小学必背 75 首”对应的诗，转换成工作台的数据格式即可——**数据已经有了，不需要自己编**。

**整合方式**：
1. 从 `chinese-poetry` 的 `全唐诗` 目录中提取 75 首诗的数据（标题、作者、正文）
2. 加上你已有的译文（或从 `chinese-gushiwen` 的 JSON 中提取译文/注释）
3. 将 75 首诗分配到 60 天路线中（每天 1-2 首）
4. 现有的“听上句选下句”玩法直接复用

---

## 二、360 节教案：逐科接到页面

**你现在**：6 科 × 60 节 = 360 节教案 JSON 已存在 `data/preschool/`，但“还没逐节接到页面”。

**可用的开源资料库**：

| 项目 | 内容 | 格式 | 直接可用性 |
|------|------|------|-----------|
| **yxj-workbench** | 5-6 岁每日打卡：语文+数学+英语+运动+游戏 | HTML/CSS/JS | ⭐⭐⭐⭐⭐ 设计思路完全一致 |
| **kids-learning-cards** | 5 种卡片：识字/词语/英语/拼音/数字 | YAML + Python | ⭐⭐⭐⭐ 卡片生成管道可复用 |
| **Marble os-taxonomy** | 小学课程知识图谱，1590 个核心概念 | 知识图谱 | ⭐⭐⭐ 可作为内容组织参考 |

**yxj-workbench** 和你做的是同一件事——5~6 岁幼小衔接工作台。它的功能模块几乎一一对应：
- 每日古诗 32 首（含拼音+译文+朗读）
- 每日 3 个新字 + 复习，含 **165 个字的离线笔顺动画数据**
- 20 以内计算（凑十法/破十法/混合）+ 错题本
- 每日 5 句日常对话 + 每日 5 个单词
- 拼音对对碰、单词找朋友、找不同、数字排队等游戏

**整合方式**：
1. 参考 **yxj-workbench** 的课程组织方式——它是把 32 首诗、166 个字、20 以内计算**直接写在代码里**，不是等教案 JSON 接完
2. 你已经有 360 节教案 JSON，缺的是“把 JSON 渲染成可交互页面”的引擎
3. 参考 **kids-learning-cards** 的卡片渲染方式——输入数据 → 输出 HTML 卡片

**关键动作**：写一个“课时渲染器”，读取 `lessons.json` 中的每一节，根据 `type` 字段（识字/拼音/数学/英语/拼读/古诗）渲染成对应的可交互页面，而不是每节课手动写 HTML。

---

## 三、专注力训练：从 choice 引导 → 真实题库

**你现在**：3 节 choice 引导题，没有独立题库【总结文档】

**可用的开源资料库**：

| 项目 | 游戏类型 | 技术栈 | 直接可用性 |
|------|---------|--------|-----------|
| **brain-game** | 12 种认知训练：记忆/反应/计算/注意力/模式识别/数独等 | Vue 3 + Vite | ⭐⭐⭐⭐ 玩法设计可直接参考 |
| **muyu-focus** | 默数白噪音次数训练专注力 | 纯 HTML/CSS/JS | ⭐⭐⭐⭐⭐ 单 HTML，直接嵌入 |
| **freefocusgames** | 舒尔特方格、Stroop 测试、Dual N-Back | Next.js 15 | ⭐⭐⭐ 可参考舒尔特实现 |

**brain-game** 包含 12 种认知训练游戏，其中：
- **注意力训练**：在干扰物中找到特定目标，10 个关卡
- **记忆训练**：观察并记住图案序列，难度递增
- **数独谜题**：4×4 简化数独

**muyu-focus** 是单 HTML 文件，可以直接嵌入工作台作为“专注力训练”的一个关卡。

**整合方式**：
1. 从 **brain-game** 中提取“注意力训练”和“记忆训练”的玩法逻辑
2. 将 **muyu-focus** 作为“听觉专注”关卡
3. 你已有的舒尔特方格和 4×4 数独继续保留

---

## 四、每日运动：从 choice 引导 → 真实动作库

**你现在**：3 节 choice 引导，没有动作库【总结文档】

**可用的开源资料库**：

| 项目 | 内容 | 格式 | 直接可用性 |
|------|------|------|-----------|
| **StarKids** | 家务/习惯/学习/运动/社交/创意等任务分类 | 全栈 | ⭐⭐⭐⭐ 任务分类方式可参考 |
| **Kids-Reward-Chart-System** | 运动任务带“组数和休息间隔” | Docker + SQLite | ⭐⭐⭐ 运动任务数据结构可参考 |

**StarKids** 的任务分类包含“运动”类别，说明运动任务在儿童激励系统中是标准模块。

**整合方式**：
1. 建立 10-15 项居家运动库（开合跳、深蹲、原地跑、高抬腿、平板支撑等）
2. 每项运动配：名称、时长/次数、安全提示、示意图（SVG 或 Grok Imagine 生成）
3. 每日从库中抽取 3 项，组合成当日的运动任务
4. 复用现有的倒计时器

---

## 五、优先级建议

| 优先级 | 模块 | 动作 | 参考项目 | 预估工作量 |
|--------|------|------|---------|-----------|
| **P0** | 古诗 | 从 chinese-poetry 提取 75 首诗，接入现有课时 | chinese-poetry | 2-3 小时 |
| **P0** | 360 节教案 | 写“课时渲染器”，读取 JSON 生成页面 | yxj-workbench | 4-6 小时 |
| **P1** | 专注力 | 嵌入 muyu-focus，从 brain-game 提取注意力玩法 | muyu-focus / brain-game | 3-4 小时 |
| **P1** | 运动 | 建立 10-15 项运动库，每日抽取 3 项 | StarKids | 2-3 小时 |
| **P2** | 英语 | 用 kids-learning-cards 批量生成英语卡 | kids-learning-cards | 按需 |

---

**一句话总结**：你缺的不是资料，是“把资料变成可交互页面”的渲染引擎。参考 **yxj-workbench** 的课程组织方式，写一个通用的“课时渲染器”，360 节教案 JSON 就能一次性全部变成可玩内容。古诗从 **chinese-poetry** 取数据，专注力嵌入 **muyu-focus** 和 **brain-game** 的玩法，运动建立动作库。