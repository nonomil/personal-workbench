# 个人工作台 v0.7.0

> 状态：2026-08-16 网页已发布到 GitHub Pages。APK/MuMu 未按本版重验，发布口径仍 **PARTIAL**。

## 版本号

- npm / 页脚：`0.7.0`
- 上一正式记录：`0.6.0`（2026-08-14）
- 本版不另开 localStorage key；幼儿成长仍写既有快照，宠物新增 `growth.pet.speciesId`。

## 本版变化

### 幼儿工作台

- 识字体验升级：25 题识字量测评、错字本字卡/专项复习/A4 打印、档案曲线与证书、首页 KPI 与三大入口卡；9 张本地 RGBA 素材。
- 认单词优化：今日 3 词走听音选图 / 看图选词 / 配对 / 拼写；英语错词本分听/读/拼；词汇档案曲线；`wordboss` 局内金币小游戏（不进世界阳光账本）。
- 宠物蛋：花园 PVZ 蛋 + 我的世界工作台 7 只 MC 蛋；喂食仍扣 5 阳光；破壳后不能换蛋。
- 我的世界成长页改为「方块基地」：去方块世界，不再浇水/保卫战/植物图鉴；未解锁蛋标明关卡条件；花园蛋不串到方块巢。
- 设置页改成单列家长卡片：主题 → 声音 → 学习级别 → 口算 → 导出本周；云同步折到最下。
- 《什么是真正的工作台》W1–W3：成人首页去掉习惯打勾主路径，儿童今日主按钮去学习，设置可导出本周 Markdown。

### 游戏

- 方块传奇：近战可连续挥剑、词卡暴击、薄商店与放置、世界扩到 192、洞穴/矿/水/村、金色单词方块回收金币与 HP。
- 方块世界（voxel-craft）：地图与敌人表、气候天空与装饰素材继续本地化。
- 花园保卫：技能/投射物素材增量（未把未引用的 skills 包算作运行时依赖）。

### 仓库整理

- `.gitignore` 排除：`.playwright-mcp/`、根目录临时 PNG、误建目录 `--/`、`prj/games/_backup/`、生图 recon / img2threejs、未接入运行时的 `blocklegend-mobs-4view` 与 `preschool-pvz-skills`、识字 `_raw` 拼图。
- 任务包与方案仍放在 `docs/plans/`、`docs/01-方案/`，不把进行中包挪进 `old/`。
- 运行时入库：识字 published RGBA、方块传奇天空图、voxel-craft 地图/敌人素材、花园新增植物/投射物。

## 本地证据

- 识字定向：`tests/preschool-literacy-assess.test.mjs` 等 **18/18**。
- 认单词定向：english-word-uplift **30/30**（任务包记录）。
- 宠物与成长 chrome：`tests/preschool-pet.test.mjs` + `tests/world-games-growth.test.mjs` 本会话 **22/22**。
- 设置页合同：`preschool settings page stacks parent cards...` 绿。
- 方块传奇定向：任务包记录最高 **61/61**（含后续词卡指针锁）。
- 全仓 `npm test` 最近一次记录 **468/475**：7 项为花园旧断言 / 图标 `bomb` / 发布树 / creeper 素材合同等既有失败，见 `docs/plans/T20260816-literacy-uplift/test-report.md`。**不声称全绿。**

## 文件夹（本版口径）

| 路径 | 是否入库 | 说明 |
| --- | --- | --- |
| `prj/` | 是 | 运行时：三套工作台、CSS、资料、小游戏 |
| `prj/assets/generated/preschool-literacy-uplift/published/` | 是 | 识字首页/档案 RGBA |
| `docs/04-发布/` | 是 | 版本说明 |
| `docs/plans/` | 是 | 进行中任务包，不挪归档 |
| `.playwright-mcp/`、`--/`、`current-english.png` | 否 | 本地预览残留 |
| `prj/games/_backup/` | 否 | 回滚快照 |
| `prj/assets/generated/blocklegend-mobs-4view/` | 否 | 未接入运行时的四视图中间件 |
| `prj/assets/generated/preschool-pvz-skills/` | 否 | 未引用技能包 |
| `android/`、`dist/`、`*.apk` | 否 | 构建产物 |

## 边界

- 未打 APK，未做 MuMu 重验。
- 识字 / 认单词 / 宠物蛋 / 方块传奇均未标 accepted，待手玩。
- 平台主题入口仍有「马里奥工作台」旧名。
- 课结束弹层仍可能写「去喂星芒」。
