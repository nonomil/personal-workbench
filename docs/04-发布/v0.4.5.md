# 个人工作台 v0.4.5

> 状态：发布候选；远端 Pages、Actions APK 与 MuMu 安装验收必须以本版实际运行结果为准。

## 本版变化

- 幼儿首页任务区参考本地游戏化学习工作台案例，改为响应式任务墙：宽屏三列、761–1180px 两列、竖屏一列。
- 每项任务仍使用现有打卡、轻量练习、植物/僵尸反馈和奖励幂等链路；60 日自然拼读与暑假资料仍是资料库，不全量进入首页。
- 入口页脚、npm 包版本和发布文档统一为 `v0.4.5`。

## 已有本地证据

- `node --test tests/preschool-workbench-refresh.test.mjs`：44/44 通过。
- `npm test`：100/100 通过。
- `npm run android:prepare`：成功生成 `dist/` 网页制品并完成 Capacitor 资源同步前置准备。
- `npm run android:build`：未完成；Gradle 报 `SDK location not found`，本机没有可识别的 Android SDK。

## 远端验收记录

推送 `main` 后填写 GitHub Actions run、Pages URL 和 APK artifact；推送 `v0.4.5` 标签后，只有实际生成并下载 Debug APK 才能填写 Release 记录。正式签名包仍不属于本版交付。
