# 发布白名单

允许进入 Pages/APK 的路径：`data/preschool/`、`assets/generated/preschool/` 中已登记 manifest 的文件、运行所需 HTML/CSS/JS。禁止把 `docs/03-研究与参考/幼儿学习工作台研究/raw/`、截图、搜索结果、未审核下载和 API key 带入制品。

发布前运行项目现有 `scripts/assemble-pages-artifact.mjs` 和 `scripts/prepare-mobile.mjs`，检查制品中没有 docs、tmp、凭证、研究原文和外部受限素材。
