# 数据迁移计划

- 旧 schema v1-v5 读取时补默认值，不覆盖已有完成状态。
- 新增掌握度只放 `courseProgress.phonics` 或对应学科命名空间，不新增幼儿 localStorage 根键。
- 导入 JSON 前做 schema 校验；失败保留当前快照并提示原因。
- 云端快照严格递增 revision；冲突时先保留本地，显示恢复入口，不自动合并未知字段。
- 迁移测试覆盖无 `garden`、旧 `zombie`、空课程进度和已有奖励事件的快照。
