# 数据与奖励合同

## 1. 唯一事实来源

- 运行状态继续使用现有幼儿快照 `petbank_huchuliang_preschool_workbench_state_v1`。
- 课程完成继续使用 `courseProgress.completedLessonIds`；需要掌握度时只在其下增加 `courseProgress.phonics` 等命名空间。
- 阳光余额、累计阳光、花园成长点、豌豆能量分离；任何学科不得直接写 localStorage。
- 日期身份统一使用本地 `YYYY-MM-DD`，审计时间才使用 ISO 时间戳。

## 2. 活动事件

```json
{
  "activityId": "activity-2026-08-02-math-001",
  "profileId": "local-default",
  "subject": "math",
  "lessonId": "preschool-math-day-001",
  "status": "completed",
  "evidence": ["counted-objects", "selected-more"],
  "rewardEventId": "activity-2026-08-02-math-001:complete"
}
```

奖励服务只接受稳定 `rewardEventId`。首页、课程页、今日页从不同入口完成同一活动时，后到者返回 duplicate，不再次增加阳光。

## 3. 奖励口径

> 2026-08-15 对数修订：本表逐项对照 `prj/app.js`、`prj/child-growth.js`、`prj/preschool-garden.js`、`prj/games/shared/workbench-bridge.js` 实读改齐；数值以代码为准。对数表见 `docs/plans/T20260815-B1-docs-truth/test-report.md`。

| 行为 | 阳光 | 豌豆能量 | 备注 |
| --- | ---: | ---: | --- |
| 今日计划打卡完成 | +10 | +1 | 每个计划实例一次（`completionRewardId` 去重，`app.js`） |
| 成长任务完成 | +15 | +1 | `task:{id}` 去重 |
| 课程练习完成（lesson-finish） | +20 | +1 | `lesson:{id}` 去重 |
| 阅读记录 | +5 | +1 | `reading:{id}` 去重 |
| 当天首次有效行动 | +10 | 0 | 写入 `checkinDates`，`daily-checkin:{date}` 一天一次 |
| 连续打卡 1 天 / 7 天 | +10 / +30 | 0 | 3/14/30 天为造型奖励，不加阳光（`child-growth.js`） |
| 浇水 | 消耗 5 | 0 | 每天一次，失败不改状态 |
| 向日葵收集阳光 | +10 | 0 | 每天一次（`preschool-garden.js`） |
| 发射豌豆 / 冰豌豆 | 0 | 消耗 1 | 命中 1 点伤害；樱桃炸弹消耗 2 点、3 点范围伤害；坚果墙不消耗 |
| 家长线下奖励 | — | — | 裁决：未实现——当前 `claimReward` 直接扣阳光核销；家长确认兑换为 `T20260815-B3-reward-loop` 范围，见 `docs/00-总控/决策记录.md` |
| "3 项核心全部完成 +20"（旧合同行） | — | — | 裁决：未实现——运行时无日闭环结算事件；+20 是单课完成奖励 |

豌豆能量：每次有效发奖行动 +1，上限 9（`preschool-garden.js`）；消耗按植物 `energyCost`。

游戏侧（三世界，经 `prj/games/shared/workbench-bridge.js` 统一发放，不动 localStorage）：

- 游戏来源阳光**每日上限 80**（`DAILY_GAME_SUN_CAP`），`game:{gameId}:{eventKey}` 与 `game-sun:{date}:...` 双重去重，超上限拒绝并提示"明天再来"。
- 数值：今日游玩 +3、三界同日 +8、本周游玩达标 +15、本周三界日 +12；里程碑奖励另计（+20/+40 等，见 bridge 内 `MILESTONES`）。

## 4. 错误和离线

- 错题、复习和重新尝试不扣阳光。
- 写入失败时 UI 不显示“已完成”，并给出重试提示。
- 同步冲突保留本地快照，不自动覆盖；只有实际 pull/恢复成功后才显示已同步。
