# 识字资料生产与版权规范

字卡字段：`id/character/pinyin/theme/radical/words/image/source`。汉字和拼音由 HTML/JSON 渲染，生图不得生成汉字、拼音或 Logo。图片优先项目原创 SVG/位图；外部图片必须登记作者、许可证、URL、署名和是否修改。

发布前检查：字形/拼音人工校对、图片语义正确、按钮文字不溢出、黑白打印可读、`source.license` 非空。研究截图和商业字卡只能进入 `research/raw/`。
