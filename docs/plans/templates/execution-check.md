# Txxx — 执行前确认卡

> 作用：在第一次写代码前，或在更换数据集 / 扩大规模 / 明显扩张改动范围前，确认“这次具体怎么走”。

## 当前任务

- 任务ID：`Txxx`
- 当前阶段：`execute`
- 对应方案：

## 本次执行范围

- 计划修改文件：
- 明确不碰的文件：

## 输入 / 数据 / 环境

- 使用的数据 / 输入：
- 运行环境：
- 是否存在高成本动作：
  - [ ] 更换数据集
  - [ ] 更换方法路线
  - [ ] 从子集扩大到全量
  - [ ] 大批量运行
  - [ ] 改动 > 3 个文件或 > 100 行

## 方案落实追踪

- 已批准方案中的关键点：
- 本次如何落实到执行步骤：
- 可能遗漏的步骤：

## 粒度自检（来自 task-decomposition.md）

- [ ] 本次改动 ≤3 个文件（或已在 task-decomposition 中记录例外原因）
- [ ] 验证命令跑完 ≤10 分钟
- [ ] 做错了能整块回滚
- [ ] 没有未经授权的”顺手优化”
- [ ] 禁止项已声明且 Agent 已知晓

## 需求分解核对

- 对照 `requirements-checklist.md`：
  - [ ] 本次执行范围对应的需求点已明确
  - [ ] 没有”只聊过、没落计划”的需求点
  - [ ] 输入 / 数据 / 约束与需求分解表一致
  - [ ] 暂不覆盖的需求点已备注原因

## 验证与证据

- 自动验证命令：
- 手动检查点：
- 证据落点：

## Git 确认卡

- 本次纳入 git 的文件 / 目录：
- 新建目录是否纳入 git：
- 数据 / 日志 / 缓存 / 生成产物是否继续排除：
- 是否需要拆成两个 commit：

> 建议：优先 `git add [显式文件/目录]`，暂不使用 `git add -A`

## Worktree 隔离确认（默认开启）

> 借鉴 Harness 工程实践：多个 Agent 并行写入同一仓库会互相踩踏。默认隔离，除非明确豁免。

- 本次子任务有文件写入 → 必须用 worktree
- 以下情况可豁免：
  - [ ] 纯只读调研（不写入任何文件）
  - [ ] 单文件小修复且验证 < 2 分钟且无并行子任务
- 豁免原因：___

## 相关 Spec 文件（借鉴 Trellis 选择性注入）

> Agent 开始执行前只读这几个 spec，不全量注入。

- 本次需要的 spec：
  - `docs/spec/___`
  - `docs/spec/___`

## 未决项

- `[?]` 1：
- `[?]` 2：

## 任务分解校验结果（自动回写）

<!-- AUTO:DECOMPOSITION-CHECK START -->
- 状态：`pending`
- 说明：等待 `write_gate.py` 自动回写分解校验结果
<!-- AUTO:DECOMPOSITION-CHECK END -->

## 人工确认

确认以下：

- [ ] 这次改动范围可接受
- [ ] 输入 / 数据选对了
- [ ] 方案关键点已经落实到执行路径
- [ ] `requirements-checklist.md` 中本阶段相关需求点都已覆盖
- [ ] 验证方法可接受
- [ ] 高成本动作已知且可接受
- [ ] 可以开始执行

## 确认结果

- 状态：`pending / approved / rejected / revised`
- 确认时间：
- 确认人：
- 备注：
