# Txxx — 阶段收口卡

> 作用：在子阶段结束后、准备扩大投入前，确认“当前结果是否支持继续”。

## 当前阶段

- 任务ID：`Txxx`
- 子阶段名称：
- 当前规模：`小样本 / 子集 / 全量前`

## 本阶段目标

- 原目标：
- 本阶段验证了什么：

## 当前结果

- 关键结果 1：
- 关键结果 2：
- 与原预期相比：

## 方向判断

- 当前结果是否支持原方向继续：
- 是否出现明显偏差：
- 是否需要调整方案 / 数据 / 方法：

## 需求分解核对

- 对照 `requirements-checklist.md`：
  - [ ] 本阶段应该覆盖的需求点已有结果或证据
  - [ ] 没有悄悄偏离原需求点
  - [ ] 如需调整方向，相关需求点会同步回写

## 下一步动作候选

- 继续扩大规模：
- 调整后继续：
- 停止并重新讨论：

## 若下一步是高成本动作，再补这两张卡

### Git 确认卡

- 下一个动作会纳入 git 的文件 / 目录：
- 新建目录是否纳入 git：
- 数据 / 日志 / 缓存 / 生成产物是否继续排除：
- 是否需要拆成两个 commit：

> 建议：优先 `git add [显式文件/目录]`，暂不使用 `git add -A`

### Worktree 判断卡

- 下一步是否需要独立 build / run / test：
- 下一步是否会跨多个目录持续改动：
- 主工作区是否还有别的未完成任务可能互相干扰：
- 是否存在大数据 / 大日志 / 权重 / 缓存，应继续共享而不是复制：

## 任务分解校验结果（自动回写）

<!-- AUTO:DECOMPOSITION-CHECK START -->
- 状态：`pending`
- 说明：等待 `write_gate.py` 自动回写分解校验结果
<!-- AUTO:DECOMPOSITION-CHECK END -->

## 人工确认

请选择一个结果：

- [ ] ✅ 继续 — 当前结果支持进入下一阶段
- [ ] ⚠️ 调整后继续 — 方向仍成立，但要先改这些点：
- [ ] ❌ 停止 — 当前方向有问题，回到方案讨论

如果选择了“调整后继续”或“停止”，先补：

- `change-request.md`

并至少同步：

- `requirements-checklist.md`
- `steps.md`
- `acceptance.md`
- `test-plan.md`
- `test-report.md`

## 确认结果

- 状态：`pending / continue / adjust / stop`
- 确认时间：
- 确认人：
- 备注：
