# Txxx — 任务名称

> 优先级：Px | 状态：pending | 前置任务：无
> 完成日期：YYYY-MM-DD
> 简要描述任务目的

## 目标

（一句话说明本任务要达成什么）

## Steps

> **验证约定**（借鉴 Harness 工程实践）：所有验证命令以退出码判定结果。
> - `exit 0` = 通过，非零 = 失败。Agent 依据退出码自动判定，不解析输出文本。
> - 如命令本身不返回退出码，用 `&& echo PASS || echo FAIL` 包装。
> - 回滚成本标注让 Agent 在失败时快速决策：继续修 / 整块回退。

### 1. 子步骤标题
- [ ] 具体操作 1
- [ ] 具体操作 2
- **验证：** `{command}` — 退出码 0，≈ {时间}，预期：{结果}
- **回滚成本：** {描述}

### 2. 子步骤标题
- [ ] 具体操作 1
- [ ] 具体操作 2
- **验证：** `{command}` — 退出码 0，≈ {时间}，预期：{结果}
- **回滚成本：** {描述}

## Acceptance

- [ ] 验收条件 1
- [ ] 验收条件 2

## 实际耗时

（完成后填写）
