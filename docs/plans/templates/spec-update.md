# Txxx — 规范回写卡

> 作用：任务验收通过后，把这次真正稳定下来的约束、禁忌和推荐做法回写到 `docs/spec/`。
> 这是轻量版 golden 机制，不要求每次都新增 spec，但要求显式判断“这次有没有值得沉淀的规范”。

## 回写触发判断

- 是否需要回写项目规范：`pending / yes / no`
- 回写结论：`pending / updated / not_needed / blocked`
- 对应 spec 文件：

## 本次沉淀点

| 类型 | 内容 | 写入位置 | 处理方式 |
|------|------|---------|---------|
| 新约束 |  | `docs/spec/...` | 新增 / 修订 |
| 禁止项 |  | `docs/spec/...` | 新增 / 修订 |
| 推荐做法 |  | `docs/spec/...` | 新增 / 修订 |

## 黄金样板候选

- 是否提名黄金样板：`pending / yes / no`
- 入池结论：`pending / nominated / not_needed`

| 候选类型 | 候选路径 | 适用场景 | 为什么值得复用 |
|------|---------|---------|---------|
| 需求分解 / 测试报告 / 截图证据 / 验收样例 / 规范回写 |  |  |  |

## 执行动作

- [ ] 已判断这次发现是否应该进入 `docs/spec/`，而不只停留在交接记录
- [ ] 如需回写，目标 spec 文件已更新
- [ ] 如需回写，`docs/spec/index.md` 已同步
- [ ] 如提名黄金样板，`docs/spec/golden-candidates.md` 已登记
- [ ] `session-journal.md` 已记录本次规范回写结果

## 备注

- 不回写的原因：
- 后续仍需补充的规范：
