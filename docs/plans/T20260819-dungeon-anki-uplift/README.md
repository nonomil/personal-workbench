# T20260819-dungeon-anki-uplift：地下城式关卡循环 + 单词记忆状态

> 状态：**review**（D1–D4 代码已绿。A1 隔离新档已核。独立仓未推。不标 accepted）。
> 源头：`docs/01-方案/工作台小游戏设计/05-方块传奇/07-地下城式关卡与ANKI复习优化.md`（调研与定位分析）。
> 核心一句话：**把 ANKI 的到期队列做成地下城的重玩理由**——每词一个记忆状态（Leitner 5 盒）驱动出词，词力数值驱动难度档推荐，重玩老关就是消复习债。

## 与已落地包的关系（重要，先读）

- **E2 复习之门已绿**（`T20260819-E2-review-gate/`）：关卡级七轮课表、今日冒险、`hardWords`、听说统计都已在代码里。本包**不推倒重来**：
  - D1 的 `wordMemory` 是**词级**状态，与 E2 的**关级**课表互补：门什么时候开归 E2 课表管，门里/波次里**出哪些词**归 D1 到期队列管；
  - `hardWords` 维护逻辑并入 D1 的 `recordAnswer`（同一函数写两处，规则不变），避免两套词账本；
  - E2 的出题池组装函数在 D1-S4 接入 due 词，是本包对 E2 的唯一改动点。
- **W1 工作台 mastery** 是另一条轨（workbench 课程域），本包不动它；跨轨对齐记门控债。

## 文档导航

| 文件 | 用途 |
| --- | --- |
| [`task.md`](./task.md) | 目标、输入基线、D1–D4 子任务表、设计参数、边界 |
| [`requirements-checklist.md`](./requirements-checklist.md) | 需求裁决（采纳/拒绝及依据） |
| [`steps.md`](./steps.md) | 执行步骤（先红后绿、假时钟、每步验证命令） |
| [`acceptance.md`](./acceptance.md) | 手玩验收清单 A1–A14 |
| [`test-plan.md`](./test-plan.md) | 自动 + 浏览器测试 |
| [`test-report.md`](./test-report.md) | 执行后回填 |

## 分期与门控

| 期 | 内容 | 优先级 | 门控 |
| --- | --- | --- | --- |
| D1 | 词级记忆状态（Leitner 5 盒）+ 词力 + 到期出词 | P0 | 无（纯数据+测试，独立可验） |
| D2 | 重玩三档难度（默认/冒险/天启）+ 词力推荐 + Boss 关解锁门槛 | P0 | 依赖 D1（词力、到期队列） |
| D3 | 词卷轴 → 隐藏关 + 营地 Hub | P1 | 卷轴条件依赖 D1 盒号；建议 D2 后做 |
| D4 | 难词降级支架 + 学习密度结算行 | P1 | 依赖 D1；可与 D2 并行 |

建议执行顺序：**D1 → D2 ∥ D4 → D3**。每期结束跑全量 `node --test tests/blocklegend.test.mjs` 并同步推独立仓。

## 刻意不做

完整 FSRS、25 级天启、程序生成房间池、换装/宠物等装饰系统、第 13 关、新词库、新录音频、多人。依据见 07 文档 §2 的 Prodigy 反面教训与 00-README 口径。
