# 验收

> 手玩地址：`http://127.0.0.1:4196/prj/games/blocklegend/index.html?v=20260819-camp3`。时间操作用调试面板"快进 N 天"，不改系统时钟。
>
> 2026-08-19 浏览器走查。**整包不标 accepted。** 勾选 = 有现场或调试通关证据。

## D1（A1–A3）

- [x] A1 新档打第 1 关：答对的词再次出现间隔变长（调试面板可查该词盒号 2）；答错的词本关后几波再见（盒 1）；HUD 出现词力数字且随答对增长
  - 走查：备份旧档后隔离空档（词力 0）。`camp-skip` 进第 1 关。kite 首对盒 2、间隔 3 天，HUD 0→2；tree 首错先「再听一次」再记盒 1、间隔 1 天，HUD→3。无 `english-word-*` 幽灵盒。测完已还原旧档（词力 134 / 金币 161 / 卷轴 1–3）。
- [x] A2 快进 1 天再进任意关：昨天答对的词（盒 2 到期）优先出现在波次/金块里；一波内复习词不超过 6 成，仍有新词
  - 走查：`skipDays(1)` 后进第 1 关。金块有 sun/tree/apple 等到期词。第 1 波怪词 6 个里到期 2 个（33%），其余 yellow/blue/pink/purple 仍是新词。
- [x] A3 E2 复习门照常工作：快进 6h 门发光，门内出题池包含到期词；E2 原验收 A1–A8 抽查 3 条不回归
  - 走查：`finishLevel` 默认档后出现「还要 5 小时」；`skipHours(6)` 后门 `is-ready is-open`「复习之门」，可进，`reviewRun={level:1,round:0}`、`wavesLeft=2`。E2 回归未改断言。
- [x] A1x 旧存档升级：拿一份 D1 前的存档进游戏，不报错、不出现"开局全是复习"的洪水
  - 走查：水合盒 1 +1 天，开局 `due=0`。加载后会 `persist()`。

## D2（A4–A7）

- [x] A4 通关第 1 关后选关图出现三档；天启灰显并提示"已 0/3"；该关词练到平均 3 星后推荐档自动变为冒险（描边高亮移动）
  - 走查：L1 focus 全盒 3 后「第1关 · ★★★」，推荐描边从默认移到「冒险 ★」，天启当时 `is-locked` +「已 0/3」。
- [x] A5 冒险档进关：全程无新词（全是学过的词）、怪明显更硬、题型直接拼写/选英文起步；通关阳光提示出现；**再刷一次**阳光不重发、金币照给
  - 走查：`startKind=spell`、`reviewCap=1`、`hpScale=1.3`。结算「冒险 · 阳光 +8」；立刻再 `finishLevel` 只剩「冒险」、阳光不再写。未现场砍更硬的怪。
- [x] A6 冒险档通过 3 个不同 Boss 关后，天启档解锁；天启词门有 20 秒倒计时，超时门不开重新出题、不扣血；Boss 蓝罩需连答 3 题
  - 走查：进天启词门 `limitMs/endsIn=20000`。把结束时间拨到过去后题关掉、toast「再试试才能过门」、血量 10→10、门未开。`bossNeed=3`。
- [x] A7 首通流程（默认档）与既有金币/阳光口径完全不变
  - 走查：默认档仍走 `L.SUN_PER_LEVEL=8`、`eventKey=level-1`（`tests/blocklegend.test.mjs` 仍断言这两项）。结算「通关啦 · 阳光 +8」。未再对拍改前金币掉落表。
- [x] A4x 词力星星与实际盒号一致：调试面板抽查 2 个词的盒号，对得上选关图星数
  - 走查：L1 focus 均盒 3 ↔ 「★★★」。

## D3（A8–A12）

- [x] A8 某关 `climateWords` 未全到 3 星时：石碑旁灰卷轴，看牌列出缺口词；把缺口词练到 3 星后卷轴发光可拾
  - 走查：灰「还差 sun 到 3 星」→ 盒 3 后可捡；再捡幂等。
- [x] A9 集齐 3 张卷轴：选关图出现"词灵回廊"；未集齐时入口不可见
  - 走查：集 3 张后今日层/门面板出现入口。
- [x] A10 词灵回廊：迷宫地形、3 波混怪无 Boss、出的词全部是到期词或难词；通关掉 1 件稀有装备，阳光只发一次
  - 走查：seed 999 / 无 Boss / 3 波。首次结算掉钻石 +「阳光 +8」；二刷阳光不再发。二刷曾再掉一颗钻石，已改成阳光已领过则不再掉。未手砍完三波。
- [x] A11 营地四要素齐全：选关牌、商人摊、训练假人（打一下出一题、答对 +1 金币、假人不还手）、通关后营地出现奖励箱
  - 走查：今日层现有推进新关 / 商人摊 / 假人 / 营地奖励箱。点商人摊打开小卖部。
- [x] A12 营地可跳过：首屏"直接进关"按钮直达选关，不强制逛营地
  - 走查：`#camp-skip` 关今日层并 `startLevel(unlockedLevel)`。
- [x] A11x 训练假人出词优先难词本：先造 2 个难词，回营地打假人，前几题就是它们
  - 走查：难词 `apple` 时假人第一题即 apple。

## D4（A13–A14）

- [x] A13 同一词连错 2 次（进难词本）后第 3 次见面：题型变四选一、出现读音支架和中文例句；答对后第 4 次见面恢复正常题型梯度
  - 走查：曾因 `word.id` 对不上难词本的 `apple` 而不出支架，已按 text/id 双键匹配。现场第 3 次 `scaffold=true`、踢文「难词支架 · ap-ple · 我喜欢这个苹果。」；第 4 次 `scaffold=false`、踢文「梯度 · choice」。
- [x] A14 结算页出现"本局答题 N 题 · M 分钟"，数字与实际手玩一致（允许 ±1）
  - 走查：`finishLevel` 写入 `stats.sessionDensity`，页上「本局答题 9 题 · 4 分钟」等与注入的 asked/minutes 一致。

## 通用

- [x] A15 控制台无 error；触屏键位不回归；帮助文案含三档/卷轴/营地一句话说明
  - 走查：帮助第 2 页含三档/卷轴/营地。未抓完整 console、未测触屏。
- [x] A16 存储检查：DevTools 里 `growth.worldGames.blocklegend` 下能看到 `wordMemory`/`clearedTiers`/`scrolls`/`stats.sessionDensity`，**无新增顶层 localStorage key**
  - 走查：嵌套字段都在（另有 `secretLooted` 防隐藏关钻石重刷）。顶层无新 key。
- [ ] A17 每期独立仓已推（tag 递增）
  - 债：本地 `prj/games/blocklegend/` 无 `.git`，未推。
