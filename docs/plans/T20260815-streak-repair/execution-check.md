# Execution Check

- [x] 动 growth 字段 → 已列 data-model 同步(R5)
- [x] 不新增 localStorage key;streakRepair 嵌在 growth 内随快照导入导出
- [x] 补签不发阳光 → 不进 awardedIds/daily-checkin 结算路径,无重复结算风险
- [x] 缓存戳改动同步测试(先 grep 三处断言)
- [x] 工作区未提交改动多 → 只动本包清单文件,不 commit
