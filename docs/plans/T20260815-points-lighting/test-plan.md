# test-plan

## 自动化（npm test）

### S1

| 组 | 文件 | 断言 |
|---|---|---|
| 兑换合同（新增） | `tests/preschool-rewards-claim.test.mjs` | 余额不足抛错且不扣款；成功兑换扣 cost 且写入 `claimedRewardIds`；重复兑换抛错（幂等）；兑换后 `totalSunlightEarned` 不变 |
| 旁路锁定 | `tests/preschool-garden.test.mjs`（扩展） | 向日葵技能 +10 与防守 tick +10 的记账行为 = 裁决结果（默认：只加 `sunlight`） |
| 文案合同 | 既有断言中文文案的测试（侦查后列清单） | "打卡"→"点亮"叙事更新，先红后绿，红数 = 清单数 |
| 回归 | 全部既有测试 | 零删除、全绿 |

### S2（门控启用后）

| 组 | 文件 | 断言 |
|---|---|---|
| 强度派生 | `tests/child-growth.test.mjs`（扩展）或新文件 | `dailyIntensity`：0/1/2/3+ 分档；跨月边界；补签日 ≥1 档且无强度加成；不可解析日期事件不计 |
| 收集箱合同 | `tests/preschool-achievements.test.mjs`（扩展） | 渲染含"世界勋章"分组；既有 19 枚判定断言零改动 |

## 人工（真机浏览器）

1. S1：打开幼儿工作台，走查日历/首页/花园/连续奖励卡/徽章描述，确认零"打卡"，截图归档。
2. S1：兑换一次奖励，确认余额扣减、按钮禁用、无家长确认（现状）。
3. S2：日历热力格四档着色肉眼可辨；有补签记录的账号补签日点亮；完美日星标。
4. S2：收集箱两组徽章、灰占位正常。

## 判定

- 每阶段：命令 + 退出码 + 结论（允许推进/否）写入 test-report。
- 最终：`npm test` 退出码 0 + 人工走查全过 + checklist 证据齐。
