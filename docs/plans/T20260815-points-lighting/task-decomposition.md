# task-decomposition：S1 / S2

## S1：口径与账本（P0，先执行）

| 子项 | 内容 | 对应方案 |
|---|---|---|
| S1-a | claimReward 合同测试（锁现状，特征测试直接绿） | P0-3 |
| S1-b | 向日葵旁路记账裁决：默认方向 A（保持不进总账 + data-model 文档化 + 锁定测试）；侦查证据支持方向 B 时请示 | P0-2 |
| S1-c | "打卡"→"点亮"文案统一（先列清单，测试先红后绿） | P0-1 |
| S1-d | `docs/data-model.md` 增"点亮日 vs 完美日"口径节 | P0-1 |

依赖：S1-a 最先（独立且是 B3 前置保险）；S1-b 与 S1-c 无依赖可并行；S1-d 在 S1-b/S1-c 裁决落定后写。

## S2：可见性（P1，门控：S1 验收 + 用户确认）

| 子项 | 内容 | 对应方案 |
|---|---|---|
| S2-a | `child-growth.js` 新增纯函数 `dailyIntensity(growth, month)`：按日聚合 `awardedIds` 可解析日期的事件数，分 0/1/2/3+ 四档；补签日至少 1 档但无强度加成 | P1-1 |
| S2-b | `#calendar` 日历渲染热力格 + 完美日星标 | P1-1 |
| S2-c | 收集箱增加"世界勋章"分组（只读 `worldGames.meta.badges` 渲染，不合并判定、不迁移存储）；家长报告徽章墙同步 | P1-3 |

依赖：S2-a → S2-b；S2-c 独立。

## 不在本包

- P1-2 "还差 X"提示、P1-4 学习专区余额 → `T20260815-points-hints`（等 B2/B3 排序）
- 家长确认兑换 → `T20260815-B3-reward-loop` S1
- P2 池（假期模式等）→ 未成包
