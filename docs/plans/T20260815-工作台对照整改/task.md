# task：对照文章整改三套工作台

- 级别：L2
- 优先级：P0
- 状态：pending（先确认范围，再写红合同）

## 1. 目标

让工作台看起来像在帮人做事，而不是让人来给工作台打勾。

1. **W1 成人**：首页和工作区不再以习惯打勾为产品。真实入口是生活记录、阅读、任务、复盘；完成这些才留下年度痕迹。
2. **W2 儿童**：今日页主路径是去学习/去练习，不是勾「完成得 10 阳光」。
3. **W3 导出**：家长/自己能把工作台里已经有的真实数据一键导出成周总结素材。

## 2. 只改

`prj/app.js`、`prj/config.js`、`prj/css/adult.css`、`prj/css/child.css`（若只需改文案可不动 css）、对应测试。幼儿设置页导出只动 `app.js` 里已有设置页渲染。

## 3. 不碰

`prj/games/**`、`workbench-bridge.js`、激励发放函数、三套 storage 主 key。

## 4. 启动程序

1. 用户确认：W1 是「砍掉习惯勾选」还是「勾选降级为次要、不进统计」。默认按 steps.md：**勾选从首页移除，生活页保留为备忘且不进 `yearActivityDates`**。
2. 先写红合同再改 `app.js`。
3. 改完填 test-report，不 commit。
