# T20260815-B3 - 任务定义卡

> 模式：L2（四个独立可交付的功能面，串行）
> 执行策略：CLOSED 串行。S1 → S2 → S3 → S4，每个 S 单独验收可停。不开 fleet。

## 0. 领域画像

- 主画像：软件开发（参考 `docs/plans/templates/profiles/software-dev.md`）
- 次画像：儿童产品激励设计（防刷分、家长参与、无惩罚）

## 1. Loop 类型判断

- [x] CLOSED 单 Agent：四件事都有明确验收和测试路径
- [ ] CLOSED Fleet
- [ ] OPEN 转 CLOSED

## 2. 动机自检

- [x] 加速已理解的执行：外部笔记已裁决（03 借鉴清单 13 条），四件套是"采纳"项的全部
- [x] 不是用流程回避理解：商城/伙伴/英语引擎均已存在，本包是加环节不是造系统

## 3. 目标与背景

- 一句话目标：阳光有出口、游戏有门槛、伙伴有直连、英语有日定量——孩子每天有理由学完再玩。
- 背景：`2026-08-15-学习内容体系优化/04-学习功能优化方案.md` P1 全节。
- 历史约束（红线）：阳光唯一账本、`awardSunlight` 去重 + 日上限不放松；星芒唯一伙伴主角；无外部 IP；无惩罚性负反馈；Vanilla JS 纯本地。
- 兼容要求：`claimedRewardIds` 既有语义保持（已核销含义不变，pending 用新字段/新态表达）；老快照 migrate。

## 4. 为什么是 L2 而不是 L1

四个功能面各自跨 UI+storage/数据，S1 有 storage 字段评审门控，S4 有内容生产（80 词自写例句）与版权红线；但互相独立、可分批交付，不需要 L3 loop。

## 5. 子任务

见 `task-decomposition.md`。S1–S4 代码已绿。浏览器走查待做。

## 6. 边界

**S1 只改：** `prj/app.js`（商城渲染与兑换流）、`prj/storage.js`（pending 态 + migrate）、`docs/data-model.md`、新增 `tests/preschool-rewards-confirm.test.mjs`

**S2 只改：** `prj/app.js`（今日卡两档渲染）、`prj/css/preschool/34-course-wall.css`

**S3 只改：** `prj/app.js`（完成弹层、连击徽记、防刷分提示）、`prj/preschool-pet.js`（喂养入口复用）

**S4 只改：** `prj/preschool-english-data.js`（词库 30→80）、`prj/app.js`（今日 3 词 + 词库分色）、`docs/02-课程/英语/`（词表口径入文档）、相关英语测试

**不碰：** `workbench-bridge.js` 协议、三世界游戏逻辑、奖励数值公式、徽章判定、卡片墙整体布局。

**明确不做：** 完整家长后台（只做确认环节）、第二货币/券、外部 IP 皮肤、惩罚机制、磨耳朵/阅读计划页（观察项）、AI 排任务。

**禁止顺手优化：** 商城 UI 改版、宠物系统扩展、英语引擎重构、把三档做成外部笔记的完整"冒险系统"。

## 7. 验收（整体）

- [ ] S1：pending→确认→核销链路测试绿 + 真机走通 + data-model 同步
- [x] S2：必做未完冒险置灰/完成点亮（代码绿，真机待验）
- [x] S3：三处 UI 可见且无新 storage 字段（连击为派生值；代码绿，真机待验）
- [x] S4：80 词全部自写例句可溯源；今日 3 词 + 分色可见；英语测试绿（代码绿，真机待验）
- [ ] 全程 `npm test` 绿；红线零违反

## 8. 升级触发

- S1 若 `claimedRewardIds` 语义必须变 → 停，走变更与同步规则评审
- S2 若"必做全完成"判定需要 B2 未交付的数据 → 停，回 B2 补验收
- S4 若词库扩容发现现有 30 词有版权来源问题 → 停，记 test-report，问用户
- 任何 S 想加"孩子会哭闹"的强制门槛（如不学锁全站）→ 禁止，这是激励不是惩罚
