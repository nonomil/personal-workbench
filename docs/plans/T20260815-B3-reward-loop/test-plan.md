# Test Plan — T20260815-B3

> 验证以退出码为准；每个 S 交付前跑全量回归。

## 阶段 0：S1 侦查与字段评审

| 项 | 动作 | 预期 |
|---|---|---|
| 现有兑换流 | 读 app.js / storage.js / config.js | 流程图 + `claimedRewardIds` 写入时机明确 |
| 字段方案 | 方案 A/B 择一登记 | 不动既有字段语义，或停下问用户 |

## 阶段 1：S1 合同先红后绿

| 项 | 命令 | 预期 |
|---|---|---|
| 五条断言先红 | `node --test tests/preschool-rewards-confirm.test.mjs` | 非 0 |
| 实现后 | 同上 | 退出码 0 |

## 阶段 2：S1 浏览器证据

- 发起 → 刷新 → pending 保留 → 长按确认 → 阳光扣减正确 → 已兑换态
- 取消路径；阳光不足路径；控制台无错

## 阶段 3：S2 两档任务

- 必做未完：冒险置灰 + 提示（截图）
- 必做全完成：冒险点亮 → 进游戏 → bridge 奖励规则未变（对照阳光流水）

## 阶段 4：S3 三件小改

- 完成课 → 弹层"去喂星芒"→ 喂养成功
- 今日卡连击徽记数值 = `checkinDates` 手算值
- 重开已完成课 → 防刷分提示出现；`git diff` 确认无新 storage 字段

## 阶段 5：S4 英语日定量

| 项 | 命令/动作 | 预期 |
|---|---|---|
| 词表溯源 | review 溯源表（词/来源/例句） | 80 词齐全，无抄录 |
| 引擎测试 | `node --test tests/preschool-english*.test.mjs` | 退出码 0 |
| UI | 浏览器：今日 3 词 / 词库分色 / 老进度保留 | 三项可见 |

## 阶段 6：回归（每个 S 交付前都跑）

| 项 | 命令 | 预期 |
|---|---|---|
| 全量 | `npm test` | 退出码 0 |
| 字段面 | diff `docs/data-model.md` | 仅 S1 字段登记 |

## 不测什么

- 三世界游戏内部玩法；徽章判定；卡片墙整体布局（B1 已守护）
