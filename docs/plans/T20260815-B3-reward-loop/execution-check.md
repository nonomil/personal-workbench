# execution-check — T20260815-B3（第一次写代码前过一遍）

## 前置门控（阻塞项）

- [ ] `T20260815-B2` 状态为已验收（六项全可练是 S2 的判定基础）——未完成则本包保持 blocked
- [ ] `npm test` 退出码 0，记录基线
- [ ] 已读 `docs/00-总控/当前状态.md`、`docs/00-总控/变更与同步规则.md`、`docs/data-model.md`

## 工作区处置（阻塞项）

- [ ] `git status` 核对与本包文件域重叠情况；**特别核对英语词库文件**（`prj/preschool-english-data.js` 曾有在途扩容改动）
- [ ] 核对 `T20260815-streak-repair` 包状态：若已落地，S3 连击计算必须调用其派生口径（含 `repairedDates` 补签日）；若仍 in-progress 且撞文件 → 先协调完成顺序
- [ ] 重叠 → 问用户先提交还是混行；未确认不写代码

## 红线自查（每阶段重复）

- [ ] 无第二货币；无外部 IP；无惩罚机制；`awardSunlight` 数值规则不动
- [ ] 新增 storage 字段只在 S1，且方案先写进 test-report 再实现
- [ ] S4 词条例句全部自写，公版词表来源可溯，不从 NC/ND 参考仓抄

## 范围自查

- [ ] steps.md 当前只有 S1 是可执行态；不跨 S 混做
- [ ] 家长确认用长按方案，不引入密码系统（用户另要求再说）

## 回滚预案

- 各 S 文件可 `git checkout -- <file>` 还原
- S1 storage 字段回滚：删字段 + migrate 容忍缺失（设计时保证）
- 合同测试先红是闸门

## 放行结论

- [ ] 放行 S1 / 不放行，原因：
