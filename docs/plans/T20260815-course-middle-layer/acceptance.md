# acceptance：算不算过

## Node 层

1. `npm test` 全绿（退出码 0），既有用例零删除。
2. `node --check prj/app.js` 通过。
3. `git diff` 改动面 ⊆ `.meta.yaml` files 列表（+侦查补录的缓存戳测试）。

## 浏览器层（390px 竖屏 + 桌面各一遍）

1. 卡片墙 9 张卡都有今日预览行；翻卡科显示张数、古诗显示今日诗题、专注显示今日训练；做完某科后返回，该卡显示"今天做完啦 ✓"。
2. 翻完卡点"更多练习"：看到菜单（回今日页 / 今日课 / 听一听 / 看资料 / 家长看详情），不是 8 层大卡；孩子层按钮 ≤ 5 个且每个 ≥ 44px。
3. "家长看详情"默认折叠；展开后原完整大卡内容齐全（进度条、分级带、掌握统计、路线）；展开/收起无横向溢出。
4. 全链路无死路：卡片墙 → 今日学习页 → 更多练习菜单 → 进课程 → 返回卡片墙。
5. 暑假页底部按钮为"更多资料"，落点同菜单。

## 一票否决

- 出现新 localStorage key 或 `dailyPlans`/`courseProgress` 结构改动
- 今日学习页（翻卡/古诗/运动/专注/暑假）内容或交互被改动（除三处按钮文案）
- 墙卡渲染污染 `ui.courseCards`（打开卡片墙后古诗今日页行为异常即为此症状）
- 新造了第二套"今天该学什么"推导逻辑
- `renderPreschoolCourseCard` 内部被拆改
