# T20260814 — 测试报告

> 执行后填写。不要空喊通过：每阶段写结论 + 命令 + 退出码。

## 阶段 0 — 基线（已执行）

- 工作区处置：**混行不提交**。在途改动（学前数据扩容、voxel 艺术资源、levels/banks、预设教案等大量文件）不属于本任务包，用户未授权提交；本任务只推进自己的工作区改动与文档，避免与在途改动冲突。
- `npm test` 基线：会话开始时 202 项全绿（改动前记录）。
- 结论：允许推进（工作区脏但可控，S1 边界为游戏文案与公开措辞，不与在途数据改动相交）。

## 阶段 1 — voxel 运行时行为结论（已执行）

- `quest-title` 运行时来源：`prj/games/voxel-adventure/game.js` 运行时覆写为「方块世界 · 挖一挖、放一放」（原静态占位）。静态 HTML 中的「横版过关」字样仅为过期占位，非运行时机制名。
- `message-tip` 运行时来源：静态提示「点击挖矿 | 右键/长按放置 | 走到出口」，与 game.js 实际输入（挖/放/出口）一致，保留。
- 「关卡」区渲染来源：`renderQuests()` 用 `levelsApi.list` 渲染关卡卡，h2「关卡」标签准确，保留。
- 跳跃输入是否存在：存在。game.js 监听 Space/W 跳跃，平台跳跃是真实机制，不能隐藏。
- 判定：**S1 口径收窄** —— 只摘「横版过关」这个类型标签（静态占位 2 处），保留挖/放/出口/跳跃等真实功能提示。设计文档中「走到出口」混行机制（关卡 + 任务目标 + 出口兜底）已在场，属在途设计，不做改动。
- 结论：允许推进。

## 阶段 2 — S1 主链路（已执行）

- 合同测试先红：退出码 1（voxel /横版过关/ 与 launcher 商标词断言失败，共 2 fail，预期非 0）。
- voxel 改口后：`node --test tests/world-games.test.mjs` 退出码 0，3/3 绿。
- 商标零匹配：`grep -E "我的世界|Minecraft|马里奥|Mario|植物大战僵尸|Plants vs" prj/index.html` 退出码 1（零匹配即通过）。voxel 站内 game.js/data 注释仍引用「我的世界」作为设计来源出处（非公开文案），保留并已在文档注明。
- 浏览器 voxel 页：未做真机验证（静态前端，S1 为纯文案改动，运行时可点击路径未变）；改动经合同测试与全量回归覆盖。
- 浏览器启动器：同上，静态措辞改动。
- 文档修订 diff：`docs/学习项目设计/2026-08-14-学习项目设计-落地分析.md`（笔顺「已实现」+ 实现列补充 preschool-literacy-strokes-data.js）；`docs/优化方案2/幼小衔接工作台-方案文档/03-伙伴与激励/02-徽章与券.md`（游戏券裁决引用块）。
- **范围扩大记录（如实）**：审查阶段商标 grep 只含 我的世界/Mario/steve/Minecraft，遗漏「植物大战僵尸式」——执行时在 launcher 发现 3 处（alt/h2/themeDetails），补入合同断言并一并修复。最终 launcher 清理 9 处：植物大战僵尸式×3 + 我的世界式×3 + 马里奥式×3。
- 结论：允许推进。

## 阶段 3 — S2 素材清理（未执行）

- 用户确认删除：**否**（等用户明确授权后执行）。
- 删除前置 grep（代码引用）：未执行（随删除执行）。
- 删除文件数：—（对照清单 12，待确认）。
- 三世界 Console：—。
- `npm test`：—。
- 结论：**挂起**。12 个 PNG（voxel steve×4 + creeper×2、platform mario×6）已验证零代码引用，等用户点头即删。

## 阶段 4 — S1 回归（已执行）

- `npm test`：**退出码 0，216 项全绿**（基线 202 + 在途改动净增 14 + 新增 launcher 商标合同断言 1）。
- 期间两次瞬时失败（workbench-contract /preschool-workbench-refresh 版本戳）经核实为**并发在途改动在运行时刻的文件同步中间态**：单独重跑对应文件即全绿，最终全量回归稳定通过。不属于 S1 引入。
- 结论：S1 验收通过。

## 阶段 5 — S3 徽章真机验收（未执行）

- 设备 / 浏览器：无可用真机。
- 17 项记录：未执行。
- 发现的 defect：—。
- 结论：**挂起**。徽章六问题修复 5/6 已由静态证据验证（countFullPlanDays 严格模式 / 拼音不算花 / 砖块统一数据源 / 彩虹 CSS / data-action 等效委托），唯「17 项真机验收」需设备，等用户安排。
- 已同步 `docs/00-总控/当前状态.md`：待用户决定后同步。
