# T20260815-learning-acceptance

> 综合改进规划 S1 第 4 条:**学习模块浏览器真机闭环验收(识字/拼音/古诗)**。
> 背景:2026-08-15 学习 UI 文字简化(jumper/kidui 批)后,三模块只做过识字闪卡的局部冒烟,未整轮闭环。

## 验收口径

每个模块走完整一课:进入 → 互动(标记/听选/答题)→ 完成 → 阳光/进度落账。检查:
1. 互动可点、反馈正确(答对/再试);
2. 无 JS 报错导致的卡死(以运行状态与 DOM 更新为准);
3. 完成后 `growth.sunlight` / 课程进度有变化(桥接落账);
4. 简化后的文案保持幼儿口径(无成人说明文回归)。

## 模块清单

- 识字(preschool-literacy):闪卡会/不会 → 学不会的字 → 找字闯关
- 拼音(preschool-pinyin):听音选声韵母
- 古诗(preschool-poetry):听句/读句选下一句

## 用户裁定(2026-08-15)

- **不需要风格统一**:识字专区新卡片流与练习弹窗保持各自现状,验收只看功能,不做视觉统一改造。

## 范围外

- 英语/数学/拼读/专注(S2 批次再验)
- 花园保卫相关(另一会话在途改造,不碰)
- 性能与移动端布局(390px 已在卡片墙批次验过)

## 当前状态

- 状态:`in-progress`
