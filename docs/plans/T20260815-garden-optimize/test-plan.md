# T20260815-GD — 测试方案

证据 = 命令退出码 + 浏览器可玩路径。不解析 TAP 文案判定。

## 测试目标

- [ ] 主路径：三档难度参数正确应用；结算三行数据正确；庆祝卡按 awards 触发
- [ ] 关键边界：bridge 无 `getPlayMods` 时兜底不炸；sunMult 放大后仍受 40/80 上限；同局多枚里程碑排队弹
- [ ] 回归：238 项存量测试全绿 + 新增合同测试

## 分阶段

| 阶段 | 何时 | 测什么 | 通过后 |
|---|---|---|---|
| 0 基线 | 开工前 | 工作区处置 + `npm test` 基线 | 步骤 1 |
| 1 前提 | 写代码前 | game.js 速度应用点与结算 DOM 结论 | 步骤 2 写测试 |
| 2 主链路 | 每切片完 | 合同测试红→绿 + 浏览器 | 步骤 6 回归 |
| 4 回归 | S1 收口 | `npm test` 退出码 0 | S1 验收 |
| 5 S2 | 展开后 | roster / 土豆地雷 / 星级断言 | 任务收口 |

## 验证命令

```text
# S1 合同（先红后绿）
node --test tests/world-games.test.mjs

# 回归
npm test

# 无新 localStorage key（浏览器 devtools）
Object.keys(localStorage)  # 仅 petbank_huchuliang_preschool_workbench_state_v1
```

浏览器：`http://127.0.0.1:4180/prj/games/garden-defense/index.html`

## 需求点映射

| ID | 阶段 | 动作 | 时长 | 证据 |
|---|---|---|---|---|
| R4 | 2 | 合同测试红→绿 | ≤30min | 阶段-2 |
| R1 | 2 | 三档 mock 断言 + 浏览器改 mastery 实测 | ≤2h | 阶段-2 |
| R2 | 2 | 组装函数断言 + 目检 | ≤2h | 阶段-2 |
| R3 | 2 | 文案池/弹层断言 + 触发 ms-garden-3 | ≤3h | 阶段-2 |
| R8 | 4 | npm test + devtools key 检查 | ≤10min | 阶段-4 |
| R5–R7 | 5 | S2 展开后按分册 04 定义 | — | 阶段-5 |

## 真值

| 指标 | 含义 | 通过=成功？ | 补充 |
|---|---|---|---|
| 合同测试绿 | 参数与组装逻辑成立 | 否 | 必须浏览器亲眼确认档位切换 |
| npm test 绿 | 无回归 | 否 | 还需 devtools 确认无新 key |
| 庆祝卡弹出 | 仪式感成立 | 是 | 触发一次即证据 |

## 报告约定

- 阶段测试报告：要（回写 test-report.md，结论 + 命令 + 退出码）
- 截图：结算三行 + 庆祝卡各 1 张可选
- 受众：自己复盘 + 用户看 S1 结论
