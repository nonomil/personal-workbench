# T20260815-PQ — S1（P0 碰撞债 / P1 手感锁定 / P2 playMods / P3 结算三行）

> 优先级：P0 | 状态：pending | 前置：工作区处置（步骤 0）
> 只执行 S1。S2（P4–P6）不写进本页当正在做。
> 验证以退出码为准：`exit 0` 通过。

## 目标

碰撞干净、跳跃手感被测试锁死、识字量改变难度与奖励、bestTime 和长线成长看得见。

## Steps

### 0. 处置工作区 + 基线

- [ ] `git status` 确认在途文件归属
- [ ] `npm test` 基线记录（上次已知 238 全绿）；确认 `tests/platform-physics.test.mjs` 是否已存在
- **验证：** 退出码 0
- **回滚成本：** 无写入

### 1. 读码定位（不改代码）

- [ ] `game.js`：定位水平击退分支（当前状态 3.13 记录为死代码）与全宽地面碰撞路径；记录现有跳跃实现的实际参数（是否已有 coyote/buffer？数值多少？）
- [ ] 定位结算弹层、`bestTime` 写入点、敌人巡逻速度应用点
- **判定：** 若击退分支实际可达 → 停，走 task.md §8；若现状手感参数 ≠ 方案数值（100ms 等）→ 记录并问用户
- **验证：** 结论写进 test-report 阶段 1
- **回滚成本：** 无写入

### 2. 合同测试先红（R5）

- [ ] `tests/platform-physics.test.mjs`（新建或扩展）：
  - 碰撞 2 条：击退分支不存在（源码断言或行为断言）；全宽地面上任意 x 不穿地
  - 手感 4 条：离地 80ms 内按跳成功（coyote）；落地前 80ms 按跳落地即跳（buffer）；离地 150ms 按跳失败；长按跳高比点按 ≥30%
- [ ] `tests/world-games.test.mjs`：playMods 三档参数断言 + 结算三行组装断言 + 星芒节流断言（5 秒内两次调用只出 1 条）
- **验证：** 两条命令 — **先非 0**（红）
- **回滚成本：** 还原测试文件

### 3. P0 碰撞债清偿（R1）

- [ ] 删除水平击退死代码分支
- [ ] 全宽地面碰撞加保护（地面段覆盖整个关卡宽度时的边界条件）
- **验证：** 碰撞 2 断言绿；浏览器随抽 3 关走一遍不穿地、踩怪/被撞行为不变
- **回滚成本：** 还原 game.js 该区块

### 4. P1 手感纯函数（R2）

- [ ] 抽 `canJump(state, now, opts)`、`jumpVelocity(held)` 等纯函数（挂 `global.PlatformFeel` 之类可测命名空间，参照 stages/quests 的 IIFE 风格）；`game.js` 主循环改调它们
- [ ] 常量集中文件顶部并注明来源（分册 01§2）；**数值一律不改**（若步骤 1 发现现状无 coyote/buffer，按方案 100ms 实现——这是补缺不是调参）
- **验证：** 手感 4 断言绿；浏览器手玩第 3、6 关，跳跃无"吞输入"感
- **回滚成本：** 纯函数与内联实现行为等价，还原调用点即可

### 5. P2 playMods 接入（R3）

- [ ] `USE_PLAY_MODS` 常量 + 开局读 `bridge.getPlayMods()`（无方法兜底 null）
- [ ] 敌速×`enemySpeed`；通关阳光×`sunMult`（仍受 40/80 上限）；困难档巡逻 `minX/maxX` 扩 20%；简单档 coyote 放宽 140ms（传入 `canJump` 的 opts，不动默认常量）
- [ ] 关卡选择页与 HUD 显示档位徽标
- **验证：** 三档 mock 断言绿；浏览器改 mastery 实测
- **回滚成本：** 开关置 false

### 6. P3 结算三行 + 星芒（R4）

- [ ] 结算三行：① 用时 + 金币 + 星，破 `bestTime[level]` 时"新纪录！"帧 ② 冒险等级进度条（`getMetaSummary()`）③ 下一目标（最近未解锁里程碑或"第 n 关差 x 秒到 3 星"）
- [ ] 星芒 HUD（占位图允许）：开局加油；踩怪/破纪录短欢呼**节流 5 秒 ≤1 条**；失败回检查点按原因选池——掉坑池/被撞池各 ≥4 条，全部给操作提示不给否定
- **验证：** 组装/节流断言绿 + 浏览器通关与失败各目检一次
- **回滚成本：** 还原结算与 HUD 代码

### 7. 回归（R9）

- [ ] **更新缓存戳**：`prj/games/platform-quest/index.html` 中 `game.js?v=` 改为新戳（如 `20260815-pq-s1`）；`game.css` 如有 `?v=` 同步；若测试有戳断言一并更新
- [ ] `node --test tests/platform-physics.test.mjs tests/world-games.test.mjs`
- [ ] `npm test`
- **验证：** 退出码 0（预期 238+新增）
- **回滚成本：** 整包 S1 文件还原

### 8. 浏览器证据

- [ ] `http://127.0.0.1:4180/prj/games/platform-quest/index.html`：手玩 1/3/6/10 关；跳跃跟手、无穿地；结算三行与新纪录帧；触控三键热区 ≥56px
- [ ] Console 无报错；localStorage 仅既有 key；同关重复通关阳光不重复入账
- [ ] 结果回写 `test-report.md` 阶段 2
- **回滚成本：** 无

## Acceptance（S1）

- [ ] R1–R5 R9 有测试或浏览器证据
- [ ] R6–R8（S2）未开始——不是遗忘
- [ ] 未 commit（除非用户要求）
