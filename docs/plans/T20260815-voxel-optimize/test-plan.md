# T20260815-VX — 测试方案

证据 = 命令退出码 + 浏览器可玩路径。bridge 是共享件，每次改动后全量回归。

## 测试目标

- [ ] 主路径：周报总数动态化；三行结算；升段卡触发一次
- [ ] 关键边界：无 `VoxelQuests` 全局的兜底；同段位不重复弹；每日挑战重复完成不重复发奖
- [ ] 回归：238 项存量全绿 + 新增合同；花园/横版页加载不受 bridge 改动影响

## 分阶段

| 阶段 | 何时 | 测什么 | 通过后 |
|---|---|---|---|
| 0 基线 | 开工前 | 工作区处置 + `npm test` 基线 | 步骤 1 |
| 1 前提 | 写代码前 | bridge labels 位置 + rank 写入点结论 | 步骤 2 写测试 |
| 2 主链路 | 每切片完 | 合同红→绿 + 浏览器（含花园/横版两页冒烟） | 步骤 6 回归 |
| 4 回归 | S1 收口 | `npm test` 退出码 0 | S1 验收 |
| 5 S2 | 展开后 | 门禁/蓝图/快照/扩容断言 | 任务收口 |

## 验证命令

```text
# S1 合同（先红后绿）
node --test tests/world-games.test.mjs

# bridge 改动护栏（V1 后立即）
npm test

# 硬编码残留检查（期望退出码 1 = 零匹配）
grep -n "total: 8" prj/games/shared/workbench-bridge.js
```

浏览器：
- `http://127.0.0.1:4180/prj/games/voxel-adventure/index.html`（主链路）
- `http://127.0.0.1:4180/prj/games/garden-defense/index.html`、`.../platform-quest/index.html`（bridge 冒烟）
- 工作台成长页周报（total=12 目检）

## 需求点映射

| ID | 阶段 | 动作 | 时长 | 证据 |
|---|---|---|---|---|
| R4 | 2 | 合同红→绿 | ≤30min | 阶段-2 |
| R1 | 2 | mock 注入断言 + 三页冒烟 + 周报目检 | ≤1h | 阶段-2 |
| R2 | 2 | 组装函数断言 + 目检 | ≤3h | 阶段-2 |
| R3 | 2 | rank 2→3 断言 + 实测 | ≤2h | 阶段-2 |
| R9 | 4 | npm test + 关键词合同 | ≤10min | 阶段-4 |
| R5–R8 | 5 | S2 展开后按分册 04 | — | 阶段-5 |

## 真值

| 指标 | 含义 | 通过=成功？ | 补充 |
|---|---|---|---|
| total=12 断言绿 | 周报 bug 修复 | 否 | 必须目检工作台周报页 |
| 花园/横版冒烟无报错 | bridge 改动无副作用 | 是 | Console 零报错即证据 |
| 升段卡弹出一次 | 仪式感成立 | 是 | 同段位二次进入不弹 |

## 报告约定

- 阶段测试报告：要（结论 + 命令 + 退出码）
- 截图：升段卡 1 张可选
- 受众：自己复盘 + 用户看 S1 结论
