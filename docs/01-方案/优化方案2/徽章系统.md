好的，这是 P1 成就徽章系统的完整提示词，复制到 WorkBuddy 即可执行。

---

# 🏅 成就徽章系统 · 完整提示词

> **目标**：在不新建独立账本的前提下，基于已有的成长世界数据（花园花朵数、打卡天数、小镇砖块数）自动解锁徽章。
> **原则**：数据从现有的 `localStorage` 读取，不另建假进度。


## 一、任务概述

请为「幼小衔接工作台」的「成长世界」新增一套**成就徽章系统**。徽章不是额外奖励，而是**成长里程碑的仪式感标记**。孩子完成 10 个汉字、30 天打卡、50 个英语单词——这些本身就值得一枚徽章。


## 二、徽章体系

### 2.1 徽章列表（共 19 枚）

**三域成长（11 枚）**

| ID | 名称 | 解锁条件 | 颜色 |
|----|------|----------|------|
| `GARDEN_BRONZE` | 花园新秀 | 花园花朵 ≥ 10 朵 | 铜色 #CD7F32 |
| `GARDEN_SILVER` | 花园园丁 | 花园花朵 ≥ 50 朵 | 银色 #C0C0C0 |
| `GARDEN_GOLD` | 花园大师 | 花园花朵 ≥ 100 朵 | 金色 #FFD700 |
| `MAP_BRONZE` | 小探险家 | 全日打卡 ≥ 3 天 | 铜色 #CD7F32 |
| `MAP_SILVER` | 探险先锋 | 全日打卡 ≥ 15 天 | 银色 #C0C0C0 |
| `MAP_GOLD` | 大冒险家 | 全日打卡 ≥ 30 天 | 金色 #FFD700 |
| `BUILDER_BRONZE` | 小镇居民 | 英语/拼读课 ≥ 10 节 | 铜色 #CD7F32 |
| `BUILDER_SILVER` | 小镇工匠 | 英语/拼读课 ≥ 50 节 | 银色 #C0C0C0 |
| `BUILDER_GOLD` | 镇长 | 英语/拼读课 ≥ 100 节 | 金色 #FFD700 |
| `UNIFIED_SILVER` | 三域行者 | 三项银牌全齐 | 彩虹渐变 |
| `UNIFIED_GOLD` | 全能大师 | 三项金牌全齐 | 彩虹渐变+ |

**分级解锁（8 枚）** — 与 L1–L5 词库解锁规则一致，上一级 band ≥80% 为 `ready`/`maintenance` 时点亮

| ID | 名称 | 解锁条件 |
|----|------|----------|
| `LITERACY_L2` ~ `LITERACY_L5` | 识字 L2–L5 | 识字库分级开到对应级别 |
| `ENGLISH_L2` ~ `ENGLISH_L5` | 英语 L2–L5 | 英语词库分级开到对应级别 |

实现：`prj/preschool-achievements.js`（`getGrowthStats` 读 `PersonalWorkbenchBankLevels.resolveLevelStats`）；成长世界面板显示「识字 Lx / 英语 Lx」。

### 2.2 徽章数据结构

```javascript
// 在现有 growthData 中新增
{
  // ... 现有数据（花园、地图、小镇）
  achievements: {
    unlocked: ['GARDEN_BRONZE', 'MAP_BRONZE'],  // 已解锁徽章 ID 列表
    history: [
      { id: 'GARDEN_BRONZE', unlockedAt: 1734567890123 },
      // ...
    ]
  }
}
```


## 三、触发逻辑

### 3.1 解锁检查

每次学习任务完成时，自动执行检查：

```javascript
function checkAchievements() {
  // 1. 从 localStorage 读取当前数据
  const stats = getGrowthStats();  // { garden: { flowers: 23 }, adventure: { days: 12 }, builder: { bricks: 45 } }
  const existing = loadAchievements();  // ['GARDEN_BRONZE', ...]
  const newUnlocked = [];
  
  // 2. 依次检查所有徽章条件
  const rules = [
    { id: 'GARDEN_BRONZE', check: () => stats.garden.flowers >= 10 },
    { id: 'GARDEN_SILVER', check: () => stats.garden.flowers >= 50 },
    { id: 'GARDEN_GOLD', check: () => stats.garden.flowers >= 100 },
    { id: 'MAP_BRONZE', check: () => stats.adventure.days >= 3 },
    { id: 'MAP_SILVER', check: () => stats.adventure.days >= 15 },
    { id: 'MAP_GOLD', check: () => stats.adventure.days >= 30 },
    { id: 'BUILDER_BRONZE', check: () => stats.builder.bricks >= 10 },
    { id: 'BUILDER_SILVER', check: () => stats.builder.bricks >= 50 },
    { id: 'BUILDER_GOLD', check: () => stats.builder.bricks >= 100 },
    { id: 'UNIFIED_SILVER', check: () => 
      existing.includes('GARDEN_SILVER') && 
      existing.includes('MAP_SILVER') && 
      existing.includes('BUILDER_SILVER') 
    },
    { id: 'UNIFIED_GOLD', check: () => 
      existing.includes('GARDEN_GOLD') && 
      existing.includes('MAP_GOLD') && 
      existing.includes('BUILDER_GOLD') 
    }
  ];
  
  // 3. 只触发新解锁的
  rules.forEach(rule => {
    if (rule.check() && !existing.includes(rule.id)) {
      newUnlocked.push(rule.id);
    }
  });
  
  // 4. 如果有新徽章 → 弹窗庆祝 + 保存
  if (newUnlocked.length > 0) {
    showAchievementCelebration(newUnlocked);
    saveAchievements([...existing, ...newUnlocked]);
    // 宠物心情 +5（正向联动）
    if (window.petSystem) window.petSystem.addHappiness(5);
  }
}
```

### 3.2 触发时机

在以下事件发生后自动调用 `checkAchievements()`：
- 识字任务完成
- 拼音任务完成
- 英语任务完成
- 自然拼读任务完成
- 每日打卡完成（全部任务完成时）


## 四、庆祝弹窗

### 4.1 视觉效果

- **背景**：半透明遮罩，模糊背景内容
- **徽章图标**：放大展示（120px），带缩放+旋转入场动画
- **徽章名称**：大号字体显示名称（如「花园新秀」）
- **解锁条件**：小字显示「已解锁 10 朵花」
- **关闭按钮**：「太棒了！🎉」样式

### 4.2 CSS 动画

```css
@keyframes badgeUnlock {
  0% { transform: scale(0) rotate(-30deg); opacity: 0; }
  60% { transform: scale(1.2) rotate(5deg); opacity: 1; }
  100% { transform: scale(1) rotate(0deg); opacity: 1; }
}

@keyframes confettiDrop {
  0% { transform: translateY(-100px) rotate(0deg); opacity: 1; }
  100% { transform: translateY(400px) rotate(720deg); opacity: 0; }
}
```

### 4.3 彩纸粒子（轻量版）

不引入外部库，用 30-50 个纯 DOM 小方块模拟彩纸效果：

```javascript
function showConfetti() {
  const container = document.createElement('div');
  container.className = 'confetti-container';
  const colors = ['#FF6B6B', '#FFD93D', '#6BCB77', '#4D96FF', '#FF6BD6'];
  for (let i = 0; i < 40; i++) {
    const el = document.createElement('div');
    el.className = 'confetti-piece';
    el.style.cssText = `
      left: ${Math.random() * 100}%;
      background: ${colors[Math.floor(Math.random() * colors.length)]};
      animation-delay: ${Math.random() * 0.5}s;
      width: ${6 + Math.random() * 8}px;
      height: ${6 + Math.random() * 8}px;
      border-radius: ${Math.random() > 0.5 ? '50%' : '2px'};
    `;
    container.appendChild(el);
  }
  document.body.appendChild(container);
  setTimeout(() => container.remove(), 2000);
}
```


## 五、UI 位置

### 5.1 徽章收集箱（游戏世界 → 成长世界）

在成长世界顶部增加「🏅 徽章收集箱」入口按钮，点击展开：

```
┌──────────────────────────────────────────────────────────┐
│  🏅 徽章收集箱                    已收集: 3/11         │
├──────────────────────────────────────────────────────────┤
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ 🌸   │ │ 🌸   │ │ 🌸   │ │ 🌸   │ │ 🌸   │       │
│  │花园  │ │花园  │ │花园  │ │花园  │ │花园  │       │
│  │新秀  │ │园丁  │ │大师  │ │新秀  │ │园丁  │       │
│  │ ✅   │ │ ✅   │ │ ⬜   │ │ ⬜   │ │ ⬜   │       │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘       │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ 🗺️   │ │ 🗺️   │ │ 🗺️   │ │ 🏰   │ │ 🏰   │       │
│  │小探险 │ │探险  │ │大冒险 │ │小镇  │ │小镇  │       │
│  │家     │ │先锋  │ │家     │ │居民  │ │工匠  │       │
│  │ ✅   │ │ ⬜   │ │ ⬜   │ │ ✅   │ │ ⬜   │       │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘       │
│  ┌──────┐                                             │
│  │ 🏰   │                                             │
│  │镇长  │                                             │
│  │ ⬜   │                                             │
│  └──────┘                                             │
└──────────────────────────────────────────────────────────┘
```

**未解锁徽章显示为灰色占位**（⬜），让孩子知道「还有更多可以收集」。

### 5.2 家长报告 → 徽章墙

在家长报告底部新增「🏅 徽章墙」缩略图，只显示已解锁的徽章（带名称和小图标），方便家长一眼了解孩子成就。

### 5.3 首页 → 最新徽章展示

在首页宠物状态旁边，增加一枚「最新获得徽章」的小展示位（只显示最近解锁的 1 枚），3 天后自动淡出。


## 六、徽章 SVG 图标

### 6.1 通用徽章模板（所有徽章共用底框 + 自定义内图标）

```svg
<!-- 底框（所有徽章共用） -->
<svg viewBox="0 0 80 80" width="80" height="80">
  <defs>
    <linearGradient id="badgeBg" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#2C3E50" />
      <stop offset="100%" stop-color="#1A252F" />
    </linearGradient>
    <filter id="badgeShadow">
      <feDropShadow dx="0" dy="2" stdDeviation="4" flood-opacity="0.3" />
    </filter>
  </defs>
  <!-- 外圈（色环） -->
  <circle cx="40" cy="40" r="36" fill="none" stroke="{{COLOR}}" stroke-width="4" filter="url(#badgeShadow)"/>
  <!-- 内圈 -->
  <circle cx="40" cy="40" r="30" fill="url(#badgeBg)" />
  <!-- 星点装饰 -->
  <circle cx="20" cy="20" r="2" fill="{{COLOR}}" opacity="0.5"/>
  <circle cx="60" cy="20" r="2" fill="{{COLOR}}" opacity="0.5"/>
  <circle cx="40" cy="60" r="2" fill="{{COLOR}}" opacity="0.5"/>
  <!-- 中心图标（各徽章不同） -->
  {{ICON}}
  <!-- 底部名称缩写 -->
  <text x="40" y="72" text-anchor="middle" font-size="7" fill="{{COLOR}}" font-weight="bold">{{SHORT_NAME}}</text>
</svg>
```

### 6.2 各徽章中心图标

| 徽章 | ICON | SHORT_NAME |
|------|------|------------|
| GARDEN_BRONZE | 花朵图标（5瓣小花） | 🌸 |
| GARDEN_SILVER | 花朵图标 + 小星星 | 🌸⭐ |
| GARDEN_GOLD | 花朵图标 + 大星星 + 光晕 | 🌸✨ |
| MAP_BRONZE | 小旗子 🚩 | 🚩 |
| MAP_SILVER | 旗子 + 路径线 | 🚩➡️ |
| MAP_GOLD | 旗子 + 皇冠 👑 | 👑 |
| BUILDER_BRONZE | 小房子 🏠 | 🏠 |
| BUILDER_SILVER | 房子 + 砖块 | 🏠🧱 |
| BUILDER_GOLD | 城堡 🏰 | 🏰 |
| UNIFIED_SILVER | 三圆环（银） | 🔗 |
| UNIFIED_GOLD | 三圆环+皇冠（金） | 👑🔗 |


## 七、数据存储

### 7.1 localStorage 键名

```
growth_achievements: {
  unlocked: ['GARDEN_BRONZE', 'MAP_BRONZE'],
  history: [
    { id: 'GARDEN_BRONZE', unlockedAt: 1734567890123 }
  ],
  lastShown: 'GARDEN_BRONZE'  // 首页展示的最新徽章
}
```

### 7.2 徽章定义（静态配置）

```javascript
const BADGE_DEFS = {
  'GARDEN_BRONZE': {
    id: 'GARDEN_BRONZE',
    name: '花园新秀',
    description: '花园花朵达到 10 朵',
    category: 'garden',
    tier: 'bronze',
    color: '#CD7F32',
    icon: '🌸',
    check: (stats) => stats.garden.flowers >= 10
  },
  // ... 所有徽章定义
};
```


## 八、与现有系统的集成点

| 集成点 | 操作 |
|--------|------|
| 识字/拼音任务完成 | 调用 `checkAchievements()` |
| 英语/拼读任务完成 | 调用 `checkAchievements()` |
| 每日打卡完成 | 调用 `checkAchievements()` |
| 宠物系统 | 徽章解锁时 `pet.happiness += 5` |
| 首页 | 读取 `lastShown` 显示最新徽章 |
| 家长报告 | 新增「徽章墙」板块 |


## 九、验收清单

- [ ] 徽章数据正确存储到 localStorage
- [ ] 10 朵花时自动解锁「花园新秀」并弹窗
- [ ] 50 朵花时自动解锁「花园园丁」并弹窗
- [ ] 100 朵花时自动解锁「花园大师」并弹窗
- [ ] 3 天打卡时自动解锁「小探险家」并弹窗
- [ ] 15 天打卡时自动解锁「探险先锋」并弹窗
- [ ] 30 天打卡时自动解锁「大冒险家」并弹窗
- [ ] 10 块砖时自动解锁「小镇居民」并弹窗
- [ ] 50 块砖时自动解锁「小镇工匠」并弹窗
- [ ] 100 块砖时自动解锁「镇长」并弹窗
- [ ] 三项银牌全齐时解锁「三域行者」并弹窗
- [ ] 三项金牌全齐时解锁「全能大师」并弹窗
- [ ] 徽章收集箱正确显示已解锁/未解锁状态
- [ ] 家长报告徽章墙正确显示
- [ ] 首页最新徽章正确展示
- [ ] 庆祝弹窗动画流畅
- [ ] 彩纸粒子效果正常
- [ ] 宠物心情联动生效
- [ ] 已解锁徽章不重复弹窗


## 十、输出要求

> 请直接输出完整的代码，包含所有 CSS、JavaScript 和 HTML 结构。关键函数添加中文注释。确保代码可以直接合入现有工作台，不破坏已有功能。**开始生成！**
