# 学习专区布局改版方案：今日任务 + 学科卡片墙

- 日期：2026-08-15
- 状态：**方案待实施**（本文档只记录设计，未改任何代码）
- 页面范围：幼儿工作台 `#courses` 学习专区（`prj/app.js` 的 `renderPreschoolCourses` 及其子渲染函数）
- 对应综合规划维度：学习（`docs/00-总控/2026-08-14-综合改进规划.md` 第 2 节）

## 1. 问题定义

当前学习专区的问题不是"没有卡片"，而是**每张课程卡里叠加了太多层内容**，幼儿首屏文字过多。总览模式下 `renderPreschoolCourseCard`（`prj/app.js` 约 3026 行）对全部 9 个专区各渲染一张完整大卡，每张卡包含：

| 层 | 渲染函数 | 内容 |
| --- | --- | --- |
| 标题区 | 卡片头部 | 标题 + 长描述 + 英文小标题（LEARNING LANES / REFERENCE LIBRARY 等） |
| 进度条 | `renderPreschoolCourseProgress` | 成长路线 n/m + 状态文案 |
| 分级带 | `renderPreschoolLevelBands` | L1–L5 各级解锁 / 掌握度 |
| 掌握统计 | `renderPreschoolLiteracyMastery` / `EnglishMastery` / `SubjectMastery` | 会了 / 还不会 / 还没点 / 今天再认 |
| 参考说明 | `preschool-course-reference` | 参考网页说明文字 |
| 徽章/示例/资源 | `renderPreschoolCourseBadges` / `CourseSamples` / `CourseMedia` / `CourseResources` / `SummerLibrary` | 示例词、媒体、资料链接 |
| 学习路线 | `renderPreschoolCourseRoute` | 全部练习卡步骤列表 |

另外页面左侧还有 `renderPreschoolCourseDirectory` 页内目录（约 3015 行），与侧边栏"学习专区"9 个入口（`preschool-workbench/index.html` 第 23–34 行 `data-sidebar-section="course-lanes"`）功能重复。

**幼儿一屏看到的是 9 张信息密度接近家长后台的卡。**

## 2. 目标（方案 1：今日任务 + 学科卡片墙）

```text
学习

┌──────────────────────────────┐
│ 今天学什么？                  │
│ 完成今日识字 · 认 4 个字      │   [▶ 去练习]
└──────────────────────────────┘

┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐
│ 插图 │ │ 插图 │ │ 插图 │ │ 插图 │
│ 识字 │ │ 拼音 │ │ 古诗 │ │ 数学 │
│ ●●○  │ │ ●○○  │ │ ○○○  │ │ ●●●  │
│ 继续 │ │ 继续 │ │ 开始 │ │ 完成 │
└──────┘ └──────┘ └──────┘ └──────┘
（专注 / 英语 / 拼读 / 运动 / 暑假 同样式）
```

设计原则：

1. 总览首屏只保留：大图、短标题（2 个字）、3 个进度点、状态字（开始/继续/完成）。
2. 删除总览卡片中的长描述、英文小标题、示例词、参考说明、掌握统计、分级带。
3. 9 个学科用**已有的 tone 色**（`config.js` 中每个课程的 `tone` 字段：orange/blue/lime/pink/gold）+ 已有插图（`preschoolAssetForIcon(course.icon)`）区分。
4. **整张卡可点击**（整卡是一个 `<button>`），不要求孩子找小按钮。
5. 课程详情页（focused 模式，`ui.courseId` 有值时）继续展示"认 → 练 → 测"完整路线和练习，掌握统计、分级带、资源说明只出现在这里。
6. 移动端两列大卡，所有触控区域 ≥ 44px。

## 3. 实施设计（可直接照做）

### 3.1 `prj/app.js` 改动

**删除**：`renderPreschoolCourseDirectory`（页内目录整个函数，导航职责完全交给侧边栏 course-lanes 组）。

**新增三个函数**（放在原 `renderPreschoolCourseDirectory` 位置）：

```js
function getPreschoolCourseShortTitle(course) {
    const shortTitles = {
        'preschool-summer': '暑假', 'preschool-literacy': '识字',
        'preschool-pinyin': '拼音', 'preschool-poetry': '古诗',
        'preschool-math': '数学', 'preschool-focus': '专注',
        'preschool-english': '英语', 'preschool-phonics': '拼读',
        'preschool-exercise': '运动'
    };
    return shortTitles[course.id] || course.title;
}

function renderPreschoolCourseWallCard(course) {
    const completion = getPreschoolCourseCompletion(course);
    const status = completion.percent >= 100 ? 'complete' : completion.completed ? 'active' : 'ready';
    const statusLabel = status === 'complete' ? '完成' : status === 'active' ? '继续' : '开始';
    const dots = [1, 2, 3].map(function (step) {
        return `<i class="${completion.percent >= step * 33 ? 'is-on' : ''}"></i>`;
    }).join('');
    const iconName = course.icon || 'book-open';
    const art = preschoolAssetForIcon(iconName) ? preschoolAsset(preschoolAssetForIcon(iconName), course.title) : icon(iconName);
    return `<button class="preschool-course-wall-card tone-${escapeHtml(course.tone || 'blue')}" type="button" data-action="navigate" data-page="courses" data-course-id="${escapeHtml(course.id)}" aria-label="打开${escapeHtml(course.title)}"><span class="preschool-course-wall-art" aria-hidden="true">${art}</span><strong>${escapeHtml(getPreschoolCourseShortTitle(course))}</strong><span class="preschool-course-wall-dots" role="progressbar" aria-label="${escapeHtml(course.title)}完成度" aria-valuemin="0" aria-valuemax="100" aria-valuenow="${completion.percent}">${dots}</span><span class="preschool-course-wall-state is-${status}" data-route-state="${status}">${statusLabel}</span></button>`;
}

function renderPreschoolCoursesTodayCard() {
    const today = storage.localDate();
    const plans = (Array.isArray(state.dailyPlans) ? state.dailyPlans : [])
        .filter(function (item) { return item.date === today; })
        .sort(function (a, b) { return (a.order || 0) - (b.order || 0); });
    const workflow = getPreschoolHomeWorkflow(plans);
    const headline = workflow.kind === 'game' ? '今天都学完啦！' : workflow.kind === 'rest' ? '还差一点点' : '今天学什么？';
    return `<section class="preschool-course-today"><span class="preschool-course-today-art" aria-hidden="true">${preschoolAsset('storybook-token', '今日学习')}</span><div class="preschool-course-today-copy"><small>${headline}</small><strong>${escapeHtml(workflow.title)}</strong></div><button class="btn-primary preschool-course-today-cta" type="button" ${preschoolWorkflowActionAttrs(workflow)}>${icon('play')}<span>${escapeHtml(workflow.cta || '开始')}</span></button></section>`;
}
```

要点：

- "今天学什么"**完全复用**首页真实工作流推导 `getPreschoolHomeWorkflow`（约 3096 行）与 `preschoolWorkflowActionAttrs`（约 3157 行），四种状态自动成立：有练习任务 → `open-plan-practice` 去练习；全部完成 → 去当前主题游戏；剩非练习任务 → 去任务页；无任务 → 安排一项。不新造第二套"下一步"逻辑。
- 进度点用现有 `getPreschoolCourseCompletion`，percent ≥33/66/99 各点亮一颗；同时保留 `role="progressbar"` 无障碍语义。
- 状态字延续现有三态口径（ready/active/complete），`data-route-state` 属性保留，供测试与主题皮肤使用。

**改写 `renderPreschoolCourses`**（约 3030 行）：

```js
function renderPreschoolCourses() {
    const courses = getPreschoolCourses();
    const activeCourse = getPreschoolCourseById(ui.courseId);
    if (!activeCourse) {
        return `${renderPreschoolIntro(PAGE_META.courses, '', '', `<span class="tag lime">${courses.length} 个专区</span>`)}${renderPreschoolCoursesTodayCard()}<div class="preschool-course-wall" aria-label="学科卡片墙">${courses.map(function (course) { return renderPreschoolCourseWallCard(course); }).join('')}</div>`;
    }
    return `${renderPreschoolIntro(PAGE_META.courses, '', '', `<span class="tag lime">${escapeHtml(activeCourse.title)}</span>`)}<div class="preschool-course-layout is-focused"><div class="preschool-course-content"><section class="preschool-course-library-note"><span class="preschool-course-library-art">${preschoolAsset('storybook-token', '学习资源')}</span><div><span class="eyebrow">REFERENCE LIBRARY</span><h2>正在学习：${escapeHtml(activeCourse.title)}</h2><p>这里保留题型、样例和练习节奏，做完一张就点亮一张。</p></div><button class="btn-secondary" type="button" data-action="navigate" data-page="courses">查看全部${icon('layout-grid')}</button></section><div class="preschool-course-grid">${renderPreschoolCourseCard(activeCourse, true)}</div></div></div>`;
}
```

即：总览 = 今日卡 + 卡片墙（无目录、无 library-note、无完整大卡）；详情 = 原有完整卡（路线、掌握统计、分级带、资源都保留在这里）+"查看全部"返回。

`renderPreschoolCourseCard`、`renderPreschoolCourseRoute` 等详情渲染函数**本期不改**。

### 3.2 新增 CSS：`prj/css/preschool/34-course-wall.css`

```css
/* 34 · 学习专区：今日任务卡 + 学科卡片墙 */

.preschool-course-today {
    display: flex; align-items: center; gap: 14px;
    margin: 14px 0 18px; padding: 16px 18px;
    border-radius: 22px;
    background: linear-gradient(135deg, #fff7dc, #ffe9b3);
    border: 2px solid rgba(224, 172, 62, 0.45);
    box-shadow: 0 10px 24px rgba(224, 172, 62, 0.18);
}
.preschool-course-today-art { flex: 0 0 auto; width: 64px; height: 64px; display: grid; place-items: center; }
.preschool-course-today-art img { width: 100%; height: 100%; object-fit: contain; }
.preschool-course-today-copy { flex: 1 1 auto; min-width: 0; display: grid; gap: 2px; }
.preschool-course-today-copy small { font-size: 0.82rem; color: #8a6b1f; font-weight: 700; }
.preschool-course-today-copy strong { font-size: 1.18rem; color: #4a3a10; line-height: 1.3; overflow-wrap: anywhere; }
.preschool-course-today-cta { flex: 0 0 auto; min-height: 48px; padding: 0 20px; border-radius: 16px; font-size: 1rem; }

.preschool-course-wall {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
    gap: 14px;
}
.preschool-course-wall-card {
    display: grid; justify-items: center; gap: 8px;
    min-height: 168px; padding: 18px 12px 14px;
    border: 2px solid transparent; border-radius: 22px;
    background: #ffffff; cursor: pointer; text-align: center;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
    box-shadow: 0 8px 20px rgba(31, 73, 42, 0.08);
}
.preschool-course-wall-card:hover,
.preschool-course-wall-card:focus-visible { transform: translateY(-3px); box-shadow: 0 12px 26px rgba(31, 73, 42, 0.16); }
.preschool-course-wall-card strong { font-size: 1.22rem; color: #23422c; }
.preschool-course-wall-art { width: 76px; height: 76px; display: grid; place-items: center; }
.preschool-course-wall-art img, .preschool-course-wall-art svg { width: 100%; height: 100%; object-fit: contain; }
.preschool-course-wall-dots { display: inline-flex; gap: 6px; }
.preschool-course-wall-dots i { width: 12px; height: 12px; border-radius: 50%; background: rgba(35, 66, 44, 0.15); }
.preschool-course-wall-dots i.is-on { background: #3fa25c; }
.preschool-course-wall-state { font-size: 0.85rem; font-weight: 700; padding: 4px 14px; border-radius: 999px; background: rgba(63, 162, 92, 0.14); color: #2c7a45; }
.preschool-course-wall-state.is-complete { background: rgba(224, 172, 62, 0.2); color: #8a6b1f; }
.preschool-course-wall-state.is-active { background: rgba(47, 128, 237, 0.14); color: #2456a6; }

/* 学科色区分（沿用 config.js 的 tone 字段） */
.preschool-course-wall-card.tone-orange { background: #fff3e4; border-color: rgba(240, 145, 55, 0.35); }
.preschool-course-wall-card.tone-blue { background: #e9f3ff; border-color: rgba(64, 140, 230, 0.35); }
.preschool-course-wall-card.tone-lime { background: #eefae4; border-color: rgba(120, 190, 60, 0.4); }
.preschool-course-wall-card.tone-pink { background: #ffeef3; border-color: rgba(235, 110, 150, 0.35); }
.preschool-course-wall-card.tone-gold { background: #fff7dc; border-color: rgba(224, 172, 62, 0.45); }

@media (max-width: 760px) {
    .preschool-course-wall { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 12px; }
    .preschool-course-today { flex-wrap: wrap; }
    .preschool-course-today-cta { flex: 1 1 100%; }
}
```

在 `prj/css/preschool-workbench.css` 末尾追加导入：

```css
@import url("./preschool/34-course-wall.css?v=20260815-course-wall-v1");
```

### 3.3 缓存戳

`prj/preschool-workbench/index.html`：

- 第 9 行 `preschool-workbench.css?v=` 与第 64 行 `app.js?v=` 统一升为 `20260815-course-wall-v1`。
- **并行冲突提示**：2026-08-15 00:09 另一会话已把 `app.js?v=` 改为 `20260815-kidui-v1`（同时移除了 voxel world/pixel-tiles/workshop 三个 script 标签），且尚未同步测试（当前 4 个测试仍断言旧戳 `20260814-walker-v2` 而失败）。实施本方案前先与该会话的改动合流，再统一定戳。

### 3.4 需同步的合同测试（`tests/preschool-workbench-refresh.test.mjs` 等）

| 测试 | 现断言 | 改为 |
| --- | --- | --- |
| `keeps preschool course content spacious...`（约 262 行） | app/styles 含 `preschool-course-directory` | app/styles 含 `preschool-course-wall`、`preschool-course-today`；保留 `preschool-course-card\.is-focused`、`repeat\(2, minmax\(0, 1fr\)\)` |
| `shows progress and current state in the preschool course directory`（约 851 行） | `preschool-course-directory-progress/-state` | 改名并断言 `preschool-course-wall-dots`、`preschool-course-wall-state`、`role="progressbar"`、`data-route-state="\$\{status\}"` |
| `keeps sidebar course lanes as the persistent navigation...`（约 862 行） | 断言 `activeCourse ? '' : renderPreschoolCourseDirectory(...)` | 改为 `assert.doesNotMatch(app, /renderPreschoolCourseDirectory/)`；侧边栏 `data-sidebar-section="course-lanes"` 与 focused 布局断言保留 |
| 缓存戳断言（本文件约 108/111/585 行，`preschool-lesson-mistakes.test.mjs` 约 90 行，`preschool-math-practice.test.mjs` 约 129 行） | `app.js?v=20260814-walker-v2` | 与 3.3 定稿的新戳一致 |

`renders preschool courses as visual learning routes...`（约 275 行，断言 route-step 等）不需要改——路线仍在详情页。

## 4. 验收标准

Node 层：

- `npm test` 全绿（含上表更新后的合同测试）；`node --check prj/app.js` 通过。

浏览器层（390px 竖屏 + 桌面各过一遍）：

1. 打开 `#courses`：首屏只有 今日任务卡 + 9 张学科大卡；无长描述、无英文小标题、无掌握统计。
2. 今日卡：有未完成练习任务时点"去练习"直接进练习；练习完成后回到学习页，今日卡自动切到下一项；全部完成时 CTA 变为进游戏。
3. 点击任意学科卡任意位置（含图、字、点）都能进入该学科详情页；详情页保留"认→练→测"路线、掌握统计与"查看全部"返回。
4. 390px 下卡片墙为两列，卡片可点击区域 ≥ 44px，无横向溢出；320px 复测无溢出。
5. 完成一节课后返回总览，对应卡的进度点与状态字（开始→继续→完成）更新。

## 5. 明确不做（本期）

- 不改课程详情页（focused 模式）的内容结构；掌握统计、分级带、资源说明保留在详情页，后续再评估是否移家长区。
- 不做"学习地图"游戏化布局（备选方案 3，维护成本高）。
- 不改侧边栏 course-lanes 导航、不改 `dailyPlans`/`courseProgress` 任何数据结构、不新增 localStorage key。
- 不动三世界游戏与首页工作流卡。

## 6. 回滚

纯渲染层改动：还原 `renderPreschoolCourses` / 删除新增两个渲染函数、恢复 `renderPreschoolCourseDirectory`、去掉 CSS import 并回退缓存戳即可，无数据迁移。
