# 01 · 现状盘点（2026-08-15）

## 一、docs/01-方案/学习项目设计（整理前）

9 个 md + `refs/`（12 个克隆参考仓，已 gitignore）。核心问题：

1. **命名三套混用**：`DS--` 前缀 / 无前缀中文名 / 日期前缀；`DS--建议.MD` 扩展名大写；`DS--游戏化学习--设计方案.md` 名实不符（实为参考项目合入裁决）。
2. **层级不可见**：两份"现行裁决"（落地分析、合入裁决）与五份调研素材、一份已被取代的对话稿平铺在一起，无 README 声明谁说了算。
3. **口径打架**：调研文写 1500 字/500 词/`cnchar.getWords`/直接 fork，裁决文全部否决（30→80 字、接现有 bank、只借玩法）；只读调研文会做错。
4. **一文多体**：多个文件是 AI 对话粘贴稿，前后推荐不一致、遗留"我直接开始写代码？"等对话残留。

→ 已整理，见 `02-文档整理执行记录.md`。

## 二、docs/02-课程（整理前）

双层结构（设计决策 D-002，保留）：`幼儿课程方案/`（跨科合同，00-总控～08-实施与验收）+ 七个学科资料包（00～05 编号 + research/）。问题：

1. **充实度失衡**：自然拼读最实（自研方案 + research 证据链 + PDF）；幼儿课程方案相对实（合同提纲）；识字/数学/英语是薄大纲；古诗/成长游戏/运动与专注纯占位。
2. **两层漂移**：每日核心任务数（合同 3 项 vs 代码 6 项）、奖励数值（合同 +10/+20 vs 运行时另一套）、识字目标（60–80 字 vs 运行时 240/目标 1500）、识字 60 日主题分段不对齐。
3. **过时路径**：`幼儿课程方案/README.md` 指向已迁移的研究目录（已修复）。
4. **缺总索引**：目录下无 README 说明两层关系与优先级（已补 `docs/02-课程/README.md`）。
5. **路径被锁**：学科文件夹名被 `tests/preschool-subject-packs.test.mjs`、`tests/phonics-course-data.test.mjs`、`scripts/generate-preschool-subject-packs.mjs` 按路径依赖，**不能改名编号**，排序职责交给索引 README。

## 三、prj 学习功能实现现状（摘要）

详见探查记录；关键事实：

- **已落地**：侧边栏 9 学习专区；`#courses` 今日任务卡 + 学科卡片墙（`prj/css/preschool/34-course-wall.css` + `app.js` 3015–3058，缓存戳 `20260815-course-wall-v1`）；多 mode 练习弹窗；识字/英语/数学引擎较深（掌握态、复习队列、L 带）；错题本；打卡；阳光商城；星芒伙伴；三世界游戏经 `workbench-bridge.js` 回写阳光（日 cap 80、eventId 去重）。
- **数据流真实路径**：`docs/02-课程/<学科>` → `generate-preschool-subject-packs.mjs` → `prj/data/preschool/<学科>/*.json` → `build_lesson_pack.py` → `preschool-lesson-pack-data.js` → `config.js` 种子课追加 → `app.js`。`assets/.../manifest.json` 多为 `items: []` 空壳登记，不驱动 UI。
- **差距清单**：
  1. 卡片墙方案文档（`01-方案/2026-08-15-学习专区卡片墙布局方案.md`）头部仍写"待实施"，代码已实现；`tests/preschool-workbench-refresh.test.mjs` 仍断言旧 `preschool-course-directory` 结构。
  2. 60 日教案的 `activityType`（图配字、拖动等）运行时多降级为 `choice`/`motion-timer`，教案深度未产品化。
  3. 古诗/拼音/拼读没有完整 60 日包挂入专区路线（lesson-pack 只挂 literacy/math/english/focus/exercise 五科）。
  4. 数据合同里的 `fourSteps`/`evidence` 字段引擎未消费。
  5. "成长游戏"在 data/测试合同里有，侧边栏无对应专区（与"趣味进独立游戏"决策一致，暂不视为缺陷）。
  6. 综合改进规划 P0（六项任务全接"去练习"、错题 1/3/7 回流）尚未完成。

## 四、外部笔记（游戏化学习工作台，约 25 篇）

小红书 WorkBuddy 系"给娃手搓学习工作台"笔记采集包。共识模式：多学科任务墙 + 每日定量 + 虚拟币喂养伙伴 + 虚实兑换（家长确认）+ 错题本 + 主题化皮肤（布鲁伊/马里奥/独角兽/PVZ/我的世界）。与本项目高度同源（本项目已有其中大半），差异点与可借鉴项的逐条裁决见 `03-外部笔记借鉴清单.md`。
