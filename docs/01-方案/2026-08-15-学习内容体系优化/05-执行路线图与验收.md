# 05 · 执行路线图与验收

> 批次按依赖排序；每批独立可交付、可验收。与 `00-总控/2026-08-14-综合改进规划.md` 的 S 批次并行不冲突：本方案 B1 属其 P0 文档面，B2/B3 对应其 S2/S3 相邻位。
> **执行控制面已装配为任务包**（2026-08-15，供 CC 接手）：B1 → `docs/plans/T20260815-B1-docs-truth/`；B2 → `docs/plans/T20260815-B2-practice-review/`；B3 → `docs/plans/T20260815-B3-reward-loop/`；B4 → `docs/plans/T20260815-B4-content-deepening/`（轻量待启动）。执行状态以各包 `.meta.yaml` 与 `test-report.md` 为准，本文不再回写进度。

## 批次

| 批 | 内容 | 前置 | 预估 |
| --- | --- | --- | --- |
| **B0（已完成，2026-08-15）** | 两个文档目录整理：学习项目设计编号改名 + 双 README 索引 + 引用同步 + 过时路径修复 | — | 已交付 |
| **B1 文档回真** | P0-1 五项文档对齐 + refresh 测试断言更新 | B0 | 0.5 天 |
| **B2 既定 P0 功能** | 六项任务接"去练习" + 错题 1/3/7 回流（综合改进规划既定项） | B1 | 1–2 天 |
| **B3 闭环四件套** | P1-1 家长确认兑换 → P1-2 两档任务 → P1-4 喂养直连/连击/防刷分文案 → P1-3 英语日定量 | B2 | 2–3 天，可拆单项交付 |
| **B4 内容深化** | P2 按需逐项：新练习 mode → 三科 60 日挂入 → 学情摘要 → fourSteps 消费 | B3 | 按项排期 |

## 每批验收

### B0（本次，验收命令）

```powershell
node --test tests/preschool-subject-packs.test.mjs tests/phonics-course-data.test.mjs
```

- 学习项目设计目录下无 `DS--*` 文件；`00-README.md` 索引存在且映射表与实际文件一致。
- `docs/02-课程/README.md` 存在；`幼儿课程方案/README.md` 研究路径可达。
- `docs/plans/T20260814-audit-remediation/` 内无旧文件名残留引用。

### B1

- `rg "每天三项核心|待实施" docs/01-方案/2026-08-15-学习专区卡片墙布局方案.md docs/02-课程/幼儿课程方案/` 无误导性残留。
- `node --test tests/preschool-workbench-refresh.test.mjs` 通过。

### B2

- 今日 6 项每项都有可点的"去练习"并能完成回写；错题在第 1/3/7 天出现在复习入口；对应 node 测试通过。

### B3

- 兑换 pending→家长确认→核销链路真机走通；必做未完时冒险入口置灰；英语页"今日 3 词 + 词库分色"可见；重开已完成课出现防刷分提示。
- `docs/data-model.md` 与新增 storage 字段同步（仅 P1-1 允许新增字段）。

### B4

- 每项单独验收：新 mode 有对应测试；三科 60 日在专区可见课程数增长；学情摘要页数值与 storage 一致。

## 风险与回退

1. **refresh 测试改断言**可能暴露卡片墙实现与方案文档的细节差异——以代码为准修文档，不反向改实现。
2. **奖励合同对数**若发现代码内部不一致（如 bridge 与 app.js 数值口径不同），先记入 00-总控决策记录再改，不顺手改数值。
3. 文件改名后若发现遗漏引用（历史 handoff 等），按"旧路径不回改"规则不处理，仅在被主动读到时映射。
