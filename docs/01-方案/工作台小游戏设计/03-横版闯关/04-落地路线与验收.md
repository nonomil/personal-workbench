# 横版闯关 · 落地路线与验收

- 日期：2026-08-15，状态：方案（未改代码）
- 前置：`docs/00-总控/变更与同步规则.md`；测试合同 `tests/world-games.test.mjs`。

## 1. 任务切片

### 一期：接通

| 切片 | 改动 | 验收 |
| --- | --- | --- |
| P1 手感纯函数 | 抽 `canJump` 等纯函数并写四个边界断言（coyote/buffer/超时/变高） | 断言全绿；参数常量与文档一致 |
| P2 playMods 接入 | 怪速×`enemySpeed`、阳光×`sunMult`、困难档巡逻范围 +20%、简单档 coyote 140ms；HUD 徽标 | mock 三档识字量断言参数差异 |
| P3 结算三行 + 星芒 HUD | 结算页（用时/新纪录/冒险进度/下一目标）+ 像素星芒与节流文案 | 断言破纪录分支与节流逻辑 |

### 二期：加深

| 切片 | 改动 | 验收 |
| --- | --- | --- |
| P4 时间挑战外显 | 关卡选择页显示 bestTime/星数/"可挑战"标；结算对比上次 | 断言 `bestTime` 读写与显示格式 |
| P5 检查点反馈 | 旗升起帧 + 音效 + 星芒台词 | 触发一次性，重复经过不再弹 |
| P6 关卡减肥复查 | 1–2 关 ≤60s、9–10 关 ≤120s 实测修剪 | 记录三档识字量下的实测用时表进本册 |

### 三期（远期）

蝙蝠敌人进 9–10 关、全 3 星成就（是否新增 `ms-platform-speed` 里程碑需连同 `MILESTONES`、`getMetaSummary`、测试一起改，走变更与同步规则）、终点烟花。

## 2. 统一验收标准

- `npm test` 全绿；手感四断言纳入 `world-games.test.mjs` 或独立 `platform-feel.test.mjs`。
- 触控三键（左/右/跳）热区 ≥56px，`data-hold` 长按连续生效。
- localStorage 只有既有 key；同关重复通关不重复发阳光（eventKey 去重）。
- 单关时长达标；掉坑重开不扣任何已有资产。

## 3. 回滚

- P2 挂 `USE_PLAY_MODS` 开关；P1 纯函数重构行为等价（先加测试再重构，红→绿）；P4/P5 是显示层，回滚即删 UI。
- 进度结构不变，无迁移。
