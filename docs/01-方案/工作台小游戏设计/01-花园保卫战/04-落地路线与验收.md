# 花园保卫战 · 落地路线与验收

- 日期：2026-08-15，状态：S1 已实现；S2 已落地（G4 roster / G5 土豆地雷占位图 / G6 星级），待手玩
- 前置：读 `docs/00-总控/变更与同步规则.md`；测试合同在 `tests/world-games.test.mjs`。

## 1. 任务切片（按期）

### 一期：接通（预计 3 个切片）

| 切片 | 改动 | 验收 |
| --- | --- | --- |
| G1 playMods 接入 | `game.js` 开局读 `bridge.getPlayMods()`，僵尸速度乘 `enemySpeed`、通关阳光乘 `sunMult`、困难档每波 +1 僵尸；HUD 档位徽标 | 单测：mock 识字 20/150/250 三档，断言波次僵尸速度与发放阳光不同 |
| G2 结算页三行 | 结算弹层加冒险等级进度条与"下一目标"（读 `getMetaSummary`） | 页面含 `adventureTitle` 文本；断言最近未解锁里程碑计算正确 |
| G3 星芒 HUD + 庆祝卡 | HUD 头像 + 文案池；`awards` 含 milestone 时弹卡 | 断言 milestone award 触发弹层 DOM；文案池 ≥12 条 |

### 二期：加深（预计 3 个切片）

> 当前切片状态（2026-08-15）：G1–G6 均已落地。土豆地雷正式贴图 `plants/plant-potatomine.png` 已接线。S2 合同绿。待手玩 7/9 关。

| 切片 | 改动 | 验收 |
| --- | --- | --- |
| G4 stages roster | `stages.js` 加 `roster` 字段并按 §2 曲线填 12 关；`spawnDefenseWave` 按 roster 抽取 | 断言第 7 关首现 bucket、第 9 关首现 football；不传 roster 行为不变 |
| G5 土豆地雷 | 素材（02 册 P1）→ `PLANT_CATALOG` 新条目 → 第 8 关解锁 | 埋雷 3 秒后触发秒杀；造价 20；`unlockPlants` 含新 id |
| G6 星级重定义 | 2 星=未过中线、3 星=parSec 内且余阳光 ≥25 | 三种结局断言星数；旧 stars 不迁移 |

### 三期（远期，暂不排期）

新关卡批次（13–18 关或周挑战关）、三星全收集奖励、僵尸受击帧。

## 2. 统一验收标准

- `npm test` 全绿；`world-games.test.mjs` 新断言全部落在纯函数或 DOM 合同上。
- localStorage 仅出现 `petbank_huchuliang_preschool_workbench_state_v1`；`awardSunlight` 日限 80 不被绕过（同日多刷通关只到上限）。
- 单局 ≤3 分钟（第 12 关困难档实测）。
- 390px 视口 HUD 不溢出，触控热区 ≥44px。
- `assets/manifest.md` 与实际引用一致，无 404。

## 3. 回滚

- 每切片独立提交；G4 的 roster 是纯增量字段，回滚即删字段恢复旧抽取；G1 playMods 有开关常量 `USE_PLAY_MODS`，出问题置 false 即回到统一难度。
- 进度数据只增不改结构，无需迁移脚本。
