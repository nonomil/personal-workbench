# 方块世界 · 成长陪伴与工作台结合

- 日期：2026-08-15，状态：方案（未改代码）
- 账本合同：沿用 bridge 现有 API；`quest-<id>` 生涯一次、`daily-<日期>` 每日，不改 key。

## 1. 已接通的部分（保持不动）

- 任务/每日挑战完成发阳光（日限 80 内）；里程碑 `ms-voxel-5/12` 自动判定。
- `getPlayMods()` 已接（三游戏中唯一），识字档影响游戏参数。
- 每日打卡戳、周目标、三界同日均经 `recordPlaySession`。
- 冒险等级反哺：`voxelCrystalBonus`（4 级 +1 → 7 级 +2 晶体产量）。

## 2. 家园展示：创造力的"学习证据"

这是方块世界独有的陪伴抓手——孩子的建造成果应该被看见（对应外部笔记"学习证据优先于打勾"）：

- **家园快照**：游戏内"拍照"按钮把当前网格渲染成缩略图数据存进 progress（如 `homeSnapshot: {grid, date, blocks}`，注意 localStorage 体积，只存最新 1 张 + 方块计数）。
- **工作台成长页展示**：成长页新增"我的家园"小卡，渲染快照 + "已用 46 块"；点击跳转游戏。孩子给家长看工作台时，作品即证据。
- **周报接入**：`getWeeklyReport` 的方块世界行附一句事实（"本周建造 +32 块，家园更新于周四"）。

## 3. 陪伴层

- **星芒工地监理**：像素小像蹲在热键栏旁；完成任务蹦跳庆祝；每日挑战完成说"今天的活干完啦"；连续 3 天完成每日挑战给专属夸奖。文案池 12 条，围绕"搭建/收集"动词。
- **升段仪式**：rank 提升时弹发放卡（新镐图标 + 称号 + 星芒），替代现在静默的段位数字变化；同时解锁的挖矿门禁在卡上讲清楚（"石镐到手，可以挖石头啦"）。
- **结算三行规范**（总纲）：任务完成弹层展示所得阳光、冒险等级进度、下一个目标（最近未完成任务或里程碑）。

## 4. 长线节奏

- **短**：每日挑战 1 条（≤5 分钟）+ 自由建造。
- **中**：一周 2–3 个生涯任务，段位一至两周升 1 级。
- **长**：12+6 任务 → `ms-voxel-12` 徽章 →"方块大师"→ 三期矿洞 biome 开新循环。
- 与学习绑定：识字里程碑（250/500 字）可在工作台侧奖励"限定方块配色"（纯 CSS 滤镜实现，不新增货币），把学习成果带进游戏世界。
