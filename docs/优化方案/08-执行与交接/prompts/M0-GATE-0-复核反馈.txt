复核反馈：你上一轮判定 BLOCKED 基本正确，但需要纠正两个含糊点，然后给出最终收敛答案。本轮仍不得调用工具或修改文件。

问题一：HEAD 168ad67 只能恢复提交态，不能恢复当前 701 项未提交变化。所谓“HEAD + 已通过测试的状态”不是可复现快照。当前规则又禁止回滚、清理、暂存、提交和推送混合工作树。请明确：在不提交、不 stash、不改工作树的约束下，最小安全恢复点究竟是什么？如果只能做外部只读归档或 patch/bundle/manifest，请写清它是否足以恢复未跟踪文件、删除、重命名和二进制资产；不要声称普通 git diff 可以完整恢复。

问题二：你建议“首个代码任务不得破坏五入口旧合同，除非先有合同切换任务”。新产品方向已经批准，所以正确顺序应是先建立恢复与迁移边界，再把合同测试切换为新目标并让它们预期失败，然后才改实现；不是长期维持旧合同。请明确合同切换任务应处在哪个门禁后、实现任务前。

请只输出：
1. 修订后的门禁结论。
2. 唯一下一步任务卡的名称、输入、允许写入范围、明确禁止项、完成证据。
3. 后续严格顺序（最多 4 步）：恢复/迁移边界、合同测试、最小实现切片、发布资产减重。
4. 指出哪些恢复措施需要用户单独授权，不能由执行代理擅自做。

不要再次展开产品愿景，不要给多个备选任务，不要开始实现。
