# 自然拼读实施验收

- [ ] 每个 lesson 的 `newPatterns` 和 `reviewPatterns` 合法且有内容。
- [ ] 每个阶段有新课、复习、应用三种活动，不能只有字母卡。
- [ ] 可解码比例由测试计算，不能信任手填数字。
- [ ] 错题进入复习队列，答错不扣阳光，也不阻止跟读完成。
- [ ] `courseProgress.phonics` 若接入，只能作为现有 `courseProgress` 的子对象，不新增 localStorage key。
