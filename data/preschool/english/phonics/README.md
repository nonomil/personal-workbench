# 自然拼读运行数据

- `route.json`：60 日路线和课程合同。
- `lessons.json`：当前已落地的每日课程条目，扩展前先通过 schema/可解码 QA。
- `word-bank.json`：规范化词库种子。
- `sentence-bank.json`：规范化短句种子和可解码元数据。
- `review-rules.json`：掌握状态、复习间隔和错题规则。

自然拼读在网页上挂在英语专区，不新增顶层入口或第二套奖励账本。
