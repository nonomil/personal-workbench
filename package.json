{
  "name": "personal-workbench",
  "version": "0.5.1",
  "private": true,
  "description": "个人学习与成长工作台的网页与 Android APK 包装工程",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "release:verify": "node scripts/release-verify.mjs",
    "android:prepare": "node scripts/prepare-mobile.mjs",
    "android:init": "npm run android:prepare && cap add android",
    "android:sync": "npm run android:prepare && cap sync android",
    "android:build": "npm run android:sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "8.4.2"
  },
  "devDependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/cli": "8.4.2"
  }
}
